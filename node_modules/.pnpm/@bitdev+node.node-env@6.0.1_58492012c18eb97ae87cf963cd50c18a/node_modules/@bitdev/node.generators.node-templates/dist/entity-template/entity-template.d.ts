import { ComponentContext, ComponentFile, ComponentTemplate } from '@teambit/generator';
export type NodeComponentTemplateOptions = Partial<Pick<ComponentTemplate, 'name' | 'description' | 'hidden' | 'env'>>;
export declare class EntityTemplate implements ComponentTemplate {
    readonly name: string;
    readonly description: string;
    readonly hidden: boolean;
    readonly env?: string;
    constructor(name?: string, description?: string, hidden?: boolean, env?: string);
    generateFiles(context: ComponentContext): ComponentFile[];
    static from(options?: NodeComponentTemplateOptions): () => EntityTemplate;
}
