import type { Bundler, BundlerResult, Target, BundlerContextMetaData } from '@teambit/bundler';
import type { Logger } from '@teambit/logger';
import type { Configuration } from '@rspack/core';
import { EnvContext } from '@teambit/envs';
import { PubsubMain } from '@teambit/pubsub';
import { RspackBundlerOptions } from './rspack-bundler-options';
export type BundlerAppContext = {
    logger: Logger;
};
type RspackBundlerAspectsContext = {
    logger: Logger;
    workspacePath?: string;
    pubsub?: PubsubMain;
};
export declare class RspackBundler implements Bundler {
    /**
     * targets to bundle.
     */
    private targets;
    /**
     * rspack configuration.
     */
    private configs;
    private logger;
    private rspackModulePath;
    private metaData?;
    private readonly rspack;
    constructor(
    /**
     * targets to bundle.
     */
    targets: Target[], 
    /**
     * rspack configuration.
     */
    configs: Configuration[], logger: Logger, rspackModulePath?: string, metaData?: BundlerContextMetaData | undefined);
    run(): Promise<BundlerResult[]>;
    private getErrors;
    private getAssets;
    private getEntriesAssetsMap;
    private getComponents;
    static from(options: RspackBundlerOptions): (context: EnvContext) => RspackBundler;
    static create(options: RspackBundlerOptions, aspectContext: RspackBundlerAspectsContext): RspackBundler;
}
export {};
