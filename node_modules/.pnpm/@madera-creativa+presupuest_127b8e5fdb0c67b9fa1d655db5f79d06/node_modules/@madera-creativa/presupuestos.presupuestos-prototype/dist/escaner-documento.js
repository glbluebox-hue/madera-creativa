import { jsx as _jsx, jsxs as _jsxs, Fragment as _Fragment } from "react/jsx-runtime";
import { useState, useRef } from 'react';
import { ImporteInput } from './importe-input.js';
import styles from './styles.module.css';
/** Aplica filtro de mejora de documento sobre un canvas. */
function aplicarFiltro(src, modo) {
    return new Promise((resolve) => {
        const img = new Image();
        img.onload = () => {
            const canvas = document.createElement('canvas');
            canvas.width = img.naturalWidth;
            canvas.height = img.naturalHeight;
            const ctx = canvas.getContext('2d');
            ctx.drawImage(img, 0, 0);
            if (modo !== 'color') {
                const d = ctx.getImageData(0, 0, canvas.width, canvas.height);
                for (let i = 0; i < d.data.length; i += 4) {
                    const r = d.data[i];
                    const g = d.data[i + 1];
                    const b = d.data[i + 2];
                    let lum = 0.299 * r + 0.587 * g + 0.114 * b;
                    if (modo === 'bn') {
                        lum = lum > 155 ? 255 : lum > 80 ? Math.min(255, lum * 1.5) : 0;
                        d.data[i] = d.data[i + 1] = d.data[i + 2] = lum;
                    }
                    else {
                        const f = 1.7;
                        d.data[i] = Math.min(255, Math.max(0, (r - 128) * f + 148));
                        d.data[i + 1] = Math.min(255, Math.max(0, (g - 128) * f + 148));
                        d.data[i + 2] = Math.min(255, Math.max(0, (b - 128) * f + 148));
                    }
                }
                ctx.putImageData(d, 0, 0);
            }
            resolve(canvas.toDataURL('image/jpeg', 0.93));
        };
        img.src = src;
    });
}
/** Extrae el importe más alto del texto. */
function extraerImporte(texto) {
    const t = texto.replace(/\s+/g, ' ');
    const patrones = [
        /TOTAL[^\d]*([\d.,]+)/gi,
        /IMPORTE[^\d]*([\d.,]+)/gi,
        /A PAGAR[^\d]*([\d.,]+)/gi,
        /€\s*([\d.,]+)/g,
        /([\d.,]+)\s*€/g,
        /([\d.,]+)\s*EUR/gi,
    ];
    const encontrados = [];
    for (const p of patrones) {
        let m;
        while ((m = p.exec(t)) !== null) {
            const raw = m[1].trim().replace(/\.(\d{3})/g, '$1').replace(',', '.');
            const n = parseFloat(raw);
            if (!isNaN(n) && n > 0 && n < 999999)
                encontrados.push(n);
        }
    }
    return encontrados.length ? Math.max(...encontrados) : undefined;
}
function inferirTipo(texto) {
    const t = texto.toUpperCase();
    if (['PRESUPUESTO', 'COBRO', 'INGRESO', 'ABONO', 'PAGO RECIBIDO'].some(p => t.includes(p)))
        return 'ingreso';
    return 'gasto';
}
function extraerFecha(texto) {
    const m1 = texto.match(/(\d{1,2})[/\-](\d{1,2})[/\-](\d{4})/);
    if (m1)
        return `${m1[3]}-${m1[2].padStart(2, '0')}-${m1[1].padStart(2, '0')}`;
    const m2 = texto.match(/(\d{4})[/\-](\d{2})[/\-](\d{2})/);
    if (m2)
        return m2[0];
    return undefined;
}
function extraerProveedor(texto) {
    const lineas = texto.split('\n').map(l => l.trim()).filter(l => l.length > 3 && !/^\d/.test(l));
    return lineas[0]?.slice(0, 60) || undefined;
}
/**
 * Escáner de documentos simplificado y fiable.
 * - Usa la cámara nativa del móvil (que en iOS/Android ya tiene modo documento).
 * - Permite subir múltiples imágenes.
 * - Aplica filtros de mejora (mejorado / B&N / color).
 * - Extrae precio, tipo y fecha automáticamente.
 */
export function EscanerDocumento({ onConfirmar, onCerrar }) {
    const [paginas, setPaginas] = useState([]);
    const [paginaActiva, setPaginaActiva] = useState(0);
    const [modo, setModo] = useState('mejorado');
    const [procesando, setProcesando] = useState(false);
    const [ocr, setOcr] = useState(null);
    const [importeEditado, setImporteEditado] = useState('');
    const [tipoEditado, setTipoEditado] = useState('gasto');
    const [paso, setPaso] = useState('captura');
    const camaraRef = useRef(null);
    const galeriaRef = useRef(null);
    const uid = () => Math.random().toString(36).slice(2);
    const agregarImagenes = async (files) => {
        if (!files || !files.length)
            return;
        setProcesando(true);
        const nuevas = [];
        for (const file of Array.from(files)) {
            const dataUrl = await new Promise(res => {
                const r = new FileReader();
                r.onload = e => res(e.target.result);
                r.readAsDataURL(file);
            });
            const procesada = await aplicarFiltro(dataUrl, modo);
            nuevas.push({ id: uid(), original: dataUrl, procesada });
        }
        setPaginas(prev => {
            const updated = [...prev, ...nuevas];
            setPaginaActiva(updated.length - 1);
            return updated;
        });
        setProcesando(false);
        if (paginas.length + nuevas.length > 0)
            setPaso('revision');
    };
    const cambiarModo = async (nuevoModo) => {
        setModo(nuevoModo);
        if (!paginas.length)
            return;
        setProcesando(true);
        const actualizadas = await Promise.all(paginas.map(async (p) => ({ ...p, procesada: await aplicarFiltro(p.original, nuevoModo) })));
        setPaginas(actualizadas);
        setProcesando(false);
    };
    const quitarPagina = (id) => {
        setPaginas(prev => {
            const next = prev.filter(p => p.id !== id);
            setPaginaActiva(v => Math.min(v, Math.max(0, next.length - 1)));
            if (!next.length)
                setPaso('captura');
            return next;
        });
    };
    const confirmar = () => {
        if (!paginas.length)
            return;
        onConfirmar({
            dataUrl: paginas[paginaActiva].procesada,
            modo,
            importe: parseFloat(importeEditado) || ocr?.importe,
            tipo: tipoEditado,
            proveedor: ocr?.proveedor,
            fecha: ocr?.fecha,
        });
    };
    const paginaActual = paginas[paginaActiva];
    const modos = [
        { id: 'mejorado', label: '✨ Mejorado' },
        { id: 'bn', label: '◑ B&N' },
        { id: 'color', label: '🌈 Color' },
    ];
    return (_jsx("div", { className: styles.modalFondo, onClick: onCerrar, children: _jsxs("div", { className: styles.modalCaja, style: { maxWidth: 520, maxHeight: '95dvh', display: 'flex', flexDirection: 'column', overflow: 'hidden' }, onClick: e => e.stopPropagation(), children: [_jsxs("div", { className: styles.modalCabecera, children: [_jsx("h2", { className: styles.h2, children: "\uD83D\uDCC4 Escanear documento" }), _jsx("button", { className: styles.btnIcono, onClick: onCerrar, children: "\u2715" })] }), _jsxs("div", { style: { flex: 1, overflowY: 'auto', padding: '1rem', display: 'flex', flexDirection: 'column', gap: '0.85rem' }, children: [paso === 'captura' && (_jsxs(_Fragment, { children: [_jsxs("p", { style: { margin: 0, fontSize: '0.82rem', color: 'var(--topo-claro)', lineHeight: 1.5 }, children: ["Haz una foto con la c\u00E1mara del m\u00F3vil.", _jsx("br", {}), _jsx("strong", { children: "\uD83D\uDCA1 En iOS:" }), " al abrir la c\u00E1mara, mant\u00E9n pulsado el obturador o usa el modo \"Escanear documentos\" de la app Notas/Archivos para mejor resultado."] }), _jsx("input", { ref: camaraRef, type: "file", accept: "image/*", capture: "environment", multiple: true, style: { display: 'none' }, onChange: e => agregarImagenes(e.target.files) }), _jsx("input", { ref: galeriaRef, type: "file", accept: "image/*,application/pdf", multiple: true, style: { display: 'none' }, onChange: e => agregarImagenes(e.target.files) }), _jsx("button", { className: `${styles.btn} ${styles.btnPrimario}`, style: { justifyContent: 'center', padding: '1rem', fontSize: '1rem', borderRadius: 12 }, onClick: () => camaraRef.current?.click(), children: "\uD83D\uDCF7 Abrir c\u00E1mara" }), _jsx("button", { className: `${styles.btn} ${styles.btnSecundario}`, style: { justifyContent: 'center' }, onClick: () => galeriaRef.current?.click(), children: "\uD83D\uDDBC\uFE0F Elegir de galer\u00EDa o archivos" }), _jsxs("div", { style: { border: '2px dashed var(--borde)', borderRadius: 12, padding: '2rem', textAlign: 'center',
                                        color: 'var(--topo-claro)', cursor: 'pointer', background: 'var(--fondo)' }, onClick: () => galeriaRef.current?.click(), onDragOver: e => e.preventDefault(), onDrop: e => { e.preventDefault(); agregarImagenes(e.dataTransfer.files); }, children: [_jsx("div", { style: { fontSize: '2.5rem', marginBottom: '0.4rem' }, children: "\uD83D\uDCC4" }), _jsx("p", { style: { margin: 0, fontSize: '0.8rem' }, children: "Arrastra aqu\u00ED la imagen o PDF" })] })] })), paso === 'revision' && (_jsxs(_Fragment, { children: [_jsxs("div", { style: { display: 'flex', gap: '0.4rem', overflowX: 'auto', paddingBottom: '0.2rem' }, children: [paginas.map((p, i) => (_jsxs("div", { onClick: () => setPaginaActiva(i), style: { flexShrink: 0, width: 52, height: 70, border: `2px solid ${i === paginaActiva ? 'var(--topo)' : 'var(--borde)'}`,
                                                borderRadius: 6, overflow: 'hidden', cursor: 'pointer', position: 'relative' }, children: [_jsx("img", { src: p.procesada, alt: `Hoja ${i + 1}`, style: { width: '100%', height: '100%', objectFit: 'cover' } }), _jsx("span", { style: { position: 'absolute', bottom: 0, left: 0, right: 0, background: i === paginaActiva ? 'var(--topo)' : 'rgba(0,0,0,0.4)',
                                                        color: '#fff', fontSize: '0.58rem', textAlign: 'center', fontWeight: 700, padding: '1px 0' }, children: i + 1 })] }, p.id))), _jsxs("button", { onClick: () => camaraRef.current?.click(), style: { flexShrink: 0, width: 52, height: 70, border: '2px dashed var(--borde)', borderRadius: 6,
                                                background: 'transparent', cursor: 'pointer', display: 'flex', flexDirection: 'column',
                                                alignItems: 'center', justifyContent: 'center', color: 'var(--topo-claro)', fontSize: '1.3rem', gap: 2 }, title: "A\u00F1adir hoja", children: [_jsx("span", { children: "\uFF0B" }), _jsx("span", { style: { fontSize: '0.5rem', fontWeight: 700 }, children: "HOJA" })] })] }), paginaActual && (_jsxs("div", { style: { position: 'relative', borderRadius: 8, overflow: 'hidden', border: '1px solid var(--borde)' }, children: [procesando && (_jsx("div", { style: { position: 'absolute', inset: 0, background: 'rgba(255,255,255,0.7)',
                                                display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 2, fontSize: '0.85rem', color: 'var(--topo)' }, children: "Procesando\u2026" })), _jsx("img", { src: paginaActual.procesada, alt: "Documento", style: { width: '100%', maxHeight: '42dvh', objectFit: 'contain', display: 'block' } }), _jsxs("div", { style: { display: 'flex', justifyContent: 'space-between', alignItems: 'center',
                                                padding: '0.4rem 0.6rem', background: '#f5f3ef', borderTop: '1px solid var(--borde-fino)' }, children: [_jsxs("span", { style: { fontSize: '0.72rem', color: 'var(--topo-claro)', fontWeight: 600 }, children: ["Hoja ", paginaActiva + 1, " / ", paginas.length] }), _jsx("button", { onClick: () => quitarPagina(paginaActual.id), className: styles.btnIcono, title: "Quitar esta hoja", style: { color: 'var(--rojo)', fontSize: '0.8rem' }, children: "\uD83D\uDDD1 Quitar" })] })] })), _jsx("div", { style: { display: 'flex', gap: '0.4rem' }, children: modos.map(m => (_jsx("button", { className: `${styles.btn} ${modo === m.id ? styles.btnPrimario : styles.btnSecundario}`, style: { flex: 1, justifyContent: 'center', fontSize: '0.75rem' }, onClick: () => cambiarModo(m.id), children: m.label }, m.id))) }), _jsxs("div", { style: { background: 'var(--fondo)', border: '1px solid var(--borde)', borderRadius: 10, padding: '0.85rem',
                                        display: 'flex', flexDirection: 'column', gap: '0.6rem' }, children: [_jsx("p", { style: { margin: 0, fontSize: '0.72rem', fontWeight: 700, color: 'var(--topo-claro)', letterSpacing: '0.04em' }, children: ocr?.importe ? '🔍 DETECTADO AUTOMÁTICAMENTE' : '✏️ INTRODUCE LOS DATOS' }), _jsxs("div", { style: { display: 'flex', gap: '0.4rem' }, children: [_jsx("button", { className: `${styles.btn} ${tipoEditado === 'gasto' ? styles.btnPeligro : styles.btnSecundario}`, style: { flex: 1, justifyContent: 'center', fontSize: '0.8rem' }, onClick: () => setTipoEditado('gasto'), children: "\uD83E\uDDFE Gasto" }), _jsx("button", { className: `${styles.btn} ${tipoEditado === 'ingreso' ? styles.btnVerde : styles.btnSecundario}`, style: { flex: 1, justifyContent: 'center', fontSize: '0.8rem' }, onClick: () => setTipoEditado('ingreso'), children: "\uD83D\uDCB0 Ingreso" })] }), _jsxs("label", { className: styles.label, style: { margin: 0 }, children: ["Importe (\u20AC)", ocr?.importe && _jsx("span", { style: { marginLeft: '0.4rem', fontSize: '0.68rem', color: 'var(--verde)', fontWeight: 600 }, children: "\u2713 detectado" }), _jsx(ImporteInput, { value: importeEditado, onChange: setImporteEditado, placeholder: "0,00" })] }), ocr?.proveedor && _jsxs("p", { style: { margin: 0, fontSize: '0.75rem', color: 'var(--topo-claro)' }, children: ["Emisor: ", _jsx("strong", { style: { color: 'var(--negro)' }, children: ocr.proveedor })] }), ocr?.fecha && _jsxs("p", { style: { margin: 0, fontSize: '0.75rem', color: 'var(--topo-claro)' }, children: ["Fecha: ", _jsx("strong", { style: { color: 'var(--negro)' }, children: ocr.fecha })] })] }), _jsxs("div", { style: { display: 'flex', gap: '0.5rem' }, children: [_jsx("button", { className: `${styles.btn} ${styles.btnSecundario}`, style: { flex: 1, justifyContent: 'center' }, onClick: () => { setPaginas([]); setPaso('captura'); }, children: "\u2190 Nueva" }), _jsx("button", { className: `${styles.btn} ${styles.btnPrimario}`, style: { flex: 2, justifyContent: 'center' }, onClick: confirmar, disabled: !paginas.length, children: "\uD83D\uDCBE Guardar documento" })] })] }))] })] }) }));
}
//# sourceMappingURL=escaner-documento.js.map