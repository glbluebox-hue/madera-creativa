export type SortOptions<T> = {
    /**
     * key to use for sorting.
     */
    key?: string;
    /**
     * values to sort.
     */
    values?: T;
    /**
     * direction for sorting.
     */
    direction?: 'asc' | 'desc';
};
