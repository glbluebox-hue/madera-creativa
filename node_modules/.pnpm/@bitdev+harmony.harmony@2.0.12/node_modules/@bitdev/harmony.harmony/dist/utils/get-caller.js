import { head, last } from 'lodash-es';
export function getCallerPath() {
    const error = new Error();
    const stackTrace = error.stack?.split('\n').at(3);
    const fileSplit = stackTrace.split('file://');
    const pathWithPosition = last(fileSplit);
    const path = head(pathWithPosition.split(':'));
    const cleanPath = path.substring(path.indexOf('/'), path.length);
    return cleanPath;
}
//# sourceMappingURL=get-caller.js.map