import { Extension } from '../extension/extension.js';
export function extensionFactory(manifest) {
    // to allow the use of `provide` as an alias to `provider` in ExtensionManifest
    // eslint-disable-next-line no-param-reassign
    if (manifest.provide)
        manifest.provider = manifest.provide;
    return new Extension(manifest);
}
//# sourceMappingURL=extension-factory.js.map