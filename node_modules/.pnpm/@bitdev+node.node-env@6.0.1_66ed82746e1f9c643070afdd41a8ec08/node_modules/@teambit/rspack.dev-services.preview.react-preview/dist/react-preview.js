"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ReactPreview = void 0;
const object_hash_1 = __importDefault(require("object-hash"));
const rspack_rspack_dev_server_1 = require("@teambit/rspack.rspack-dev-server");
const rspack_rspack_bundler_1 = require("@teambit/rspack.rspack-bundler");
const rspack_caching_transformer_1 = require("./rspack-caching.transformer");
const rspack_error_reporting_transformer_1 = require("./rspack-error-reporting.transformer");
const rspack_hmr_transformer_1 = require("./rspack-hmr.transformer");
class ReactPreview {
    constructor(name, mounter = require.resolve('./mounter'), docsTemplate = require.resolve('./docs'), previewConfig = {}, transformers = [], hostDependencies, devServer, bundler, rspackModulePath = require.resolve('@rspack/core'), rspackDevServerModulePath = require.resolve('@rspack/dev-server'), useSource = true) {
        this.name = name;
        this.mounter = mounter;
        this.docsTemplate = docsTemplate;
        this.previewConfig = previewConfig;
        this.transformers = transformers;
        this.hostDependencies = hostDependencies;
        this.devServer = devServer;
        this.bundler = bundler;
        this.rspackModulePath = rspackModulePath;
        this.rspackDevServerModulePath = rspackDevServerModulePath;
        this.useSource = useSource;
    }
    getDevServer(context) {
        if (this.devServer)
            return () => this.devServer;
        return rspack_rspack_dev_server_1.RspackDevServer.from({
            rspackModulePath: this.rspackModulePath,
            rspackDevServerModulePath: this.rspackDevServerModulePath,
            devServerContext: context,
            transformers: [
                (0, rspack_caching_transformer_1.createCacheControlTransformer)(),
                (0, rspack_error_reporting_transformer_1.createErrorReportingTransformer)(),
                (0, rspack_hmr_transformer_1.createHmrTransformer)(),
                ...this.transformers,
            ],
        });
    }
    getDevEnvId() {
        const objToHash = {
            rspackModulePath: this.rspackModulePath,
            rspackDevServerModulePath: this.rspackDevServerModulePath,
            transformers: this.transformers,
            hostDependencies: this.getHostDependencies().sort(),
        };
        const devEnvId = (0, object_hash_1.default)(objToHash);
        return devEnvId;
    }
    getBundler(context) {
        if (this.bundler)
            return () => this.bundler;
        return rspack_rspack_bundler_1.RspackBundler.from({
            targets: context.targets,
            transformers: [...this.transformers],
            bundlerContext: context,
            rspackModulePath: this.rspackModulePath,
        });
    }
    getTemplateBundler(context) {
        return rspack_rspack_bundler_1.RspackBundler.from({
            targets: context.targets,
            transformers: [...this.transformers],
            bundlerContext: context,
            rspackModulePath: this.rspackModulePath,
        });
    }
    getHostDependencies() {
        return this.hostDependencies || ReactPreview.hostDependencies;
    }
    getMounter() {
        return this.mounter;
    }
    getDocsTemplate() {
        return this.docsTemplate;
    }
    getPreviewConfig() {
        return {
            strategyName: 'component',
            splitComponentBundle: true,
            // @ts-ignore - supportsUseSource is not yet in EnvPreviewConfig type
            supportsUseSource: this.useSource,
            ...this.previewConfig,
        };
    }
    static from(options) {
        const name = options.name || 'react-preview';
        return () => {
            return new ReactPreview(name, options.mounter, options.docsTemplate, options.previewConfig, options.transformers, options.hostDependencies, options.devServer, options.bundler, options.rspackModulePath, options.rspackDevServerModulePath, options.useSource ?? true);
        };
    }
}
exports.ReactPreview = ReactPreview;
/**
 * Default host dependencies for the react preview.
 */
ReactPreview.hostDependencies = [
    '@teambit/mdx.ui.mdx-scope-context',
    '@mdx-js/react',
    'react',
    'react-dom'
];
//# sourceMappingURL=react-preview.js.map