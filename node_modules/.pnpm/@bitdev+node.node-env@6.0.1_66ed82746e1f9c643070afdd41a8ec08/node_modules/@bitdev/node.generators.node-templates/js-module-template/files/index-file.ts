import { ComponentContext } from '@teambit/generator';

export const indexFile = (context: ComponentContext) => {
  const { name, namePascalCase: nameCamelCase } = context;

  return {
    relativePath: 'index.js',
    content: `export { ${nameCamelCase} } from './${name}';
`,
  };
};
