import { OxlintFormat, OxlintNodeOptions } from './options';
import { Plugins } from './plugins';
import { Rules } from './rules';
import { Fixes } from './fixes';
export type OxlintMultiFormatResult = Partial<Record<OxlintFormat, any>>;
export declare class OxlintNode {
    binPath: string;
    plugins: Plugins;
    rules: Rules;
    fixes: Fixes;
    formats: OxlintFormat[];
    configPath?: string | undefined;
    tsconfigPath?: string | undefined;
    private options;
    private tsgolintBinPath?;
    constructor(binPath: string, plugins: Plugins, rules: Rules, fixes: Fixes, formats?: OxlintFormat[], configPath?: string | undefined, tsconfigPath?: string | undefined, options?: Omit<OxlintNodeOptions, 'pluginsFlags' | 'rulesFlags' | 'fixesFlags' | 'formats' | 'configPath' | 'tsconfigPath' | 'binPath' | 'tsgolintBinPath'>, tsgolintBinPath?: string | undefined);
    version(): string;
    private getConfigCliArg;
    private getTsConfigCliArg;
    private getFormatCliArg;
    private getExtraCliArgs;
    run(paths?: string[]): Promise<OxlintMultiFormatResult>;
    toCliArgs(format: OxlintFormat, paths?: string[]): string[];
    static create(options: OxlintNodeOptions): OxlintNode;
}
