"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.metaFromPackageJson = metaFromPackageJson;
function _fs() {
  const data = require("fs");
  _fs = function () {
    return data;
  };
  return data;
}
function _path() {
  const data = require("path");
  _path = function () {
    return data;
  };
  return data;
}
function _findRoot() {
  const data = _interopRequireDefault(require("find-root"));
  _findRoot = function () {
    return data;
  };
  return data;
}
function _componentId() {
  const data = require("@teambit/component-id");
  _componentId = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function metaFromPackageJson(filepath) {
  const root = safeFindRoot(filepath);
  if (!root) return undefined;
  const id = extractMetadata((0, _path().join)(root, 'package.json'));
  return id;
}
function extractMetadata(pkgPath) {
  const pkg = parseJsonFile(pkgPath);
  if (!pkg) return undefined;
  const compId = pkg.componentId;
  const homepage = typeof pkg.homepage === 'string' ? pkg.homepage : undefined;
  const exported = pkg.exported !== false;
  if (!compId || !_componentId().ComponentID.isValidObject(compId)) return undefined;
  try {
    const parsed = _componentId().ComponentID.fromObject(compId);
    return {
      id: parsed.toString(),
      homepage,
      exported
    };
  } catch {
    return undefined;
  }
}
function parseJsonFile(pkgPath) {
  if (!(0, _fs().existsSync)(pkgPath)) return undefined;
  try {
    const content = (0, _fs().readFileSync)(pkgPath, 'utf-8');
    const json = JSON.parse(content);
    return json;
  } catch {
    return undefined;
  }
}
function safeFindRoot(filepath) {
  try {
    return (0, _findRoot().default)(filepath);
  } catch {
    // might happen for "scaffolding" files outside the project.
    return undefined;
  }
}

//# sourceMappingURL=meta-from-pkg-json.js.map