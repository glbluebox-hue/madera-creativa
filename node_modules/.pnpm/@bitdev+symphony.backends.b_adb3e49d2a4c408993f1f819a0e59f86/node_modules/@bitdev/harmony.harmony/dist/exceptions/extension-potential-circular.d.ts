import { Extension } from '../extension/extension.js';
import { HarmonyError } from './harmony-error.js';
export default class ExtensionPotentialCircular extends HarmonyError {
    /**
     * failed extension
     */
    private extension;
    /**
     * valid extensions dependencies
     */
    private validDeps;
    constructor(
    /**
     * failed extension
     */
    extension: Extension, 
    /**
     * valid extensions dependencies
     */
    validDeps: Extension[]);
    toString(): string;
}
