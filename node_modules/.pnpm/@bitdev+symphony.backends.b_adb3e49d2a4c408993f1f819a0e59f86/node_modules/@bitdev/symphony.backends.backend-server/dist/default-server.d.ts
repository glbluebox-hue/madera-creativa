import type { BackendServer, BackendContext } from './backend-server.js';
import { DefaultRESTRoute } from './rest-route.js';
export type DefaultServerOptions = {
    /**
     * name of the backend server.
     */
    name: string;
    /**
     * routes to load.
     */
    routes?: DefaultRESTRoute;
    /**
     * enables cors.
     */
    cors?: boolean;
};
export declare class DefaultServer implements BackendServer {
    run({ name, ...context }: BackendContext): Promise<{
        appName: string;
        port: number;
        url: string;
        stop: () => Promise<void>;
    }>;
}
