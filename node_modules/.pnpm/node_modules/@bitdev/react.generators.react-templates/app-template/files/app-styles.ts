import { ComponentContext } from '@teambit/generator';

export const appStylesFile = (context: ComponentContext) => {
  const { name } = context;

  return {
    relativePath: `${name}.module.css`,
    content: `:global(body) {
  margin: 0;
}
`,
  };
};