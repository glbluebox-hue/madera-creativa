import type { ComponentContext, ComponentFile, ComponentTemplate } from '@teambit/generator';
export type RspackReactAppTemplateOptions = Partial<Pick<ComponentTemplate, 'name' | 'description' | 'hidden' | 'env'>>;
/**
 * Generates a React app component bound to the rspack app type
 * (`@teambit/rspack.app-types.react-rspack`) — the same app type used by
 * `teambit.rspack/envs/react-env` and `teambit.cloud/envs/cloud-react`.
 */
export declare class RspackReactAppTemplate implements ComponentTemplate {
    readonly name: string;
    readonly description: string;
    readonly hidden: boolean;
    readonly env?: string | undefined;
    constructor(name?: string, description?: string, hidden?: boolean, env?: string | undefined);
    generateFiles(context: ComponentContext): ComponentFile[];
    static from(options?: RspackReactAppTemplateOptions): () => RspackReactAppTemplate;
}
