import { WorkspaceContext, WorkspaceTemplate } from '@teambit/generator';
import { ComponentID } from '@teambit/component-id';
export type NodeWorkspaceStarterOptions = {
    /**
     * name of the template
     */
    name?: string;
    /**
     * description of the template.
     */
    description?: string;
    /**
     * hide the template from the `bit templates` command.
     */
    hidden?: boolean;
    /**
     * the env to use for the generated workspace.
     * use this when registering the starter without an aspect.
     */
    env?: string;
};
export declare class NodeWorkspaceStarter implements WorkspaceTemplate {
    readonly name: string;
    readonly description: string;
    readonly hidden: boolean;
    readonly env?: string;
    constructor(name?: string, description?: string, hidden?: boolean, env?: string);
    generateFiles(context: WorkspaceContext): Promise<{
        relativePath: string;
        content: string;
    }[]>;
    getAppId(context: WorkspaceContext): ComponentID;
    fork(context: WorkspaceContext): ({
        id: string;
        targetName: string;
        targetScope?: undefined;
    } | {
        id: string;
        targetName: string;
        targetScope: string;
    })[];
    static from(options?: NodeWorkspaceStarterOptions): () => NodeWorkspaceStarter;
}
