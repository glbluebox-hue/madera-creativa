"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.appSpecFile = void 0;
const lodash_1 = require("lodash");
const appSpecFile = (context) => {
    const { name, namePascalCase, nameCamelCase } = context;
    const spaced = (0, lodash_1.replace)(name, new RegExp('-', 'g'), ' ');
    return {
        relativePath: `${name}.spec.ts`,
        content: `import { ${namePascalCase} } from './${name}.js';

describe('${spaced}', () => {
  it('should say hello', async () => {
    const ${nameCamelCase} = ${namePascalCase}.from();
    const greeting = await ${nameCamelCase}.getHello();
    expect(greeting).toEqual('Hello World!');
  })
});
    `,
    };
};
exports.appSpecFile = appSpecFile;
//# sourceMappingURL=app-test.js.map