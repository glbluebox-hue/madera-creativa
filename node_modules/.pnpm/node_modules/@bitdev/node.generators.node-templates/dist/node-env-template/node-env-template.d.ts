import { ComponentContext, ComponentFile, ComponentTemplate } from '@teambit/generator';
export type NodeEnvTemplateOptions = {
    /**
     * name of the template
     */
    name?: string;
    /**
     * description of the template.
     */
    description?: string;
    /**
     * hide the template from the `bit templates` command.
     */
    hidden?: boolean;
};
export declare class NodeEnvTemplate implements ComponentTemplate {
    readonly name: string;
    readonly description: string;
    readonly hidden: boolean;
    readonly env: string;
    readonly isEnv: boolean;
    readonly installMissingDependencies: boolean;
    constructor(name?: string, description?: string, hidden?: boolean, env?: string, isEnv?: boolean, installMissingDependencies?: boolean);
    generateFiles(context: ComponentContext): ComponentFile[];
    static from(options?: NodeEnvTemplateOptions): () => NodeEnvTemplate;
}
