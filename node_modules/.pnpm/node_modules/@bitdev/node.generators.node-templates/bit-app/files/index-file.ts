import { ComponentContext } from '@teambit/generator';

export const indexFile = (context: ComponentContext) => {
  const { name, namePascalCase } = context;

  return {
    relativePath: 'index.ts',
    content: `export { run${namePascalCase} } from './${name}.js';
`,
  };
};
