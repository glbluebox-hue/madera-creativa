import { jsx as _jsx, Fragment as _Fragment } from "react/jsx-runtime";
import Tippy from '@tippyjs/react/headless';
import { ComponentStrip } from './component-strip';
import styles from './label.module.scss';
// a popper ("tooltip") that shows the additional React Components related to this dom element
export function OtherComponentsPopper({ components, children, start, end = -1, placement = 'bottom', interactive = true, ...tippyProps }) {
    const content = (_jsx(_Fragment, { children: components
            .slice(start, end)
            .reverse()
            .map((comp, idx) => (_jsx(ComponentStrip, { component: comp }, idx))) }));
    return (_jsx(Tippy, { placement: placement, interactive: interactive, ...tippyProps, 
        // second parameter "content" is always undefined, use content inline
        // https://github.com/atomiks/tippyjs-react/issues/341
        render: (attrs) => (_jsx("div", { ...attrs, className: styles.othersContainer, children: content })), children: children }));
}
//# sourceMappingURL=other-components.js.map