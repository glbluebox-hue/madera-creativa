import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useMemo, forwardRef } from 'react';
import { NativeLink } from '@teambit/base-ui.routing.native-link';
import { ComponentID } from '@teambit/component-id';
import { ScopeUrl } from '@teambit/component.modules.component-url';
import { componentMetaField, } from '@teambit/react.ui.highlighter.component-metadata.bit-component-meta';
import styles from './component-strip.module.scss';
import { calcComponentLink } from './links';
function extractMetadata(metadata) {
    if (typeof metadata === 'string') {
        return { id: metadata, exported: true };
    }
    return metadata[componentMetaField];
}
function LabelBlock({ link, children, className }) {
    const Comp = link ? NativeLink : 'span';
    return (_jsx(Comp, { href: link, external: !!link, className: className, children: children }));
}
export const ComponentStrip = forwardRef(function ComponentStrip({ component, children }, ref) {
    const { id, homepage, exported } = extractMetadata(component);
    const parsedId = useMemo(() => ComponentID.tryFromString(id), [id]);
    const componentLink = homepage || calcComponentLink(parsedId, exported);
    return (_jsxs("div", { className: styles.componentStrip, ref: ref, children: [!parsedId && _jsx(LabelBlock, { link: homepage, children: id }), parsedId && _jsx(LabelBlock, { link: ScopeUrl.toUrl(parsedId.scope), children: parsedId.scope }), parsedId && (_jsxs(LabelBlock, { link: componentLink, className: styles.nameBlock, children: [_jsx("span", { children: parsedId.fullName }), parsedId.version && parsedId.version !== 'latest' && (_jsxs("span", { className: styles.version, children: ["@", parsedId.version] }))] })), children] }));
});
//# sourceMappingURL=component-strip.js.map