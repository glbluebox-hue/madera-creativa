"use strict";

function _chai() {
  const data = require("chai");
  _chai = function () {
    return data;
  };
  return data;
}
function _scopeUrl() {
  const data = require("./scope-url");
  _scopeUrl = function () {
    return data;
  };
  return data;
}
function _constants() {
  const data = require("./constants");
  _constants = function () {
    return data;
  };
  return data;
}
describe('scope url', () => {
  it('should convert to url', () => {
    const id = 'teambit.base-ui';
    const result = _scopeUrl().ScopeUrl.toUrl(id);
    (0, _chai().expect)(result).to.equal(`${_constants().baseUrl}/teambit/base-ui`);
  });
  it('should convert to url for single-part scope', () => {
    const id = 'ioncannon';
    const result = _scopeUrl().ScopeUrl.toUrl(id);
    (0, _chai().expect)(result).to.equal(`${_constants().baseUrl}/ioncannon`);
  });
});
describe('scope pathname', () => {
  it('should convert to toPathname', () => {
    const id = 'teambit.base-ui';
    const result = _scopeUrl().ScopeUrl.toPathname(id);
    (0, _chai().expect)(result).to.equal(`teambit/base-ui`);
  });
  it('should convert to url', () => {
    const id = 'ioncannon';
    const result = _scopeUrl().ScopeUrl.toPathname(id);
    (0, _chai().expect)(result).to.equal(`ioncannon`);
  });
});

//# sourceMappingURL=scope-url.spec.js.map