import { RspackConfigTransformer } from '@teambit/rspack.modules.rspack-config-mutator';
/**
 * Creates an RSPack transformer that injects the error reporting script
 * into HTML files via a custom RSPack plugin using processAssets hook.
 */
export declare function createErrorReportingTransformer(): RspackConfigTransformer;
