import { type Control } from './composition-live-controls';
export type UseLiveControlsResult = {
    defs: Array<Control>;
    values: Record<string, any>;
    ready: boolean;
    hasLiveControls: boolean;
    onChange: (key: string, value: any) => void;
    setTimestamp: (ts: number) => void;
};
export declare function useLiveControls(channels?: string[]): UseLiveControlsResult;
