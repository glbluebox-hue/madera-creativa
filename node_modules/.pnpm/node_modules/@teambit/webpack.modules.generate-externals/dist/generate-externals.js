"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.generateExternals = generateExternals;
/**
 * Get's a list of dependencies and create an externals object out of them
 * @param dependencies
 * @param options
 * @returns
 */
function generateExternals(dependencies, options = {}) {
  const externals = dependencies.reduce((acc, dependency) => {
    acc[dependency] = options.transformName ? options.transformName(dependency) : dependency;
    return acc;
  }, {});
  return externals;
}

//# sourceMappingURL=generate-externals.js.map