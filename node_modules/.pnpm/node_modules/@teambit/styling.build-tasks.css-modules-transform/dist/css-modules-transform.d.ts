import type { BuildTask, BuildContext, BuiltTaskResult } from '@teambit/builder';
/**
 * Recursively finds files with a given extension in a directory.
 */
export declare function findFilesByExtension(dir: string, ext: string): string[];
/**
 * Transforms JS files to replace CSS module imports with plain CSS imports
 * and an inlined class name mapping object.
 */
export declare function transformJsFiles(distDir: string, originalCssPath: string, classNameMap: Record<string, string>): string[];
export declare class CssModulesTransformTask implements BuildTask {
    readonly aspectId = "";
    readonly name = "CssModulesTransform";
    readonly description = "Transforms CSS Modules into plain CSS with hashed class names so consumers need no special bundler config";
    execute(context: BuildContext): Promise<BuiltTaskResult>;
    static from(options?: {
        name?: string;
    }): {
        name: string;
        handler: () => CssModulesTransformTask;
    };
}
