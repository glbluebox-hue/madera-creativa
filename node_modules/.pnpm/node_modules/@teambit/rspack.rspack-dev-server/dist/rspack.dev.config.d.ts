import { Configuration } from '@rspack/core';
import { PubsubMain } from '@teambit/pubsub';
import '@teambit/react.babel.bit-react-transformer';
export declare const configFactory: (devServerID: string, workspaceDir: string, entryFiles: string[], publicRoot: string, publicPath: string, pubsub: PubsubMain, title?: string, favicon?: string) => Configuration;
