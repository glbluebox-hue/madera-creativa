export type BundleResults = {
    bundleFiles: {
        system: 'windows' | 'macos' | 'linux';
        bundlePath: string;
    }[];
};
export type Options = {
    outPutRootDir?: string;
    outPutFileName?: string;
};
/** TODO: need to handle windows and linux */
export declare class SingleExecutableApplications {
    private options;
    constructor(options: Options);
    private bundleName;
    private outPutBlob;
    private configFile;
    bundle(file: string): Promise<BundleResults>;
    private getOutPutPath;
    private buildMacOS;
    getConfig(mainFile: string, dir: string): {
        main: string;
        output: string;
        disableExperimentalSEAWarning: boolean;
        useSnapshot: boolean;
        useCodeCache: boolean;
    };
    private createTmpDir;
}
