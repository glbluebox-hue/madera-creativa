import { AppContext } from '@teambit/application';
import type { Application, ApplicationInstance } from '@teambit/application';
export declare class GatewayContext extends AppContext {
    readonly appContext: AppContext;
    readonly services: ApplicationInstance[];
    readonly port: number;
    readonly envVars: Record<string, string>;
    constructor(appContext: AppContext, services: ApplicationInstance[], port: number, envVars: Record<string, string>);
    static from(appContext: AppContext, services?: ApplicationInstance[], port?: number, envVars?: Record<string, string>): GatewayContext;
}
export interface GatewayServer extends Application {
    /**
     * run the application.
     */
    run(context: GatewayContext): Promise<ApplicationInstance>;
}
