// import * as appExports from "@teambit/application";
import { AppContext } from '@teambit/application';
// const app = appExports.default;
export class GatewayContext extends AppContext {
    constructor(appContext, services, port, envVars) {
        super(appContext.appName, appContext.harmony, appContext.dev, appContext.appComponent, appContext.workdir, appContext.execContext, appContext.hostRootDir, port || appContext.port, appContext.args, appContext.workspaceComponentPath, envVars);
        this.appContext = appContext;
        this.services = services;
        this.port = port;
        this.envVars = envVars;
    }
    static from(appContext, services, port, envVars = {}) {
        return new GatewayContext(appContext, services, port, envVars);
    }
}
;
//# sourceMappingURL=gateway-context.js.map