/**
 * Utilidades para registrar y autenticar usuarios contra el servidor de Madera Creativa.
 * El servidor es la fuente de verdad para el control de acceso.
 */
const BASE = '/api/presupuestos-service';
/** Hash simple sincronizado con el backend. */
function hashSimple(texto) {
    let hash = 0;
    for (let i = 0; i < texto.length; i++) {
        const c = texto.charCodeAt(i);
        hash = ((hash << 5) - hash) + c;
        hash |= 0;
    }
    return 'h' + Math.abs(hash).toString(36);
}
/**
 * Registra un nuevo usuario en el servidor.
 * La cuenta queda en estado "pendiente" hasta que el admin la apruebe.
 */
export async function registrarEnServidor(nombre, password) {
    try {
        const res = await fetch(`${BASE}/auth/registrar`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            credentials: 'include',
            body: JSON.stringify({ nombre, passwordHash: hashSimple(password) }),
        });
        const data = await res.json();
        if (res.status === 409)
            return { ok: false, error: 'Ese nombre de usuario ya existe.', codigo: 'credenciales' };
        if (!res.ok)
            return { ok: false, error: data.error || 'Error al registrarse.', codigo: 'error-red' };
        return { ok: true, id: data.id, estado: 'pendiente' };
    }
    catch {
        return { ok: false, error: 'Sin conexión con el servidor.', codigo: 'error-red' };
    }
}
/**
 * Inicia sesión verificando credenciales contra el servidor.
 * Devuelve error específico si la cuenta está pendiente o suspendida.
 */
export async function loginEnServidor(nombre, password) {
    try {
        const res = await fetch(`${BASE}/auth/login`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            credentials: 'include',
            body: JSON.stringify({ nombre, passwordHash: hashSimple(password) }),
        });
        const data = await res.json();
        if (res.status === 403 && data.error === 'pendiente') {
            return { ok: false, error: data.mensaje, codigo: 'pendiente' };
        }
        if (res.status === 403 && data.error === 'suspendido') {
            return { ok: false, error: data.mensaje, codigo: 'suspendido' };
        }
        if (!res.ok)
            return { ok: false, error: data.error || 'Usuario o contraseña incorrectos.', codigo: 'credenciales' };
        // Guardar el token único del usuario para que las llamadas a la API usen su identidad
        if (data.token) {
            try {
                localStorage.setItem('mc-auth-token', data.token);
            }
            catch { /* noop */ }
        }
        return { ok: true, id: data.id, nombre: data.nombre, esAdmin: data.esAdmin, estado: data.estado };
    }
    catch {
        return { ok: false, error: 'Sin conexión con el servidor. Inténtalo de nuevo.', codigo: 'error-red' };
    }
}
//# sourceMappingURL=use-registro.js.map