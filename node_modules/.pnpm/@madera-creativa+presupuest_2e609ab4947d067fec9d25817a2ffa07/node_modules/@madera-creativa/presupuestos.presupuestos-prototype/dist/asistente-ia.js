import { jsx as _jsx, jsxs as _jsxs, Fragment as _Fragment } from "react/jsx-runtime";
import { useState, useRef, useEffect } from 'react';
import styles from './asistente-ia.module.css';
const CLAVE_TOKEN = 'mc-auth-token';
const BASE = '/api/presupuestos-service';
/** Genera un ID único simple. */
const uid = () => Math.random().toString(36).slice(2);
/** Extrae la acción JSON del texto de respuesta del asistente. */
function parsearAccion(texto) {
    const match = texto.match(/<accion>([\s\S]*?)<\/accion>/);
    if (!match)
        return { texto, accion: null };
    try {
        const accion = JSON.parse(match[1].trim());
        const textoLimpio = texto.replace(/<accion>[\s\S]*?<\/accion>/, '').trim();
        return { texto: textoLimpio, accion };
    }
    catch {
        return { texto, accion: null };
    }
}
/**
 * Asistente IA flotante de Madera Creativa.
 * Procesa lenguaje natural y ejecuta acciones dentro de la app.
 */
export function AsistenteIA({ contexto, clientes, onNavegar, onAbrirCliente, onCrearCliente, sinFab, abiertoProp, onCerrar, }) {
    const [abiertoInterno, setAbiertoInterno] = useState(false);
    const abierto = abiertoProp !== undefined ? abiertoProp : abiertoInterno;
    const setAbierto = (v) => {
        const next = typeof v === 'function' ? v(abierto) : v;
        setAbiertoInterno(next);
        if (!next)
            onCerrar?.();
    };
    const [mensajes, setMensajes] = useState([
        {
            id: uid(),
            rol: 'asistente',
            texto: '¡Hola! Soy tu asistente de Madera Creativa 🪚 Puedo abrir clientes, buscar proyectos, decirte el beneficio del mes o cualquier cosa que necesites. ¿En qué te ayudo?',
        },
    ]);
    const [input, setInput] = useState('');
    const [cargando, setCargando] = useState(false);
    const [escuchando, setEscuchando] = useState(false);
    const bottomRef = useRef(null);
    const inputRef = useRef(null);
    const recognitionRef = useRef(null);
    // Auto-scroll al último mensaje
    useEffect(() => {
        bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
    }, [mensajes]);
    // Focus al abrir
    useEffect(() => {
        if (abierto)
            setTimeout(() => inputRef.current?.focus(), 100);
    }, [abierto]);
    /** Ejecuta la acción devuelta por el asistente. */
    const ejecutarAccion = (accion) => {
        switch (accion.action) {
            case 'navegarSeccion':
                if (accion.seccion)
                    onNavegar(accion.seccion);
                break;
            case 'abrirCliente':
                if (accion.clienteId) {
                    onAbrirCliente(accion.clienteId);
                }
                else if (accion.clienteNombre) {
                    const encontrado = clientes.find(c => c.nombre.toLowerCase().includes((accion.clienteNombre || '').toLowerCase()));
                    if (encontrado)
                        onAbrirCliente(encontrado.id);
                }
                break;
            case 'crearCliente':
                onCrearCliente();
                break;
            case 'abrirFacturas':
                onNavegar('facturas');
                break;
            case 'buscarCliente':
                onNavegar('clientes');
                break;
        }
    };
    /** Envía un mensaje al asistente. */
    const enviar = async (texto) => {
        const pregunta = (texto ?? input).trim();
        if (!pregunta || cargando)
            return;
        setInput('');
        const msgUsuario = { id: uid(), rol: 'usuario', texto: pregunta };
        const msgCargando = { id: uid(), rol: 'asistente', texto: '', cargando: true };
        setMensajes(prev => [...prev, msgUsuario, msgCargando]);
        setCargando(true);
        try {
            const historial = [...mensajes, msgUsuario]
                .filter(m => !m.cargando)
                .slice(-10)
                .map(m => ({ role: m.rol === 'usuario' ? 'user' : 'assistant', content: m.texto }));
            const token = localStorage.getItem(CLAVE_TOKEN) ?? '';
            const res = await fetch(`${BASE}/asistente`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` },
                body: JSON.stringify({ mensajes: historial, contexto }),
                credentials: 'include',
            });
            const data = await res.json();
            const raw = data.respuesta || 'No pude procesar tu solicitud.';
            const { texto: textoLimpio, accion } = parsearAccion(raw);
            setMensajes(prev => prev.map(m => m.cargando ? { ...m, texto: textoLimpio || raw, cargando: false } : m));
            if (accion) {
                setTimeout(() => ejecutarAccion(accion), 400);
            }
        }
        catch {
            setMensajes(prev => prev.map(m => m.cargando ? { ...m, texto: 'Error de conexión. Comprueba tu conexión a internet.', cargando: false } : m));
        }
        finally {
            setCargando(false);
        }
    };
    /** Activa reconocimiento de voz. */
    const toggleVoz = () => {
        const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
        if (!SpeechRecognition) {
            alert('Tu navegador no soporta reconocimiento de voz.');
            return;
        }
        if (escuchando) {
            recognitionRef.current?.stop();
            setEscuchando(false);
            return;
        }
        const rec = new SpeechRecognition();
        rec.lang = 'es-ES';
        rec.continuous = false;
        rec.interimResults = false;
        rec.onresult = (e) => {
            const transcript = e.results[0][0].transcript;
            setEscuchando(false);
            enviar(transcript);
        };
        rec.onerror = () => setEscuchando(false);
        rec.onend = () => setEscuchando(false);
        rec.start();
        recognitionRef.current = rec;
        setEscuchando(true);
    };
    // Sugerencias rápidas
    const sugerencias = [
        '¿Cuánto beneficio llevo este mes?',
        'Muéstrame los clientes',
        'Crea un nuevo cliente',
        '¿Qué proyectos están en curso?',
    ];
    return (_jsxs(_Fragment, { children: [!sinFab && _jsx("button", { className: `${styles.fab} ${abierto ? styles.fabAbierto : ''}`, onClick: () => setAbierto(v => !v), title: "Asistente IA", "aria-label": "Abrir asistente inteligente", children: abierto ? (_jsxs("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", children: [_jsx("line", { x1: "18", y1: "6", x2: "6", y2: "18" }), _jsx("line", { x1: "6", y1: "6", x2: "18", y2: "18" })] })) : (_jsxs("svg", { width: "22", height: "22", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", children: [_jsx("path", { d: "M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" }), _jsx("circle", { cx: "9", cy: "10", r: "1", fill: "currentColor" }), _jsx("circle", { cx: "12", cy: "10", r: "1", fill: "currentColor" }), _jsx("circle", { cx: "15", cy: "10", r: "1", fill: "currentColor" })] })) }), abierto && (_jsxs("div", { className: styles.panel, children: [_jsxs("div", { className: styles.cabecera, children: [_jsxs("div", { className: styles.cabeceraInfo, children: [_jsx("div", { className: styles.avatar, children: "\uD83E\uDE9A" }), _jsxs("div", { children: [_jsx("p", { className: styles.cabeceraTitle, children: "Asistente Madera" }), _jsxs("p", { className: styles.cabeceraStatus, children: [_jsx("span", { className: styles.dot }), " En l\u00EDnea"] })] })] }), _jsx("button", { className: styles.btnCerrar, onClick: () => setAbierto(false), children: _jsxs("svg", { width: "16", height: "16", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", children: [_jsx("line", { x1: "18", y1: "6", x2: "6", y2: "18" }), _jsx("line", { x1: "6", y1: "6", x2: "18", y2: "18" })] }) })] }), _jsxs("div", { className: styles.mensajes, children: [mensajes.map(m => (_jsx("div", { className: `${styles.msg} ${m.rol === 'usuario' ? styles.msgUsuario : styles.msgAsistente}`, children: m.cargando ? (_jsxs("span", { className: styles.typing, children: [_jsx("span", {}), _jsx("span", {}), _jsx("span", {})] })) : (_jsx("p", { className: styles.msgTexto, children: m.texto })) }, m.id))), _jsx("div", { ref: bottomRef })] }), mensajes.length <= 1 && (_jsx("div", { className: styles.sugerencias, children: sugerencias.map(s => (_jsx("button", { className: styles.sugerencia, onClick: () => enviar(s), children: s }, s))) })), _jsxs("div", { className: styles.inputArea, children: [_jsx("button", { className: `${styles.btnVoz} ${escuchando ? styles.btnVozActivo : ''}`, onClick: toggleVoz, title: escuchando ? 'Detener voz' : 'Hablar', children: _jsxs("svg", { width: "16", height: "16", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", children: [_jsx("path", { d: "M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z" }), _jsx("path", { d: "M19 10v2a7 7 0 0 1-14 0v-2" }), _jsx("line", { x1: "12", y1: "19", x2: "12", y2: "23" }), _jsx("line", { x1: "8", y1: "23", x2: "16", y2: "23" })] }) }), _jsx("input", { ref: inputRef, className: styles.input, value: input, onChange: e => setInput(e.target.value), onKeyDown: e => e.key === 'Enter' && !e.shiftKey && enviar(), placeholder: escuchando ? '🎙️ Escuchando...' : 'Escribe o habla...', disabled: cargando }), _jsx("button", { className: styles.btnEnviar, onClick: () => enviar(), disabled: !input.trim() || cargando, children: _jsxs("svg", { width: "16", height: "16", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round", children: [_jsx("line", { x1: "22", y1: "2", x2: "11", y2: "13" }), _jsx("polygon", { points: "22 2 15 22 11 13 2 9 22 2" })] }) })] })] }))] }));
}
//# sourceMappingURL=asistente-ia.js.map