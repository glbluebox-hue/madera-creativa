import type { AppBuildContext } from '@teambit/application';
import type { BuilderData } from '@teambit/builder';
export interface PlatformBuildContext extends AppBuildContext {
    /**
     * builder data for all backend services.
     */
    backends: BuilderData[];
    /**
     * frontends data for all
     */
    frontends: BuilderData[];
}
