import { Linter, LinterContext, LintResults } from '@teambit/linter';
import { Logger } from '@teambit/logger';
import { EnvHandler } from '@teambit/envs';
import { BuildContext } from '@teambit/builder';
import { OxlintOptions } from './oxlint-linter-options';
export declare class OxlintLinter implements Linter {
    readonly id: string;
    private logger;
    private options;
    private oxlintNode;
    constructor(id: string, logger: Logger, options: OxlintOptions);
    displayName: string;
    displayConfig(): string;
    version(): string;
    lint(context: LinterContext, buildContext?: BuildContext): Promise<LintResults>;
    private getFilesPaths;
    private computeComponentResultsWithTotals;
    private computeManyComponentsTotals;
    private static resolveBinPath;
    static create(options: OxlintOptions, { logger }: {
        logger: Logger;
    }): Linter;
    static from(options: OxlintOptions): EnvHandler<Linter>;
}
