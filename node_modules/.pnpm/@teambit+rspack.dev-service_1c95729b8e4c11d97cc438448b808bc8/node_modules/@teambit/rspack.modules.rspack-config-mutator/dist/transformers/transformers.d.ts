import { Logger } from '@teambit/logger';
import { RspackConfigMutator, RspackConfigTransformContext } from '../rspack-config-mutator';
/**
 * Generate a transformer that expose all the peers as global via the expose loader
 * @param peers
 * @returns
 */
export declare function generateExternalsTransformer(depes: string[]): (config: RspackConfigMutator) => RspackConfigMutator;
export declare function generateAddAliasesFromPeersTransformer(peers: string[], logger: Logger): (config: RspackConfigMutator, context: RspackConfigTransformContext) => RspackConfigMutator;
