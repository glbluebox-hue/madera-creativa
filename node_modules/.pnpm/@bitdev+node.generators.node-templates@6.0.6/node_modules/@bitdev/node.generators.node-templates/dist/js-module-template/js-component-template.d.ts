import { ComponentContext, ComponentFile, ComponentTemplate } from '@teambit/generator';
export type NodeJSComponentTemplateOptions = Partial<Pick<ComponentTemplate, 'name' | 'description' | 'hidden' | 'env'>>;
export declare class NodeJSComponentTemplate implements ComponentTemplate {
    readonly name: string;
    readonly description: string;
    readonly hidden: boolean;
    readonly env?: string;
    constructor(name?: string, description?: string, hidden?: boolean, env?: string);
    generateFiles(context: ComponentContext): ComponentFile[];
    static from(options?: NodeJSComponentTemplateOptions): () => NodeJSComponentTemplate;
}
