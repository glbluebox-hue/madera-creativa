import { ComponentContext, ComponentFile, ComponentTemplate } from '@teambit/generator';
export type ExpressAppTemplateOptions = Partial<Pick<ComponentTemplate, 'name' | 'description' | 'hidden' | 'env'>>;
export declare class PlatformTemplate implements ComponentTemplate {
    readonly name: string;
    readonly description: string;
    readonly hidden: boolean;
    readonly env?: string;
    readonly isApp: boolean;
    readonly installMissingDependencies: boolean;
    constructor(name?: string, description?: string, hidden?: boolean, env?: string, isApp?: boolean, installMissingDependencies?: boolean);
    generateFiles(context: ComponentContext): ComponentFile[];
    static from(options?: ExpressAppTemplateOptions): () => PlatformTemplate;
}
