import { jsx as _jsx } from "react/jsx-runtime";
import { render } from '@testing-library/react';
import { MockTarget } from '../mock-component';
import { bubbleToComponent } from './bubble-to-component';
it('should find component when starting from a div', () => {
    const { getByText, getByTestId } = render(_jsx(MockTarget, { "data-testid": "expected-result", children: _jsx("div", { children: "hello world" }) }));
    const rendered = getByText('hello world');
    const result = bubbleToComponent(rendered);
    expect(result?.element).toBe(getByTestId('expected-result'));
    expect(result?.components).toEqual([MockTarget]);
});
it('should bubble to root component when it renders itself recursively', () => {
    const { getByText, getByTestId } = render(_jsx(MockTarget, { "data-testid": "expected-result", children: _jsx(MockTarget, { children: _jsx(MockTarget, { children: _jsx("div", { children: "hello world" }) }) }) }));
    const rendered = getByText('hello world');
    const result = bubbleToComponent(rendered);
    expect(result?.element).toBe(getByTestId('expected-result'));
    expect(result?.components).toEqual([MockTarget]);
});
it('should find first component, when parent propagation is disabled', () => {
    const { getByText, getByTestId } = render(_jsx(MockTarget, { children: _jsx(MockTarget, { children: _jsx(MockTarget, { "data-testid": "expected-result", children: _jsx("div", { children: "hello world" }) }) }) }));
    const rendered = getByText('hello world');
    const result = bubbleToComponent(rendered, { propagateSameParents: false });
    expect(result?.element).toBe(getByTestId('expected-result'));
    expect(result?.components).toEqual([MockTarget]);
});
//# sourceMappingURL=bubble-to-component.spec.js.map