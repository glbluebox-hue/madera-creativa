import { jsx as _jsx } from "react/jsx-runtime";
import { BrowserRouter } from 'react-router-dom';
import { createRoot } from 'react-dom/client';
import { PresupuestosPrototype } from './presupuestos-prototype.js';
import { ErrorBoundary } from './error-boundary.js';
// Importar iconos PWA para que Vite los incluya en el build
import _icon192 from './assets/icon-192.png';
import _icon512 from './assets/icon-512.png';
import _icon180 from './assets/icon-180.png';
import _appIcon from './assets/app-icon.png';
// Inyectar manifest dinámico con las rutas reales de los iconos
const manifestData = {
    name: 'Madera Creativa',
    short_name: 'Madera',
    description: 'Gestión de clientes, proyectos y presupuestos de carpintería',
    start_url: '/',
    display: 'standalone',
    orientation: 'portrait-primary',
    background_color: '#3d2810',
    theme_color: '#3d2810',
    lang: 'es',
    icons: [
        { src: _appIcon, sizes: '192x192', type: 'image/png', purpose: 'any' },
        { src: _icon192, sizes: '192x192', type: 'image/png', purpose: 'any' },
        { src: _icon512, sizes: '512x512', type: 'image/png', purpose: 'any' },
        { src: _icon512, sizes: '512x512', type: 'image/png', purpose: 'maskable' },
        { src: _icon180, sizes: '180x180', type: 'image/png', purpose: 'any' },
    ],
};
const blob = new Blob([JSON.stringify(manifestData)], { type: 'application/manifest+json' });
const manifestUrl = URL.createObjectURL(blob);
const existingManifest = document.querySelector('link[rel="manifest"]');
if (existingManifest)
    existingManifest.setAttribute('href', manifestUrl);
// Apple touch icon dinámico
const appleIcon = document.querySelector('link[rel="apple-touch-icon"]');
if (appleIcon)
    appleIcon.href = _icon180;
if (import.meta.hot) {
    import.meta.hot.accept();
}
/**
 * Punto de entrada client-side — sin SSR.
 */
const container = document.getElementById('root');
if (container) {
    const root = createRoot(container);
    root.render(_jsx(BrowserRouter, { children: _jsx(ErrorBoundary, { children: _jsx(PresupuestosPrototype, {}) }) }));
}
//# sourceMappingURL=presupuestos-prototype.app-root.js.map