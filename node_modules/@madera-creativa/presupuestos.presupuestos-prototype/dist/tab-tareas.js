import { jsxs as _jsxs, jsx as _jsx, Fragment as _Fragment } from "react/jsx-runtime";
import { useState } from 'react';
import { generarId } from './mock.js';
import styles from './styles.module.css';
/** Tareas por defecto del flujo de carpintería. */
const TAREAS_BASE = [
    'Medir', 'Diseñar', 'Presupuesto', 'Cobro inicial', 'Comprar material',
    'Fabricar', 'Lijar', 'Pintar', 'Montar', 'Cobro final',
];
/**
 * Pestaña "Tareas": checklist del proyecto con las fases de carpintería.
 * Permite marcar, añadir y eliminar tareas.
 */
export function TabTareas({ cliente, onActualizar }) {
    const tareas = cliente.tareas ?? [];
    const [nueva, setNueva] = useState('');
    const guardar = (nuevas) => onActualizar({ ...cliente, tareas: nuevas });
    const crearBase = () => guardar(TAREAS_BASE.map((t) => ({ id: generarId(), texto: t, hecha: false })));
    const alternar = (id) => guardar(tareas.map((t) => (t.id === id ? { ...t, hecha: !t.hecha } : t)));
    const anadir = () => {
        if (!nueva.trim())
            return;
        guardar([...tareas, { id: generarId(), texto: nueva.trim(), hecha: false }]);
        setNueva('');
    };
    const borrar = (id) => guardar(tareas.filter((t) => t.id !== id));
    const hechas = tareas.filter((t) => t.hecha).length;
    return (_jsxs("div", { className: styles.tabPanel, children: [_jsxs("div", { className: styles.barraSeccion, children: [_jsxs("h3", { style: { margin: 0 }, children: ["Tareas del proyecto ", tareas.length > 0 && _jsxs("span", { style: { color: 'var(--topo-claro)', fontWeight: 400 }, children: ["(", hechas, "/", tareas.length, ")"] })] }), tareas.length === 0 && (_jsx("button", { className: `${styles.btn} ${styles.btnPrimario}`, onClick: crearBase, children: "Crear checklist de carpinter\u00EDa" }))] }), tareas.length === 0 ? (_jsxs("div", { className: styles.tabVacio, children: [_jsx("div", { className: styles.tabVacioIcono, children: "\u2611\uFE0F" }), _jsx("p", { children: "Crea la checklist con las fases t\u00EDpicas: medir, dise\u00F1ar, fabricar, montar, cobrar\u2026" })] })) : (_jsxs(_Fragment, { children: [_jsx("div", { style: { display: 'flex', flexDirection: 'column', gap: '0.5rem' }, children: tareas.map((t) => (_jsxs("div", { className: `${styles.checklistItem} ${t.hecha ? styles.checklistHecha : ''}`, onClick: () => alternar(t.id), children: [_jsx("span", { className: styles.checklistCheck, children: t.hecha ? '✓' : '' }), _jsx("span", { className: styles.checklistTexto, style: { flex: 1 }, children: t.texto }), _jsx("button", { className: styles.btnIconoBorrar ?? '', style: { background: 'none', border: 'none', cursor: 'pointer', color: 'var(--topo-claro)' }, onClick: (e) => { e.stopPropagation(); borrar(t.id); }, children: "\u2715" })] }, t.id))) }), _jsxs("div", { style: { display: 'flex', gap: '0.5rem' }, children: [_jsx("input", { className: styles.input, placeholder: "Nueva tarea\u2026", value: nueva, onChange: (e) => setNueva(e.target.value), onKeyDown: (e) => e.key === 'Enter' && anadir() }), _jsx("button", { className: `${styles.btn} ${styles.btnPrimario}`, onClick: anadir, children: "A\u00F1adir" })] })] }))] }));
}
//# sourceMappingURL=tab-tareas.js.map