import type { BundlerMain, ComponentServer } from '@teambit/bundler';
import type { PubsubMain } from '@teambit/pubsub';
import type { ProxyEntry, StartPlugin, StartPluginOptions, UiMain } from '@teambit/ui';
import type { Workspace } from '@teambit/workspace';
import type { Logger } from '@teambit/logger';
import type { WatcherMain } from '@teambit/watcher';
import type { GraphqlMain } from '@teambit/graphql';
type ServerState = {
    isCompiling?: boolean;
    isReady?: boolean;
    errors?: Error[];
    warnings?: Error[];
    results?: any[];
    isStarted?: boolean;
    isCompilationDone?: boolean;
    isPendingPublish?: boolean;
};
type ServerStateMap = Record<string, ServerState>;
export declare class PreviewStartPlugin implements StartPlugin {
    private workspace;
    private bundler;
    private ui;
    private pubsub;
    private logger;
    private watcher;
    private graphql;
    previewServers: ComponentServer[];
    serversState: ServerStateMap;
    serversMap: Record<string, ComponentServer>;
    private pendingServers;
    private pendingPostInstallPublish;
    constructor(workspace: Workspace, bundler: BundlerMain, ui: UiMain, pubsub: PubsubMain, logger: Logger, watcher: WatcherMain, graphql: GraphqlMain);
    onComponentServerStarted(componentServer: ComponentServer): Promise<void>;
    private publishServerStarted;
    onNewDevServersCreated(servers: ComponentServer[]): Promise<void>;
    initiate(options: StartPluginOptions): Promise<void>;
    getProxy(): ProxyEntry[];
    private listenToDevServers;
    private handleOnStartCompiling;
    private handleOnDoneCompiling;
    markAllForPostInstallPublish(): void;
    restartExistingServers(): Promise<void>;
    invalidateAllResolverCaches(): void;
    private setReady;
    private readyPromise;
    get whenReady(): Promise<void>;
}
export {};
