"use strict";

function _os() {
  const data = _interopRequireDefault(require("os"));
  _os = function () {
    return data;
  };
  return data;
}
function _path() {
  const data = require("path");
  _path = function () {
    return data;
  };
  return data;
}
function _fsExtra() {
  const data = _interopRequireDefault(require("fs-extra"));
  _fsExtra = function () {
    return data;
  };
  return data;
}
function _chai() {
  const data = require("chai");
  _chai = function () {
    return data;
  };
  return data;
}
function _resolvePreviewFilePath() {
  const data = require("./resolve-preview-file-path");
  _resolvePreviewFilePath = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
describe('resolvePreviewFilePath', () => {
  // serve-preview builds the path from an untrusted URL segment; it must never escape publicDir.
  const publicDir = (0, _path().join)(_path().sep, 'tmp', 'env-preview', 'public');
  it('returns the joined path for a normal file', () => {
    (0, _chai().expect)((0, _resolvePreviewFilePath().resolvePreviewFilePath)(publicDir, 'index.html')).to.equal((0, _path().join)(publicDir, 'index.html'));
  });
  it('returns the joined path for a nested file', () => {
    (0, _chai().expect)((0, _resolvePreviewFilePath().resolvePreviewFilePath)(publicDir, 'assets/main.js')).to.equal((0, _path().join)(publicDir, 'assets', 'main.js'));
  });
  it('returns undefined for a parent-traversal path', () => {
    (0, _chai().expect)((0, _resolvePreviewFilePath().resolvePreviewFilePath)(publicDir, '../../../etc/passwd')).to.equal(undefined);
  });
  it('returns undefined for an embedded parent-traversal path', () => {
    (0, _chai().expect)((0, _resolvePreviewFilePath().resolvePreviewFilePath)(publicDir, 'assets/../../../../etc/passwd')).to.equal(undefined);
  });
  it('returns undefined for a percent-decoded-style traversal already containing ".."', () => {
    (0, _chai().expect)((0, _resolvePreviewFilePath().resolvePreviewFilePath)(publicDir, '..' + _path().sep + '..' + _path().sep + 'secret')).to.equal(undefined);
  });
  it('does not treat a sibling dir with the same prefix as inside publicDir', () => {
    // ".../public-evil" must not be considered within ".../public"
    (0, _chai().expect)((0, _resolvePreviewFilePath().resolvePreviewFilePath)(publicDir, (0, _path().join)('..', 'public-evil', 'x'))).to.equal(undefined);
  });
  it('allows a file that resolves back to publicDir itself', () => {
    (0, _chai().expect)((0, _resolvePreviewFilePath().resolvePreviewFilePath)(publicDir, '.')).to.equal(publicDir);
  });

  // a symlink inside publicDir must not be allowed to redirect the served file outside it. these
  // use a real temp tree because symlink resolution needs files on disk.
  describe('symlink handling', () => {
    let root;
    let realPublicDir;
    beforeEach(async () => {
      root = await _fsExtra().default.mkdtemp((0, _path().join)(_os().default.tmpdir(), 'preview-symlink-'));
      realPublicDir = (0, _path().join)(root, 'public');
      await _fsExtra().default.ensureDir((0, _path().join)(realPublicDir, 'assets'));
      await _fsExtra().default.writeFile((0, _path().join)(realPublicDir, 'assets', 'main.js'), 'ok');
      await _fsExtra().default.writeFile((0, _path().join)(root, 'secret.txt'), 'SECRET'); // sibling of public, outside it
    });
    afterEach(async () => {
      await _fsExtra().default.remove(root);
    });
    it('allows a real nested file', () => {
      (0, _chai().expect)((0, _resolvePreviewFilePath().resolvePreviewFilePath)(realPublicDir, 'assets/main.js')).to.equal((0, _path().join)(realPublicDir, 'assets', 'main.js'));
    });
    it('rejects a symlink inside publicDir that points outside', async () => {
      await _fsExtra().default.symlink((0, _path().join)(root, 'secret.txt'), (0, _path().join)(realPublicDir, 'evil')); // public/evil -> ../secret.txt
      (0, _chai().expect)((0, _resolvePreviewFilePath().resolvePreviewFilePath)(realPublicDir, 'evil')).to.equal(undefined);
    });
    it('allows a symlink inside publicDir that points to another file inside it', async () => {
      await _fsExtra().default.symlink((0, _path().join)(realPublicDir, 'assets', 'main.js'), (0, _path().join)(realPublicDir, 'link.js'));
      (0, _chai().expect)((0, _resolvePreviewFilePath().resolvePreviewFilePath)(realPublicDir, 'link.js')).to.equal((0, _path().join)(realPublicDir, 'link.js'));
    });
  });
});

//# sourceMappingURL=resolve-preview-file-path.spec.js.map