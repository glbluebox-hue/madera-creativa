"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.InitCmd = void 0;
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function pathlib() {
  const data = _interopRequireWildcard(require("path"));
  pathlib = function () {
    return data;
  };
  return data;
}
function _bitError() {
  const data = require("@teambit/bit-error");
  _bitError = function () {
    return data;
  };
  return data;
}
function _configStore() {
  const data = require("@teambit/config-store");
  _configStore = function () {
    return data;
  };
  return data;
}
function _legacyBitId() {
  const data = require("@teambit/legacy-bit-id");
  _legacyBitId = function () {
    return data;
  };
  return data;
}
function _legacy() {
  const data = require("@teambit/legacy.scope-api");
  _legacy = function () {
    return data;
  };
  return data;
}
function _legacy2() {
  const data = require("@teambit/legacy.constants");
  _legacy2 = function () {
    return data;
  };
  return data;
}
function _cli() {
  const data = require("@teambit/cli");
  _cli = function () {
    return data;
  };
  return data;
}
function _mcp() {
  const data = require("@teambit/mcp.mcp-config-writer");
  _mcp = function () {
    return data;
  };
  return data;
}
function _hostInitializerMain() {
  const data = require("./host-initializer.main.runtime");
  _hostInitializerMain = function () {
    return data;
  };
  return data;
}
function _interopRequireWildcard(e, t) { if ("function" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function (e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, default: e }; if (null === e || "object" != typeof e && "function" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]); return f; })(e, t); }
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class InitCmd {
  constructor(hostInitializer, logger) {
    this.hostInitializer = hostInitializer;
    this.logger = logger;
    _defineProperty(this, "name", 'init [path]');
    _defineProperty(this, "skipWorkspace", true);
    _defineProperty(this, "description", 'initialize a Bit workspace in an existing project');
    _defineProperty(this, "helpUrl", 'reference/workspace/creating-workspaces/?new_existing_project=1');
    _defineProperty(this, "group", 'workspace-setup');
    _defineProperty(this, "extendedDescription", `creates Bit configuration files in an existing project directory to start tracking components.
if already a workspace, validates and repairs Bit files as needed.
supports various reset options to recover from corrupted state or restart from scratch.`);
    _defineProperty(this, "alias", '');
    _defineProperty(this, "loadAspects", false);
    _defineProperty(this, "options", [['n', 'name <workspace-name>', 'name of the workspace'], ['', 'generator <env-id>', 'for multiple, separate by a comma. add env-ids into the generators field in the workspace config for future "bit create" templates'], ['T', 'standalone', 'do not nest component store within .git directory and do not write config data inside package.json'], ['', 'no-package-json', 'do not generate package.json'], ['r', 'reset', 'write missing or damaged Bit files'], ['', 'reset-new', 'reset .bitmap file as if the components were newly added and remove all model data (objects)'], ['', 'reset-lane-new', 'same as reset-new, but it only resets components belonging to lanes. main components are left intact'], ['', 'reset-hard', 'delete all Bit files and directories, including Bit configuration, tracking and model data. Useful for re-starting workspace from scratch'], ['', 'reset-scope', 'removes local scope (.bit or .git/bit). tags/snaps that have not been exported will be lost. workspace is left intact'], ['d', 'default-directory <default-directory>', 'set the default directory pattern to import/create components into'], ['', 'default-scope <default-scope>', 'set the default scope for components in the workspace'], ['f', 'force', 'force workspace initialization without clearing local objects'], ['b', 'bare [name]', 'initialize an empty bit bare scope'], ['s', 'shared <groupname>', 'add group write permissions to a scope properly'], ['', 'external-package-manager', 'enable external package manager mode (npm/yarn/pnpm)'], ['', 'skip-interactive', 'skip interactive mode for Git repositories'], ['', 'agent [type]', 'create an AI agent instructions file. options: claude, cursor, copilot (default: AGENTS.md)']]);
  }
  async handleInteractiveMode(projectPath, flags) {
    const {
      reset,
      resetNew,
      resetLaneNew,
      resetHard,
      resetScope,
      standalone,
      skipInteractive,
      externalPackageManager
    } = flags;

    // Check if we should run interactive mode
    if (reset || resetNew || resetLaneNew || resetHard || resetScope || standalone || skipInteractive || externalPackageManager || !(await _hostInitializerMain().HostInitializerMain.hasGitDirectory(projectPath)) || (await _hostInitializerMain().HostInitializerMain.hasWorkspaceInitialized(projectPath))) {
      return null;
    }
    this.logger.off();
    this.logger.console(_chalk().default.cyan('🔧 Interactive setup for existing Git repository\n'));
    try {
      const interactiveConfig = await _hostInitializerMain().HostInitializerMain.runInteractiveMode(projectPath);
      if (interactiveConfig.mcpEditor) {
        const displayName = _mcp().McpConfigWriter.getEditorDisplayName(interactiveConfig.mcpEditor);
        this.logger.console(_chalk().default.cyan(`\nConnecting Bit Cloud MCP to ${displayName}...`));
        await _hostInitializerMain().HostInitializerMain.setupMcpServer(interactiveConfig.mcpEditor, projectPath);
        this.logger.console((0, _cli().formatSuccessSummary)(`Bit Cloud MCP connected to ${displayName}`));
        interactiveConfig.agentFileWritten = await _hostInitializerMain().HostInitializerMain.writeMcpAgentRules(interactiveConfig.mcpEditor, projectPath);
      }
      return interactiveConfig;
    } catch (error) {
      this.logger.consoleWarning(`Warning: Interactive setup failed: ${error.message}`);
      return null;
    }
  }
  async report([path], flags) {
    const {
      name,
      generator,
      bare,
      shared,
      standalone,
      noPackageJson,
      reset,
      resetNew,
      resetLaneNew,
      resetHard,
      resetScope,
      force,
      defaultDirectory,
      defaultScope,
      externalPackageManager,
      agent
    } = flags;
    if (path) path = pathlib().resolve(path);
    if (bare) {
      if (reset || resetHard) throw new (_bitError().BitError)('--reset and --reset-hard flags are not available for bare scope');
      // Handle both cases init --bare and init --bare [scopeName]
      const bareVal = bare === true ? '' : bare;
      await (0, _legacy().initScope)(path, bareVal, shared);
      return (0, _cli().formatSuccessSummary)('initialized an empty bare bit scope.');
    }
    if (reset && resetHard) {
      throw new (_bitError().BitError)('cannot use both --reset and --reset-hard, please use only one of them');
    }
    const defaultScopeToUse = defaultScope || (0, _configStore().getConfig)(_legacy2().CFG_INIT_DEFAULT_SCOPE);
    // validate before creating any file, otherwise, the workspace is created with an invalid
    // default-scope and every subsequent command fails to load it.
    if (defaultScopeToUse && !(0, _legacyBitId().isValidScopeName)(defaultScopeToUse)) {
      throw new (_legacyBitId().InvalidScopeName)(defaultScopeToUse);
    }
    const projectPath = path || process.cwd();
    const interactiveConfig = await this.handleInteractiveMode(projectPath, flags);
    const workspaceExtensionProps = {
      defaultDirectory: interactiveConfig?.defaultDirectory || (externalPackageManager ? 'bit-components/{scope}/{name}' : defaultDirectory) || (0, _configStore().getConfig)(_legacy2().CFG_INIT_DEFAULT_DIRECTORY),
      defaultScope: defaultScopeToUse,
      name,
      externalPackageManager: interactiveConfig?.externalPackageManager || externalPackageManager
    };

    // Resolve agent flag: true means no specific type (use default AGENTS.md), string means a specific tool.
    const agentType = agent === true ? undefined : agent || undefined;

    // Skip the baseline `.mcp.json` only when the user explicitly opted out
    // of Cloud MCP in interactive mode. All other paths (non-interactive,
    // skip-interactive, no .git) still write it so it stays consistent with
    // the agent template that mentions a Cloud MCP config.
    const userOptedOutOfMcp = interactiveConfig !== null && !interactiveConfig.mcpEditor;
    const {
      created,
      agentFileWritten,
      mcpFileWritten
    } = await _hostInitializerMain().HostInitializerMain.init(path, standalone, noPackageJson, reset, resetNew, resetLaneNew, resetHard, resetScope, force, workspaceExtensionProps, interactiveConfig?.generator || generator, agentType, {
      skipDefaultMcp: userOptedOutOfMcp
    });
    return _hostInitializerMain().HostInitializerMain.generateInitMessage(created, reset, resetHard, resetScope, interactiveConfig, interactiveConfig?.agentFileWritten ?? agentFileWritten, mcpFileWritten);
  }
}
exports.InitCmd = InitCmd;

//# sourceMappingURL=init-cmd.js.map