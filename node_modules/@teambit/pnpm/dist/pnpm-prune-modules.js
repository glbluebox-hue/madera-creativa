"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.pnpmPruneModules = pnpmPruneModules;
function _fsExtra() {
  const data = _interopRequireDefault(require("fs-extra"));
  _fsExtra = function () {
    return data;
  };
  return data;
}
function _path() {
  const data = _interopRequireDefault(require("path"));
  _path = function () {
    return data;
  };
  return data;
}
function _lodash() {
  const data = require("lodash");
  _lodash = function () {
    return data;
  };
  return data;
}
function _lockfile() {
  const data = require("@pnpm/lockfile.fs");
  _lockfile = function () {
    return data;
  };
  return data;
}
function _dependenciesPnpm() {
  const data = require("@teambit/dependencies.pnpm.dep-path");
  _dependenciesPnpm = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
/**
 * Reads the private lockfile at node_modules/.pnpm/lock.yaml
 * and removes any directories from node_modules/.pnpm that are not listed in the lockfile.
 */
async function pnpmPruneModules(rootDir) {
  const virtualStoreDir = _path().default.join(rootDir, 'node_modules/.pnpm');
  const pkgDirs = await readPackageDirsFromVirtualStore(virtualStoreDir);
  if (pkgDirs.length === 0) return;
  const lockfile = await (0, _lockfile().readCurrentLockfile)(virtualStoreDir, {
    ignoreIncompatible: false
  });
  const dirsShouldBePresent = Object.keys(lockfile?.packages ?? {}).map(depPath => (0, _dependenciesPnpm().depPathToDirName)(depPath));
  await Promise.all((0, _lodash().difference)(pkgDirs, dirsShouldBePresent).map(dir => _fsExtra().default.remove(_path().default.join(virtualStoreDir, dir))));
}
async function readPackageDirsFromVirtualStore(virtualStoreDir) {
  const allDirs = await _fsExtra().default.readdir(virtualStoreDir);
  return allDirs.filter(dir => dir !== 'lock.yaml' && dir !== 'node_modules');
}

//# sourceMappingURL=pnpm-prune-modules.js.map