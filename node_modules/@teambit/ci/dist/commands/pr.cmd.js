"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.CiPrCmd = void 0;
function _workspace() {
  const data = require("@teambit/workspace");
  _workspace = function () {
    return data;
  };
  return data;
}
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class CiPrCmd {
  constructor(workspace, logger, ci) {
    this.workspace = workspace;
    this.logger = logger;
    this.ci = ci;
    _defineProperty(this, "name", 'pr');
    _defineProperty(this, "description", 'Exports a feature lane to Bit Cloud when a Pull Request is opened or updated.');
    _defineProperty(this, "extendedDescription", `Resolves the lane name from --lane or the current Git branch, validates it, and runs install, status, snap, and export. By default it then restores the workspace by switching back to main; pass --skip-cleanup to skip that restore when the workspace is about to be discarded (e.g. an ephemeral CI container). PR builds run the full pipeline by default; to trade specific tasks for speed on a given PR, add a [skip-tasks: ...] token to the commit message (e.g. [skip-tasks: GeneratePreview,ExtractSchema]), which merges with any --skip-tasks flag. Use in pull-request CI pipelines after tests and before deploy.`);
    _defineProperty(this, "group", 'collaborate');
    _defineProperty(this, "options", [['m', 'message <message>', 'If set, set it as the snap message, if not, try and grab from git-commit-message'], ['l', 'lane <lane>', 'If set, use as the lane name, if not available, grab from git-branch name'], ['b', 'build', 'Set to true to build the app locally, false (default) will build on Ripple CI'], ['s', 'strict', 'Set to true to fail on warnings as well as errors, false (default) only fails on errors'], ['d', 'dry-run', 'Run the full pipeline but skip exporting to remote (build runs only if --build is set)'], ['', 'keep-lane', 'Reuse the same remote lane across PR commits (preserves lane history and cloud UI edits) instead of recreating it on every run'], ['', 'skip-cleanup', 'Skip restoring the workspace (switching back to main) after export. Use when the workspace is discarded right after, e.g. an ephemeral CI container'], ['', 'skip-tasks <tasks>', `Comma-separated list of build/publish task names (or aspect-ids) to skip during snap, e.g. "ExtractSchema,GeneratePreview,GenerateEnvTemplate,PublishComponents". Speeds up PR builds by trading optional artifacts (schema/preview) and publishing for build time; they are produced on the final export to main`]]);
  }
  async report(args, options) {
    this.logger.console('\n\n');
    this.logger.console('🚀 Initializing PR command');
    if (!this.workspace) throw new (_workspace().OutsideWorkspaceError)();
    let laneIdStr;
    let message;
    if (options.lane) {
      laneIdStr = options.lane;
    } else {
      const currentBranch = await this.ci.getBranchName().catch(e => {
        throw new Error(`Failed to get branch name from Git: ${e.toString()}`);
      });
      if (!currentBranch) {
        throw new Error('Failed to get branch name');
      }
      laneIdStr = this.ci.convertBranchToLaneId(currentBranch);
    }
    if (options.message) {
      message = options.message;
    } else {
      const commitMessage = await this.ci.getGitCommitMessage();
      if (!commitMessage) {
        throw new Error('Failed to get commit message');
      }
      message = commitMessage;
    }
    const results = await this.ci.snapPrCommit({
      laneIdStr: laneIdStr,
      message,
      build: options.build,
      strict: options.strict,
      dryRun: options.dryRun,
      keepLane: options.keepLane,
      skipCleanup: options.skipCleanup,
      skipTasks: options.skipTasks
    });
    if (results) {
      return results;
    }
    return `PR command executed successfully`;
  }
}
exports.CiPrCmd = CiPrCmd;

//# sourceMappingURL=pr.cmd.js.map