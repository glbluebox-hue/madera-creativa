import type { Formatter, FormatterContext, FormatResults } from '@teambit/formatter';
import type { Options as PrettierModuleOptions } from 'prettier';
import PrettierLib from 'prettier';
import type { Logger } from '@teambit/logger';
export declare class PrettierFormatter implements Formatter {
    private logger;
    private options;
    /**
     * reference to the prettier module.
     */
    private prettierModule;
    constructor(logger: Logger, options: PrettierModuleOptions, 
    /**
     * reference to the prettier module.
     */
    prettierModule?: typeof PrettierLib);
    id: string;
    displayName: string;
    displayConfig(): string;
    format(context: FormatterContext): Promise<FormatResults>;
    formatSnippet(snippet: string, filePath?: string): Promise<string>;
    check(context: FormatterContext): Promise<FormatResults>;
    private run;
    /**
     * get options for eslint.
     */
    private getOptions;
    private addFilePathToOpts;
    version(): string;
}
