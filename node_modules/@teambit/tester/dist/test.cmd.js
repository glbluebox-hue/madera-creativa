"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.TestCmd = void 0;
function _path() {
  const data = _interopRequireDefault(require("path"));
  _path = function () {
    return data;
  };
  return data;
}
function _fsExtra() {
  const data = _interopRequireDefault(require("fs-extra"));
  _fsExtra = function () {
    return data;
  };
  return data;
}
function _cli() {
  const data = require("@teambit/cli");
  _cli = function () {
    return data;
  };
  return data;
}
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _bitError() {
  const data = require("@teambit/bit-error");
  _bitError = function () {
    return data;
  };
  return data;
}
function _workspace() {
  const data = require("@teambit/workspace");
  _workspace = function () {
    return data;
  };
  return data;
}
function _toolboxTime() {
  const data = require("@teambit/toolbox.time.timer");
  _toolboxTime = function () {
    return data;
  };
  return data;
}
function _toolboxPath() {
  const data = require("@teambit/toolbox.path.path");
  _toolboxPath = function () {
    return data;
  };
  return data;
}
function _legacy() {
  const data = require("@teambit/legacy.constants");
  _legacy = function () {
    return data;
  };
  return data;
}
function _testOutputFormatter() {
  const data = require("./test-output-formatter");
  _testOutputFormatter = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class TestCmd {
  constructor(tester, workspace, logger) {
    this.tester = tester;
    this.workspace = workspace;
    this.logger = logger;
    _defineProperty(this, "name", 'test [pattern-or-test-file...]');
    _defineProperty(this, "description", 'run component tests');
    _defineProperty(this, "extendedDescription", `executes tests using the testing framework configured by each component's environment (Jest, Mocha, etc.).
by default only runs tests for new and modified components. use --unmodified to test all components.
to run specific test files only, pass their paths instead of a component pattern, e.g. "bit test path/to/comp/my-comp.spec.ts".
supports watch mode, coverage reporting, and debug mode for development workflows.`);
    _defineProperty(this, "helpUrl", 'reference/testing/tester-overview');
    _defineProperty(this, "arguments", [{
      name: 'pattern-or-test-file...',
      description: `one of two modes (multiple values are supported, separated by spaces):
1) component pattern: ${_legacy().COMPONENT_PATTERN_HELP}
2) test-file path: a path to a test file, to run only this file, e.g. "bit test path/to/comp/my-comp.spec.ts".
note that a directory path selects the component in that directory and runs all its tests, while a test-file path runs only that file`
    }]);
    _defineProperty(this, "alias", 'at');
    _defineProperty(this, "group", 'testing');
    _defineProperty(this, "options", [['w', 'watch', 'start the tester in watch mode.'], ['d', 'debug', 'start the tester in debug mode.'], ['a', 'all', 'DEPRECATED. (use --unmodified)'], ['u', 'unmodified', 'test all components, not only new and modified'], ['', 'junit <filepath>', 'write tests results as JUnit XML format into the specified file path'], ['', 'coverage', 'show code coverage data'], ['e', 'env <id>', 'test only components assigned the given env'], ['', 'update-snapshot', 'if supported by the tester, re-record every snapshot that fails during the test run'], ['s', 'scope <scope-name>', 'DEPRECATED. (use the pattern instead, e.g. "scopeName/**"). name of the scope to test'], ['j', 'json', 'return the results in json format'], ['', 'verbose', 'list the component ids that have no tests (default collapses them into a count)'], ['', 'summary', 'suppress tester output, print only the final pass/fail headline (or summary object with --json)']
    // TODO: we need to reduce this redundant casting every time.
    ]);
  }
  async report([userPatterns = []], {
    watch = false,
    debug = false,
    all = false,
    env,
    scope,
    junit,
    coverage = false,
    unmodified = false,
    updateSnapshot = false,
    verbose = false,
    summary: summaryOnly = false
  }) {
    const timer = _toolboxTime().Timer.create();
    const scopeName = typeof scope === 'string' ? scope : undefined;
    if (scopeName) {
      this.logger.consoleWarning(`--scope is deprecated, use the pattern argument instead. e.g. "scopeName/**" for the entire scope`);
    }
    if (all) {
      unmodified = all;
      this.logger.consoleWarning(`--all is deprecated, use --unmodified instead`);
    }
    timer.start();
    if (!this.workspace) throw new (_workspace().OutsideWorkspaceError)();
    const {
      components,
      testFiles
    } = await this.getComponentsToTest(userPatterns, unmodified, scopeName);
    if (!components.length) {
      const data = (0, _cli().formatHint)(`no components found to test.\nuse "--unmodified" flag to test all components or specify the ids to test.\notherwise, only new and modified components will be tested`);
      return {
        code: 0,
        data
      };
    }
    if (!summaryOnly) {
      this.logger.console(`testing total of ${components.length} components in workspace '${_chalk().default.cyan(this.workspace.name)}'`);
    }
    let code = 0;
    let tests;
    if (watch && !debug) {
      // avoid turning off the logger for non-watch scenario. otherwise, when this aspect throws errors, they'll be
      // swallowed. (Jest errors are shown regardless via Jest, but if the tester is unable to run Jest in the first
      // place, these errors won't be shown)
      this.logger.off();
      await this.tester.watch(components, {
        watch,
        debug,
        env,
        coverage,
        updateSnapshot,
        testFiles
      });
    } else {
      // testers such as Jest reassign `console.warn` to forward into `this.logger.warn` inside their `test()`,
      // bypassing our stdout/console monkey-patch. also turn the logger off so those re-routed calls don't surface.
      if (summaryOnly) this.logger.off();
      const restore = summaryOnly ? silenceConsoleAndStdout() : undefined;
      try {
        tests = await this.tester.test(components, {
          watch,
          debug,
          env,
          junit,
          coverage,
          updateSnapshot,
          testFiles
        });
      } finally {
        restore?.();
        if (summaryOnly) this.logger.on();
      }
      if (tests.hasErrors()) code = 1;
      if (process.exitCode && process.exitCode !== 0 && typeof process.exitCode === 'number') {
        // this is needed for testers such as "vitest", where it sets the exitCode to non zero when the coverage is not met.
        code = process.exitCode;
      }
    }
    const {
      seconds
    } = timer.stop();
    if (watch) return '';
    const summary = tests ? (0, _testOutputFormatter().aggregateTestResults)(tests, components) : undefined;
    const failedDueToExitCode = code !== 0 && !!tests && !tests.hasErrors();
    const data = summary ? `${summaryOnly ? '' : '\n'}${(0, _testOutputFormatter().formatTestReport)(summary, {
      verbose,
      duration: `${seconds}s`,
      summaryOnly,
      failedDueToExitCode
    })}` : (0, _cli().formatHint)(`tests completed in ${seconds} seconds`);
    return {
      code,
      data
    };
  }
  async json([userPatterns = []], {
    watch = false,
    debug = false,
    env,
    junit,
    coverage = false,
    unmodified = false,
    updateSnapshot = false,
    summary: summaryOnly = false
  }) {
    const timer = _toolboxTime().Timer.create();
    timer.start();
    if (!this.workspace) throw new (_workspace().OutsideWorkspaceError)();
    const {
      components,
      testFiles
    } = await this.getComponentsToTest(userPatterns, unmodified);
    if (!components.length) {
      this.logger.info(`no components found to test.
  use "--unmodified" flag to test all components or specify the ids to test.
  otherwise, only new and modified components will be tested`);
      return {
        code: 0,
        data: []
      };
    }
    let code = 0;
    // also disable the logger: testers like Jest reassign `console.warn` to `this.logger.warn`,
    // which bypasses the stdout/console silencer and can corrupt machine-readable JSON output.
    this.logger.off();
    const restore = silenceConsoleAndStdout();
    let tests;
    try {
      tests = await this.tester.test(components, {
        watch,
        debug,
        env,
        junit,
        coverage,
        updateSnapshot,
        testFiles
      });
    } finally {
      restore();
      this.logger.on();
    }
    if (tests.hasErrors()) code = 1;
    if (process.exitCode && process.exitCode !== 0 && typeof process.exitCode === 'number') {
      // this is needed for testers such as "vitest", where it sets the exitCode to non zero when the coverage is not met.
      code = process.exitCode;
    }
    const aggregated = (0, _testOutputFormatter().aggregateTestResults)(tests, components);
    const summary = {
      totals: aggregated.totals,
      componentsWithTests: aggregated.componentsWithTests.map(c => ({
        id: c.id.toString({
          ignoreVersion: true
        }),
        passed: c.passed,
        failed: c.failed,
        pending: c.pending,
        hasError: c.hasError
      })),
      componentsWithoutTests: aggregated.componentsWithoutTests.map(id => id.toString({
        ignoreVersion: true
      })),
      componentsAffectedByEnvError: aggregated.componentsAffectedByEnvError.map(id => id.toString({
        ignoreVersion: true
      })),
      envErrors: aggregated.envErrors.map(e => ({
        envId: e.envId,
        message: e.error.message
      }))
    };
    if (summaryOnly) {
      return {
        code,
        data: summary
      };
    }
    const data = tests.results.map(r => ({
      data: {
        components: r.data?.components.map(c => _objectSpread(_objectSpread({}, c), {}, {
          componentId: c.componentId.toString()
        })),
        errors: r.data?.errors
      },
      error: r.error
    }));
    return {
      code,
      data
    };
  }

  /**
   * resolve the components to test from the user input. the input entries can be either component patterns or paths
   * of test files. when test-file paths are provided, also return their absolute paths so the tester runs only them.
   */
  async getComponentsToTest(userPatterns, unmodified, scopeName) {
    const byTestFiles = await this.getComponentsByTestFiles(userPatterns);
    if (byTestFiles) return byTestFiles;
    const getPatternWithScope = () => {
      if (!userPatterns.length && !scopeName) return undefined;
      const pattern = userPatterns.length ? userPatterns.join(', ') : '**';
      return scopeName ? `${scopeName}/${pattern}` : pattern;
    };
    const patternWithScope = getPatternWithScope();
    // If pattern is provided, don't pass the unmodified flag as "all" - the pattern should take precedence
    const components = await this.workspace.getComponentsByUserInput(patternWithScope ? false : unmodified, patternWithScope, true);
    return {
      components
    };
  }

  /**
   * if the given entries are paths of existing files, resolve them to their owning components.
   * returns undefined when none of the entries is an existing file, in which case they are treated as
   * component patterns.
   */
  async getComponentsByTestFiles(entries) {
    if (!entries.length) return undefined;
    const isFile = entry => {
      const absPath = _path().default.resolve(entry);
      try {
        return _fsExtra().default.statSync(absPath).isFile();
      } catch {
        // e.g. the file doesn't exist or permission issues. treat as a non-file so it goes through the pattern flow.
        return false;
      }
    };
    const fileEntries = entries.filter(entry => isFile(entry));
    if (!fileEntries.length) return undefined;
    if (fileEntries.length < entries.length) {
      const nonFiles = entries.filter(entry => !fileEntries.includes(entry));
      throw new (_bitError().BitError)(`unable to mix test-file paths with component patterns. the following are not files: ${nonFiles.join(', ')}`);
    }
    const componentsById = {};
    const testFiles = [];
    for (const entry of fileEntries) {
      const absPath = _path().default.resolve(entry);
      const componentId = await this.workspace.getComponentIdByPath(absPath);
      if (!componentId) {
        throw new (_bitError().BitError)(`the file "${entry}" does not belong to any component in this workspace, unable to test it`);
      }
      const idStr = componentId.toString();
      if (!componentsById[idStr]) {
        componentsById[idStr] = await this.workspace.get(componentId);
      }
      const component = componentsById[idStr];
      const testFilesOfComp = this.tester.getTestFiles(component);
      const absPathLinux = (0, _toolboxPath().pathNormalizeToLinux)(absPath);
      if (!testFilesOfComp.some(file => (0, _toolboxPath().pathNormalizeToLinux)(file.path) === absPathLinux)) {
        const suggestion = testFilesOfComp.length ? `the test files of "${component.id.toStringWithoutVersion()}" are: ${testFilesOfComp.map(file => file.relative).join(', ')}` : `the component "${component.id.toStringWithoutVersion()}" has no test files`;
        throw new (_bitError().BitError)(`the file "${entry}" is not a recognized test file. ${suggestion}`);
      }
      testFiles.push(absPath);
    }
    return {
      components: Object.values(componentsById),
      testFiles
    };
  }
}

/**
 * Disables all console logging (via console.*) and direct writes to
 * process.stdout / process.stderr. Returns a function that, when called,
 * restores everything back to normal.
 */
exports.TestCmd = TestCmd;
function silenceConsoleAndStdout() {
  const CONSOLE_METHODS = ['log', 'warn', 'error', 'info', 'debug'];
  const originalConsole = Object.fromEntries(
  // eslint-disable-next-line no-console
  CONSOLE_METHODS.map(m => [m, console[m]]));
  const originalStdoutWrite = process.stdout.write.bind(process.stdout);
  const originalStderrWrite = process.stderr.write.bind(process.stderr);
  for (const method of CONSOLE_METHODS) {
    // eslint-disable-next-line no-console
    console[method] = () => {};
  }

  // process.stdout.write(chunk, encoding?, callback?) — callers may rely on the optional
  // callback firing. Invoke it so we don't leave writers hanging.
  const stubWrite = (...args) => {
    const cb = args.find(a => typeof a === 'function');
    if (cb) cb();
    return true;
  };
  process.stdout.write = stubWrite;
  process.stderr.write = stubWrite;
  return () => {
    for (const method of CONSOLE_METHODS) {
      // eslint-disable-next-line no-console
      console[method] = originalConsole[method];
    }
    process.stdout.write = originalStdoutWrite;
    process.stderr.write = originalStderrWrite;
  };
}

//# sourceMappingURL=test.cmd.js.map