import type { Command, CommandOptions, GenericObject } from '@teambit/cli';
import type { Logger } from '@teambit/logger';
import type { Workspace } from '@teambit/workspace';
import type { TesterMain } from './tester.main.runtime';
type TestFlags = {
    watch: boolean;
    debug: boolean;
    all: boolean;
    unmodified: boolean;
    env?: string;
    scope?: string;
    junit?: string;
    coverage?: boolean;
    updateSnapshot: boolean;
    verbose?: boolean;
    summary?: boolean;
};
export declare class TestCmd implements Command {
    private tester;
    private workspace;
    private logger;
    name: string;
    description: string;
    extendedDescription: string;
    helpUrl: string;
    arguments: {
        name: string;
        description: string;
    }[];
    alias: string;
    group: string;
    options: CommandOptions;
    constructor(tester: TesterMain, workspace: Workspace, logger: Logger);
    report([userPatterns]: [string[]], { watch, debug, all, env, scope, junit, coverage, unmodified, updateSnapshot, verbose, summary: summaryOnly, }: TestFlags): Promise<"" | {
        code: number;
        data: string;
    }>;
    json([userPatterns]: [string[]], { watch, debug, env, junit, coverage, unmodified, updateSnapshot, summary: summaryOnly, }: TestFlags): Promise<GenericObject>;
    /**
     * resolve the components to test from the user input. the input entries can be either component patterns or paths
     * of test files. when test-file paths are provided, also return their absolute paths so the tester runs only them.
     */
    private getComponentsToTest;
    /**
     * if the given entries are paths of existing files, resolve them to their owning components.
     * returns undefined when none of the entries is an existing file, in which case they are treated as
     * component patterns.
     */
    private getComponentsByTestFiles;
}
export {};
