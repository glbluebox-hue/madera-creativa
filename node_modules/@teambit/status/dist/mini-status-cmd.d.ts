import type { Command, CommandOptions } from '@teambit/cli';
import type { StatusMain } from './status.main.runtime';
export type MiniStatusOpts = {
    showIssues?: boolean;
    ignoreCircularDependencies?: boolean;
};
export declare class MiniStatusCmd implements Command {
    private status;
    name: string;
    description: string;
    extendedDescription: string;
    arguments: {
        name: string;
        description: string;
    }[];
    group: string;
    alias: string;
    private: boolean;
    options: CommandOptions;
    loadAspects: boolean;
    loader: boolean;
    constructor(status: StatusMain);
    report([pattern]: [string], opts: MiniStatusOpts): Promise<string>;
    json([pattern]: [string], opts: MiniStatusOpts): Promise<Record<string, any>>;
}
