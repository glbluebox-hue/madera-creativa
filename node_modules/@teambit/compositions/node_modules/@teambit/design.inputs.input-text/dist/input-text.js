var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { forwardRef, createRef, useState, useEffect, } from 'react';
import classNames from 'classnames';
import { useForm } from '@teambit/base-react.forms.form';
import { NoticeTooltip, Accent } from '@teambit/design.overlays.notice-tooltip';
import { Icon } from '@teambit/design.elements.icon';
import { getAccent } from './color-accent';
import styles from './input-text.module.scss';
import sizes from './sizes.module.scss';
export const InputText = forwardRef(function InputTextWithRef(_a, ref) {
    var { className, style, error, success, filled, icon, placeholder, inputClass, messageClass, message, activeLabel = true, inputSize = 'm' } = _a, rest = __rest(_a, ["className", "style", "error", "success", "filled", "icon", "placeholder", "inputClass", "messageClass", "message", "activeLabel", "inputSize"]);
    const accent = getAccent({ error, success });
    const { onChange } = useForm();
    const [containerEl, setContainerEl] = useState();
    const containerRef = createRef();
    const hasIcon = !!icon;
    const hasMessage = !!message;
    useEffect(() => {
        setContainerEl(containerRef.current);
    }, []);
    return (_jsxs("div", { ref: containerRef, className: classNames(styles.container, (hasIcon || hasMessage) && styles.inputTextWithIcon, hasIcon && hasMessage && styles.twoIcons, className), style: style, children: [_jsx("input", Object.assign({ ref: ref, className: classNames(styles.inputText, filled && styles.filled, accent, sizes.inputSizes, inputClass), "data-size": inputSize, placeholder: activeLabel ? ' ' : placeholder, onChange: onChange || rest.onChange }, rest)), activeLabel && _jsx("label", { "data-size": inputSize, children: placeholder }), _jsxs("div", { className: classNames(styles.iconsHolder, !hasIcon && !hasMessage && styles.hide), children: [!!message && (_jsx(NoticeTooltip, { content: { message }, accent: Accent.negative, trigger: "mouseenter focus, focusin", hideOnClick: false, triggerTarget: containerEl, children: _jsx("div", { className: styles.errorIconWrapper, children: _jsx(Icon, { of: "info-exclamation", className: styles.errorIcon }) }) })), icon] })] }));
});
//# sourceMappingURL=input-text.js.map