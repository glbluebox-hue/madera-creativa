var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import { useCallback, useEffect, useState } from 'react';
import { useForm } from '@teambit/base-react.forms.form';
import { InputText } from './input-text';
function useRequired(name) {
    const [message, setMessage] = useState(undefined);
    const { onValidate, values } = useForm();
    const value = values === null || values === void 0 ? void 0 : values[name];
    useEffect(() => {
        if (!value || value === '')
            return;
        setMessage(undefined);
    }, [value]);
    useEffect(() => {
        onValidate === null || onValidate === void 0 ? void 0 : onValidate(name, message);
    }, [name, message, onValidate]);
    const handleBlur = useCallback((e) => {
        if (e.target.required && (!e.target.value || e.target.value === '')) {
            return setMessage('Required');
        }
        return undefined;
    }, []);
    return { errorMessage: message, onBlur: handleBlur };
}
export function RequiredInputText(_a) {
    var { name = 'required', placeholder = 'Required', customErrorMessage } = _a, rest = __rest(_a, ["name", "placeholder", "customErrorMessage"]);
    const { errorMessage, onBlur } = useRequired(name);
    const error = errorMessage || customErrorMessage;
    return (_jsx(InputText, Object.assign({}, rest, { name: name, placeholder: placeholder, error: !!error, message: error, onBlur: onBlur, required: true })));
}
//# sourceMappingURL=required-input-text.js.map