"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.classNamesTransformer = classNamesTransformer;
function _typescript() {
  const data = _interopRequireDefault(require("typescript"));
  _typescript = function () {
    return data;
  };
  return data;
}
function _replaceName() {
  const data = require("./replaceName");
  _replaceName = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function classNamesTransformer(nameMapping) {
  return context => {
    const {
      factory
    } = context;
    const visit = node => {
      if (_typescript().default.isClassDeclaration(node) && node.name) {
        const oldName = node.name.text;
        const newName = (0, _replaceName().replaceName)(oldName, nameMapping);
        if (newName) {
          const updatedMembers = node.members.map(member => {
            if (_typescript().default.isPropertyDeclaration(member)) {
              const oldMemberName = member.name.getText();
              const newMemberName = (0, _replaceName().replaceName)(oldMemberName, nameMapping);
              return factory.updatePropertyDeclaration(member, member.modifiers, newMemberName ? _typescript().default.factory.createIdentifier(newMemberName) : member.name, member.questionToken, member.type, member.initializer);
            }
            if (_typescript().default.isMethodDeclaration(member)) {
              const oldMemberName = member.name.getText();
              const newMemberName = (0, _replaceName().replaceName)(oldMemberName, nameMapping);
              const updatedParameters = member.parameters.map(param => {
                return _typescript().default.factory.updateParameterDeclaration(param, param.modifiers, param.dotDotDotToken, param.name, param.questionToken, param.type, param.initializer);
              });
              return factory.updateMethodDeclaration(member, member.modifiers, member.asteriskToken, newMemberName ? _typescript().default.factory.createIdentifier(newMemberName) : member.name, member.questionToken, member.typeParameters, updatedParameters, member.type, member.body);
            }
            return member;
          });
          return factory.updateClassDeclaration(node, node.modifiers, factory.createIdentifier(newName), node.typeParameters, node.heritageClauses, updatedMembers);
        }
      }
      return _typescript().default.visitEachChild(node, visit, context);
    };
    return node => _typescript().default.visitNode(node, visit);
  };
}

//# sourceMappingURL=class.js.map