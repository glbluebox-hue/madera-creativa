"use strict";

function _chai() {
  const data = require("chai");
  _chai = function () {
    return data;
  };
  return data;
}
function _fsExtra() {
  const data = _interopRequireDefault(require("fs-extra"));
  _fsExtra = function () {
    return data;
  };
  return data;
}
function _path() {
  const data = _interopRequireDefault(require("path"));
  _path = function () {
    return data;
  };
  return data;
}
function _defenderFs() {
  const data = require("@teambit/defender.fs.global-bit-temp-dir");
  _defenderFs = function () {
    return data;
  };
  return data;
}
function _symlinkDir() {
  const data = _interopRequireDefault(require("symlink-dir"));
  _symlinkDir = function () {
    return data;
  };
  return data;
}
function _hardLinkDirectory() {
  const data = require("./hard-link-directory");
  _hardLinkDirectory = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
describe('hardLinkDirectory()', () => {
  it('should hard link files (excluding node_modules)', async () => {
    const tempDir = (0, _defenderFs().globalBitTempDir)();
    const srcDir = _path().default.join(tempDir, 'source');
    const dest1Dir = _path().default.join(tempDir, 'dest1');
    const dest2Dir = _path().default.join(tempDir, 'dest2');
    _fsExtra().default.mkdirpSync(srcDir);
    _fsExtra().default.mkdirpSync(dest1Dir);
    _fsExtra().default.mkdirpSync(_path().default.join(srcDir, 'node_modules'));
    _fsExtra().default.mkdirpSync(_path().default.join(srcDir, 'subdir'));
    _fsExtra().default.writeFileSync(_path().default.join(srcDir, 'file.txt'), 'Hello World');
    _fsExtra().default.writeFileSync(_path().default.join(srcDir, 'subdir/file.txt'), 'Hello World');
    _fsExtra().default.writeFileSync(_path().default.join(srcDir, 'node_modules/file.txt'), 'Hello World');
    await (0, _hardLinkDirectory().hardLinkDirectory)(srcDir, [dest1Dir, dest2Dir]);

    // It should link the files from the root
    (0, _chai().expect)(_fsExtra().default.readFileSync(_path().default.join(dest1Dir, 'file.txt'), 'utf8')).to.equal('Hello World');
    (0, _chai().expect)(_fsExtra().default.readFileSync(_path().default.join(dest2Dir, 'file.txt'), 'utf8')).to.equal('Hello World');

    // It should link files from a subdirectory
    (0, _chai().expect)(_fsExtra().default.readFileSync(_path().default.join(dest1Dir, 'subdir/file.txt'), 'utf8')).to.equal('Hello World');
    (0, _chai().expect)(_fsExtra().default.readFileSync(_path().default.join(dest2Dir, 'subdir/file.txt'), 'utf8')).to.equal('Hello World');

    // It should not link files from node_modules
    (0, _chai().expect)(_fsExtra().default.existsSync(_path().default.join(dest1Dir, 'node_modules/file.txt'))).to.equal(false);
    (0, _chai().expect)(_fsExtra().default.existsSync(_path().default.join(dest2Dir, 'node_modules/file.txt'))).to.equal(false);
  });
  it('hard link a directory that has a symlinked directory', async () => {
    const tempDir = (0, _defenderFs().globalBitTempDir)();
    const symlinkTargetDir = _path().default.join(tempDir, 'symlink-target');
    const srcDir = _path().default.join(tempDir, 'source');
    const dest1Dir = _path().default.join(tempDir, 'dest1');
    const dest2Dir = _path().default.join(tempDir, 'dest2');
    _fsExtra().default.mkdirpSync(symlinkTargetDir);
    _fsExtra().default.writeFileSync(_path().default.join(symlinkTargetDir, 'file.txt'), 'Hello World');
    _fsExtra().default.mkdirpSync(srcDir);
    _fsExtra().default.mkdirpSync(dest1Dir);
    await (0, _symlinkDir().default)(symlinkTargetDir, _path().default.join(srcDir, 'symlinked-dir'));
    await (0, _hardLinkDirectory().hardLinkDirectory)(srcDir, [dest1Dir, dest2Dir]);
    (0, _chai().expect)(_fsExtra().default.readFileSync(_path().default.join(dest1Dir, 'symlinked-dir', 'file.txt'), 'utf8')).to.equal('Hello World');
    (0, _chai().expect)(_fsExtra().default.readFileSync(_path().default.join(dest2Dir, 'symlinked-dir', 'file.txt'), 'utf8')).to.equal('Hello World');
  });
  it('copy symlinked files', async () => {
    const tempDir = (0, _defenderFs().globalBitTempDir)();
    const symlinkTargetDir = _path().default.join(tempDir, 'symlink-target');
    const srcDir = _path().default.join(tempDir, 'source');
    const dest1Dir = _path().default.join(tempDir, 'dest1');
    const dest2Dir = _path().default.join(tempDir, 'dest2');
    _fsExtra().default.mkdirpSync(symlinkTargetDir);
    _fsExtra().default.mkdirpSync(srcDir);
    _fsExtra().default.mkdirpSync(dest1Dir);
    const symlinkTargetFile = _path().default.join(symlinkTargetDir, 'file.txt');
    _fsExtra().default.writeFileSync(symlinkTargetFile, 'Hello World');
    await (0, _symlinkDir().default)(symlinkTargetFile, _path().default.join(srcDir, 'file.txt'));
    await (0, _hardLinkDirectory().hardLinkDirectory)(srcDir, [dest1Dir, dest2Dir]);
    (0, _chai().expect)(_fsExtra().default.readFileSync(_path().default.join(dest1Dir, 'file.txt'), 'utf8')).to.equal('Hello World');
    (0, _chai().expect)(_fsExtra().default.lstatSync(_path().default.join(dest1Dir, 'file.txt')).isSymbolicLink()).to.equal(false);
    (0, _chai().expect)(_fsExtra().default.readFileSync(_path().default.join(dest2Dir, 'file.txt'), 'utf8')).to.equal('Hello World');
    (0, _chai().expect)(_fsExtra().default.lstatSync(_path().default.join(dest2Dir, 'file.txt')).isSymbolicLink()).to.equal(false);
  });
  it('fall back to copying when hard linking fails with EXDEV', async () => {
    const tempDir = (0, _defenderFs().globalBitTempDir)();
    const srcDir = _path().default.join(tempDir, 'source');
    const dest1Dir = _path().default.join(tempDir, 'dest1');
    const dest2Dir = _path().default.join(tempDir, 'dest2');
    _fsExtra().default.mkdirpSync(srcDir);
    _fsExtra().default.mkdirpSync(dest1Dir);
    _fsExtra().default.mkdirpSync(dest2Dir);
    _fsExtra().default.writeFileSync(_path().default.join(srcDir, 'file.txt'), 'Hello World');
    const originalLink = _fsExtra().default.link;
    _fsExtra().default.link = () => {
      const err = new Error('EXDEV: cross-device link not permitted');
      err.code = 'EXDEV';
      return Promise.reject(err);
    };
    try {
      await (0, _hardLinkDirectory().hardLinkDirectory)(srcDir, [dest1Dir, dest2Dir]);
    } finally {
      _fsExtra().default.link = originalLink;
    }
    (0, _chai().expect)(_fsExtra().default.readFileSync(_path().default.join(dest1Dir, 'file.txt'), 'utf8')).to.equal('Hello World');
    (0, _chai().expect)(_fsExtra().default.readFileSync(_path().default.join(dest2Dir, 'file.txt'), 'utf8')).to.equal('Hello World');
    // the fallback copies the file, so it must not share an inode with the source
    (0, _chai().expect)(_fsExtra().default.statSync(_path().default.join(dest1Dir, 'file.txt')).ino).to.not.equal(_fsExtra().default.statSync(_path().default.join(srcDir, 'file.txt')).ino);
  });
  it('skip broken symlink', async () => {
    const tempDir = (0, _defenderFs().globalBitTempDir)();
    const symlinkTargetDir = _path().default.join(tempDir, 'symlink-target');
    const srcDir = _path().default.join(tempDir, 'source');
    const dest1Dir = _path().default.join(tempDir, 'dest1');
    const dest2Dir = _path().default.join(tempDir, 'dest2');
    _fsExtra().default.mkdirpSync(symlinkTargetDir);
    _fsExtra().default.mkdirpSync(srcDir);
    _fsExtra().default.mkdirpSync(dest1Dir);
    _fsExtra().default.mkdirpSync(dest2Dir);
    const symlinkTargetFile = _path().default.join(symlinkTargetDir, 'file.txt');
    _fsExtra().default.writeFileSync(symlinkTargetFile, 'Hello World');
    await (0, _symlinkDir().default)(symlinkTargetFile, _path().default.join(srcDir, 'file.txt'));
    _fsExtra().default.unlinkSync(symlinkTargetFile);
    await (0, _hardLinkDirectory().hardLinkDirectory)(srcDir, [dest1Dir, dest2Dir]);
    (0, _chai().expect)(_fsExtra().default.readdirSync(dest1Dir)).to.deep.equal([]);
    (0, _chai().expect)(_fsExtra().default.readdirSync(dest2Dir)).to.deep.equal([]);
  });
});

//# sourceMappingURL=hard-link-directory.spec.js.map