"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.EnvServiceList = void 0;
class EnvServiceList {
  constructor(
  /**
   * environment
   */
  env,
  /**
   * services available on the env.
   */
  services) {
    this.env = env;
    this.services = services;
  }
  async toObject() {
    return {
      env: this.env.toObject(),
      services: await Promise.all(this.services.map(async ([id, service]) => {
        return {
          id,
          name: service.name,
          description: service.description,
          // @ts-ignore
          data: await service.getDescriptor(this.env)
        };
      }))
    };
  }
}
exports.EnvServiceList = EnvServiceList;

//# sourceMappingURL=env-service-list.js.map