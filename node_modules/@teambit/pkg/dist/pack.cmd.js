"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.PackCmd = void 0;
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); } // eslint-disable-next-line @typescript-eslint/no-unused-vars
class PackCmd {
  constructor(packer) {
    this.packer = packer;
    _defineProperty(this, "name", 'pack <componentId> [scopePath]');
    _defineProperty(this, "description", 'create tar for npm publish');
    _defineProperty(this, "options", [['d', 'out-dir <out-dir>', 'directory to put the result tar file'], ['o', 'override', 'override existing pack file'], ['k', 'keep', 'should keep isolated environment [default = false]'], ['p', 'prefix', 'keep custom (binding) prefix'],
    // ['c', 'use-capsule', 'isolate using the capsule and pack on the capsule'],
    ['j', 'json', 'return the output as JSON']]);
    _defineProperty(this, "alias", '');
    _defineProperty(this, "group", 'collaborate');
    _defineProperty(this, "private", true);
  }
  async report(args, options) {
    const packResult = await this.json(args, options);
    const warnings = packResult.data?.warnings || [];
    const warningsOutput = warnings.map(warning => _chalk().default.yellow(warning)).join('\n');
    const errors = packResult.data?.errors || [];
    const errorsOutput = errors.map(error => _chalk().default.yellow(error)).join('\n');
    const tarPathOutput = packResult.data.metadata?.tarPath ? _chalk().default.green(`tar path for component ${packResult.data.id}: ${packResult.data.metadata?.tarPath}`) : '';
    return `${warningsOutput}\n${errorsOutput}\n${tarPathOutput}`;
  }
  async json([componentId, scopePath], options) {
    const compId = typeof componentId === 'string' ? componentId : componentId[0];
    let scopePathStr;
    if (scopePath) {
      scopePathStr = typeof scopePath !== 'string' ? scopePath[0] : scopePath;
    }
    const concreteOpts = {
      writeOptions: {
        outDir: options.outDir,
        override: options.override
      },
      prefix: options.prefix,
      keep: options.keep
      // useCapsule: options.useCapsule,
    };
    const packResult = await this.packer.packComponent(compId, scopePathStr, concreteOpts);
    return {
      data: packResult,
      code: 0
    };
  }
}
exports.PackCmd = PackCmd;

//# sourceMappingURL=pack.cmd.js.map