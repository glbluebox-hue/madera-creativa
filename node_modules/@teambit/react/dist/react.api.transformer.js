"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ReactAPITransformer = void 0;
function _semanticsEntities() {
  const data = require("@teambit/semantics.entities.semantic-schema");
  _semanticsEntities = function () {
    return data;
  };
  return data;
}
function _react() {
  const data = require("./react.schema");
  _react = function () {
    return data;
  };
  return data;
}
const REACT_FILE_EXT = ['.tsx', '.jsx'];

// only detects functional react components for now
class ReactAPITransformer {
  predicate(node) {
    const isFunctionLike = node.__schema === _semanticsEntities().FunctionLikeSchema.name;
    if (!isFunctionLike) return false;
    const functionNode = node;
    const isReactFile = REACT_FILE_EXT.some(r => functionNode.location.filePath.includes(r));
    if (!isReactFile) return false;
    const params = functionNode.params;
    if (params.length > 1) return false;
    const returnsPotentialReactElement = ['JSX.Element', 'React.ReactNode', 'null', 'undefined', 'React.ReactChild', 'React.ReactFragment', 'React.ReactPortal', 'React.JSX.Element'].includes(this.getReturnTypeName(node));
    if (!returnsPotentialReactElement) return false;
    return true;
  }
  async transform(node) {
    return new (_react().ReactSchema)(node.location, node.name, new (_semanticsEntities().TypeRefSchema)(node.returnType.location, this.getReturnTypeName(node), undefined, 'react'), node.params[0], node.signature, node.modifiers, node.doc, node.typeParams);
  }
  getReturnTypeName(node) {
    const returnType = node.returnType;
    return returnType.name ?? returnType.toString();
  }
}
exports.ReactAPITransformer = ReactAPITransformer;

//# sourceMappingURL=react.api.transformer.js.map