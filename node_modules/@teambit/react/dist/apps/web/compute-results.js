"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.computeResults = computeResults;
async function computeResults(results, {
  publicDir,
  ssrPublicDir
}) {
  const result = results[0];
  return {
    publicDir,
    // TODO: remove this later. it's for backward compatibility. use "metadata.publicDir" instead.
    ssrPublicDir,
    // TODO: remove this later. it's for backward compatibility. use "metadata.ssrPublicDir" instead.
    metadata: {
      publicDir,
      ssrPublicDir
    },
    errors: result.errors,
    warnings: result.warnings
  };
}

//# sourceMappingURL=compute-results.js.map