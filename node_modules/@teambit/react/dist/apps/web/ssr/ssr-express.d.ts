import type { Asset } from '@teambit/bundler';
import type { Logger } from '@teambit/logger';
import type { HtmlAssets } from '@teambit/react.rendering.ssr';
import Express from 'express';
type ExpressSsrOptions = {
    name: string;
    workdir: string;
    port: number;
    app: any;
    assets: HtmlAssets;
    logger?: Logger;
};
export declare function createExpressSsr({ name, workdir, port, app, assets, logger }: ExpressSsrOptions): Express.Express;
export declare function loadSsrApp(workdir: string, appName: string): Promise<any>;
export declare function parseAssets(assets: Asset[], publicPath?: string): HtmlAssets;
export {};
