import type { BundlerResult } from '@teambit/bundler';
import type { ReactAppBuildResult } from './react-build-result';
type ResultsOptions = {
    publicDir: string;
    ssrPublicDir?: string;
};
export declare function computeResults(results: BundlerResult[], { publicDir, ssrPublicDir }: ResultsOptions): Promise<ReactAppBuildResult>;
export {};
