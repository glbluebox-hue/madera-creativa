import type { Logger } from '@teambit/logger';
import type { DependencyResolverMain } from '@teambit/dependency-resolver';
import type { ApplicationType } from '@teambit/application';
import type { ReactAppOptions } from './react-app-options';
import { ReactApp } from './react.application';
import type { ReactEnv } from '../../react.env';
export declare class ReactAppType implements ApplicationType<ReactAppOptions> {
    readonly name: string;
    private reactEnv;
    private logger;
    private dependencyResolver;
    constructor(name: string, reactEnv: ReactEnv, logger: Logger, dependencyResolver: DependencyResolverMain);
    createApp(options: ReactAppOptions): ReactApp;
}
