"use strict";

function _chai() {
  const data = require("chai");
  _chai = function () {
    return data;
  };
  return data;
}
function _modelComponent() {
  const data = _interopRequireDefault(require("./model-component"));
  _modelComponent = function () {
    return data;
  };
  return data;
}
function _lodash() {
  const data = require("lodash");
  _lodash = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
const modelComponentFixture = {
  name: 'bar/foo',
  scope: 'remote-scope',
  versions: {
    '0.0.1': '125a37bdb17220bdc1406a9a28a3dde4eec91225'
  },
  lang: 'javascript',
  deprecated: false,
  bindingPrefix: '@bit',
  remotes: [{
    url: 'file:///tmp/remote-scope',
    name: 'remote-scope',
    date: '1572532837438'
  }],
  state: {
    versions: {
      '0.0.1': {
        local: true
      }
    }
  }
};
const getModelComponentFixture = () => {
  return (0, _lodash().clone)(modelComponentFixture);
};
const getModelComponent = obj => {
  return _modelComponent().default.parse(JSON.stringify(obj));
};
describe('ModelComponent', () => {
  describe('validate', () => {
    let validateFunc;
    describe('duplicate hashes', () => {
      let modelComponent;
      before(() => {
        const fixture = getModelComponentFixture();
        fixture.versions['0.0.2'] = fixture.versions['0.0.1'];
        modelComponent = getModelComponent(fixture);
        validateFunc = () => modelComponent.validate();
      });
      it('should throw an error', () => {
        (0, _chai().expect)(validateFunc).to.throw('the following hash(es) are duplicated');
      });
    });
  });
});

//# sourceMappingURL=model-component.spec.js.map