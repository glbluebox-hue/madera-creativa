"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
function _zlib() {
  const data = require("zlib");
  _zlib = function () {
    return data;
  };
  return data;
}
function _legacy() {
  const data = require("@teambit/legacy.constants");
  _legacy = function () {
    return data;
  };
  return data;
}
function _legacy2() {
  const data = require("@teambit/legacy.utils");
  _legacy2 = function () {
    return data;
  };
  return data;
}
function _toolboxCrypto() {
  const data = require("@teambit/toolbox.crypto.sha1");
  _toolboxCrypto = function () {
    return data;
  };
  return data;
}
function _legacy3() {
  const data = require("@teambit/legacy.scope");
  _legacy3 = function () {
    return data;
  };
  return data;
}
function _ref() {
  const data = _interopRequireDefault(require("./ref"));
  _ref = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function parse(buffer) {
  const {
    type,
    hash,
    contents
  } = extractHeaderAndContent(buffer);
  if (!_legacy3().typesObj[type]) throw new (_legacy3().UnknownObjectType)(type);
  return _legacy3().typesObj[type].parse(contents, hash);
}
function extractHeaderAndContent(buffer) {
  const firstNullByteLocation = buffer.indexOf(_legacy().NULL_BYTE);
  const headers = buffer.slice(0, firstNullByteLocation).toString();
  const [type, hash] = headers.split(_legacy().SPACE_DELIMITER);
  const contents = buffer.slice(firstNullByteLocation + 1, buffer.length);
  return {
    type,
    hash,
    contents
  };
}
class BitObject {
  constructor() {
    _defineProperty(this, "validateBeforePersist", true);
  }
  // validate the object before persisting
  id() {
    throw new Error('id() was not implemented...');
  }

  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  toBuffer(pretty) {
    throw new Error('toBuffer() was not implemented...');
  }
  refs() {
    return [];
  }
  getType() {
    return this.constructor.name;
  }
  getHeader(buffer) {
    return `${this.getType()} ${this.hash().toString()} ${buffer.toString().length}${_legacy().NULL_BYTE}`;
  }
  async collectRefs(repo) {
    const refsCollection = [];
    const objectType = this.getType();
    const objectId = objectType === 'Component' ? `Component ${this.id()}` : objectType;
    async function addRefs(object) {
      const refs = object.refs();
      let objs;
      try {
        objs = await Promise.all(refs.map(ref => ref.load(repo, true)));
      } catch (err) {
        if (err.code === 'ENOENT') {
          throw new Error(`failed finding an object file required by ${object.constructor.name} object, originated from ${objectId}
path: ${err.path}`);
        }
        throw err;
      }

      // @ts-ignore AUTO-ADDED-AFTER-MIGRATION-PLEASE-FIX!
      refsCollection.push(...refs);
      await Promise.all(objs.map(obj => addRefs(obj)));
    }
    await addRefs(this);
    return refsCollection;
  }
  async collectRaw(repo) {
    const refs = await this.collectRefs(repo);
    return repo.loadManyRaw(refs);
  }
  asRaw(repo) {
    return repo.loadRaw(this.hash());
  }
  collect(repo) {
    const objects = [];
    function addRefs(object) {
      const objs = object.refs().map(ref => {
        return ref.loadSync(repo);
      });
      objects.push(...objs);
      objs.forEach(obj => addRefs(obj));
    }
    addRefs(this);
    return objects;
  }

  /**
   * indexing method
   */
  hash() {
    // console.log(`sha ${sha1(this.id())}, id ${this.id()}`); // uncomment when debugging hash issues
    return new (_ref().default)(BitObject.makeHash(this.id()));
  }
  compress() {
    return (0, _legacy2().deflate)(this.serialize());
  }
  serialize() {
    const buffer = this.toBuffer();
    return Buffer.concat([Buffer.from(this.getHeader(buffer)), buffer]);
  }

  /**
   * see `this.parseSync` for the sync version
   */
  static async parseObject(fileContents, filePath) {
    const buffer = await (0, _legacy2().inflate)(fileContents, filePath);
    return parse(buffer);
  }

  /**
   * same as `parseObject`, however, if the type is not one of the given "typeNames", it returns null.
   * the performance improvement is huge compare to "parseObject", as it doesn't parse the object if not needed.
   */
  static async parseObjectOnlyIfType(fileContents, typeNames, filePath) {
    const buffer = await (0, _legacy2().inflate)(fileContents, filePath);
    const {
      type
    } = extractHeaderAndContent(buffer);
    if (typeNames.includes(type)) return parse(buffer);
    return null;
  }

  /**
   * prefer using `this.parseObject()`, unless it must be sync.
   */
  static parseSync(fileContents) {
    // @ts-ignore todo: fix after merging #9359
    const buffer = (0, _zlib().inflateSync)(fileContents);
    return parse(buffer);
  }
  static makeHash(str) {
    return (0, _toolboxCrypto().sha1)(str);
  }
}
exports.default = BitObject;

//# sourceMappingURL=object.js.map