"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.formatWriteOutput = formatWriteOutput;
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _path() {
  const data = require("path");
  _path = function () {
    return data;
  };
  return data;
}
function _formatCleanOutput() {
  const data = require("./format-clean-output");
  _formatCleanOutput = function () {
    return data;
  };
  return data;
}
function _writeOutputsTexts() {
  const data = require("./write-outputs-texts");
  _writeOutputsTexts = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function formatWriteOutput(writeConfigFilesResult, flags) {
  const {
    cleanResults,
    writeResults,
    wsDir
  } = writeConfigFilesResult;
  const isDryRun = !!(flags.dryRun || flags.dryRunWithContent);
  const cleanResultsOutput = cleanResults?.length ? (0, _formatCleanOutput().formatCleanOutput)(cleanResults, {
    dryRun: isDryRun
  }) : undefined;
  const writeResultsOutput = getWriteResultsOutput(writeResults, wsDir, isDryRun);
  const cleanWriteOutput = cleanResultsOutput ? `${cleanResultsOutput}\n${writeResultsOutput}` : writeResultsOutput;
  return `${cleanWriteOutput}\n\n${_writeOutputsTexts().SUMMARY}`;
}
function getWriteResultsOutput(writeResults, wsDir, isDryRun) {
  const totalExtendingConfigFiles = writeResults.totalExtendingConfigFiles;
  const writeTitle = isDryRun ? _chalk().default.green(`${totalExtendingConfigFiles} files will be written`) : _chalk().default.green((0, _writeOutputsTexts().WRITE_TITLE)(totalExtendingConfigFiles));
  const writeOutput = writeResults.writersResult.map(writerResult => getOneWriterOutput(writerResult, wsDir)).join('\n\n');
  return `${writeTitle}\n${writeOutput}`;
}
function getOneWriterOutput(writerResult, wsDir) {
  const title = _chalk().default.blue(`${writerResult.totalExtendingConfigFiles} ${_chalk().default.bold(writerResult.writerId.toString())} configurations added`);
  const writersOutput = getExtendingConfigFilesOutput(writerResult.extendingConfigFiles, wsDir);
  return `${title}\n${writersOutput}`;
}
function getExtendingConfigFilesOutput(extendingConfigFiles, wsDir) {
  const extendingConfigFilesOutput = extendingConfigFiles.map(envsWrittenExtendingConfigFile => getEnvGroupExtendingConfigFilesOutput(envsWrittenExtendingConfigFile, wsDir)).join('\n');
  return `${extendingConfigFilesOutput}`;
}
function getEnvGroupExtendingConfigFilesOutput(envsWrittenExtendingConfigFile, wsDir) {
  const extendingConfigFile = envsWrittenExtendingConfigFile.extendingConfigFile;
  const paths = extendingConfigFile.filePaths.map(p => `  ${(0, _path().relative)(wsDir, p)}`).join('\n');
  return `${paths}`;
}

//# sourceMappingURL=format-write-output.js.map