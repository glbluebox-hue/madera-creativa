"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.formatCleanOutput = formatCleanOutput;
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function formatCleanOutput(cleanResults = [], flags) {
  const isDryRun = !!flags.dryRun;
  const cleanResultsOutput = getCleanResultsOutput(cleanResults, isDryRun);
  return `${cleanResultsOutput}`;
}
function getCleanResultsOutput(cleanResults, isDryRun) {
  const cleanResultsOutput = cleanResults ? `${_chalk().default.green(`${cleanResults.length} config files ${isDryRun ? 'will be' : 'were'} deleted`)}\n  ${cleanResults.join('\n  ')}\n` : '';
  return cleanResultsOutput;
}

//# sourceMappingURL=format-clean-output.js.map