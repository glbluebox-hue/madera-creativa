"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.formatListOutput = formatListOutput;
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function formatListOutput(result) {
  return Object.values(result).map(item => getEnvOutput(item.envId, item.configWriters)).join('\n\n');
}
function getEnvOutput(envId, configWriters) {
  const title = _chalk().default.bold(envId);
  const space = '  ';
  if (!configWriters.length) return `${title}\n${space}${_chalk().default.yellow('no config writers found')}`;
  const entries = configWriters.map(configWriter => {
    return `${configWriter.name} (${configWriter.id})`;
  }).join(`\n${space}`);
  return `${title}\n${space}${entries}`;
}

//# sourceMappingURL=format-list-output.js.map