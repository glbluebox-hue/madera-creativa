"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.handleExtendingConfigFiles = handleExtendingConfigFiles;
function _normalizePath() {
  const data = _interopRequireDefault(require("normalize-path"));
  _normalizePath = function () {
    return data;
  };
  return data;
}
function _stringFormat() {
  const data = _interopRequireDefault(require("string-format"));
  _stringFormat = function () {
    return data;
  };
  return data;
}
function _toolboxCrypto() {
  const data = require("@teambit/toolbox.crypto.sha1");
  _toolboxCrypto = function () {
    return data;
  };
  return data;
}
function _fsExtra() {
  const data = _interopRequireDefault(require("fs-extra"));
  _fsExtra = function () {
    return data;
  };
  return data;
}
function _path() {
  const data = require("path");
  _path = function () {
    return data;
  };
  return data;
}
function _pMapSeries() {
  const data = _interopRequireDefault(require("p-map-series"));
  _pMapSeries = function () {
    return data;
  };
  return data;
}
function _lodash() {
  const data = require("lodash");
  _lodash = function () {
    return data;
  };
  return data;
}
function _dedupPaths() {
  const data = require("../dedup-paths");
  _dedupPaths = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
async function handleExtendingConfigFiles(envEntries, envCompsDirsMap, writtenRealConfigFiles, configsRootDir, componentsRootDir, workspaceDir, opts) {
  const extendingConfigFilesMap = await buildExtendingConfigFilesMap(envEntries, writtenRealConfigFiles, envCompsDirsMap, configsRootDir, workspaceDir);
  const fileHashPerDedupedPaths = (0, _dedupPaths().dedupePaths)(extendingConfigFilesMap, envCompsDirsMap, componentsRootDir);
  await postProcessExtendingConfigFiles(envEntries, envCompsDirsMap, extendingConfigFilesMap, fileHashPerDedupedPaths, configsRootDir, workspaceDir);
  const envsWrittenExtendingConfigFiles = await writeExtendingConfigFiles(extendingConfigFilesMap, fileHashPerDedupedPaths, workspaceDir, opts);
  return envsWrittenExtendingConfigFiles;
}
async function buildExtendingConfigFilesMap(envEntries, writtenRealConfigFiles, envCompsDirsMap, configsRootDir, workspaceDir) {
  const allEnvsCalculatedExtendingConfigFiles = (0, _lodash().compact)(await (0, _pMapSeries().default)(envEntries, async envConfigFileEntry => {
    const envMapVal = envCompsDirsMap[envConfigFileEntry.envId];
    const writtenConfigFilesForEnv = getEnvOnlyWrittenRealConfigFiles(envConfigFileEntry.envId, writtenRealConfigFiles);
    const extendingConfigFile = generateOneExtendingConfigFile(writtenConfigFilesForEnv, envConfigFileEntry, envMapVal, configsRootDir, workspaceDir);
    if (!extendingConfigFile) {
      return undefined;
    }
    return {
      envId: envConfigFileEntry.envId,
      extendingConfigFile
    };
  }));
  const extendingConfigFilesMap = indexExtendingConfigFiles(allEnvsCalculatedExtendingConfigFiles);
  return extendingConfigFilesMap;
}
function getEnvOnlyWrittenRealConfigFiles(envId, writtenRealConfigFiles) {
  return Object.values(writtenRealConfigFiles).reduce((acc, {
    envIds,
    writtenRealConfigFile
  }) => {
    if (envIds.includes(envId)) {
      acc.push(writtenRealConfigFile);
    }
    return acc;
  }, []);
}
function generateOneExtendingConfigFile(writtenConfigFiles, envConfigFileEntry, envMapValue, configsRootDir, workspaceDir) {
  const {
    configWriter,
    executionContext
  } = envConfigFileEntry;
  const args = {
    workspaceDir,
    configsRootDir,
    writtenConfigFiles,
    executionContext,
    envMapValue
  };
  const extendingConfigFile = configWriter.generateExtendingFile(args);
  if (!extendingConfigFile) return undefined;
  const hash = extendingConfigFile.hash || (0, _toolboxCrypto().sha1)(extendingConfigFile.content);
  return _objectSpread(_objectSpread({}, extendingConfigFile), {}, {
    useAbsPaths: !!extendingConfigFile.useAbsPaths,
    hash
  });
}
function indexExtendingConfigFiles(multiEnvCalculatedExtendingConfigFiles) {
  const mergedConfigFiles = multiEnvCalculatedExtendingConfigFiles.reduce((acc, curr) => {
    const extendingConfigFile = curr.extendingConfigFile;
    const currentValue = acc[extendingConfigFile.hash];
    if (currentValue) {
      currentValue.envIds.push(curr.envId);
    } else {
      acc[extendingConfigFile.hash] = {
        envIds: [curr.envId],
        extendingConfigFile
      };
    }
    return acc;
  }, {});
  return mergedConfigFiles;
}
async function postProcessExtendingConfigFiles(envEntries, envCompsDirsMap, extendingConfigFilesMap, fileHashPerDedupedPaths, configsRootDir, workspaceDir) {
  await (0, _pMapSeries().default)(envEntries, async envConfigFileEntry => {
    const postProcessFunc = envConfigFileEntry.configWriter.postProcessExtendingConfigFiles;
    if (!postProcessFunc) {
      return undefined;
    }
    const envMapVal = envCompsDirsMap[envConfigFileEntry.envId];
    const extendingConfigFileEntry = Object.values(extendingConfigFilesMap).find(entry => {
      return entry.envIds.includes(envConfigFileEntry.envId);
    });
    if (!extendingConfigFileEntry) {
      return undefined;
    }
    const dedupEntry = fileHashPerDedupedPaths.find(fileHashPerDedupedPath => {
      return fileHashPerDedupedPath.fileHash === extendingConfigFileEntry.extendingConfigFile.hash;
    });
    if (!dedupEntry) {
      return undefined;
    }
    const postProcessRes = await postProcessFunc({
      configsRootDir,
      extendingConfigFile: extendingConfigFileEntry.extendingConfigFile,
      envMapValue: envMapVal,
      workspaceDir,
      paths: dedupEntry.paths,
      supportSpecificPathChange: true
    });
    if (!postProcessRes) {
      return undefined;
    }
    if (typeof postProcessRes === 'string') {
      extendingConfigFileEntry.extendingConfigFile.content = postProcessRes;
      return undefined;
    }
    postProcessRes.forEach(({
      path,
      content
    }) => {
      // Remove it from the current entry
      dedupEntry.paths = dedupEntry.paths.filter(currPath => currPath !== path);
      const newHash = (0, _toolboxCrypto().sha1)(content);
      extendingConfigFilesMap[newHash] = JSON.parse(JSON.stringify(extendingConfigFileEntry));
      extendingConfigFilesMap[newHash].extendingConfigFile.content = content;
      const foundNewHash = fileHashPerDedupedPaths.find(entry => entry.fileHash === newHash);
      if (foundNewHash) {
        foundNewHash.paths.push(path);
      } else {
        fileHashPerDedupedPaths.push({
          fileHash: newHash,
          paths: [path]
        });
      }
    });
    return undefined;
  });
}
async function writeExtendingConfigFiles(extendingConfigFilesMap, fileHashPerDedupedPaths, workspaceDir, opts) {
  const finalResult = await Promise.all(fileHashPerDedupedPaths.map(async ({
    fileHash,
    paths
  }) => {
    const envsConfigFile = extendingConfigFilesMap[fileHash];
    const configFile = envsConfigFile.extendingConfigFile;
    const hash = configFile.hash || (0, _toolboxCrypto().sha1)(configFile.content);
    const name = (0, _stringFormat().default)(configFile.name, {
      hash
    });
    const writtenPaths = await Promise.all(paths.map(async path => {
      const filePath = (0, _path().join)(workspaceDir, path, name);
      const targetPath = configFile.useAbsPaths ? configFile.extendingTarget.filePath : (0, _normalizePath().default)(`./${(0, _path().relative)((0, _path().dirname)(filePath), configFile.extendingTarget.filePath)}`);
      const content = configFile.content.replace(`{${configFile.extendingTarget.name}}`, targetPath);
      if (!opts.dryRun) {
        await _fsExtra().default.outputFile(filePath, content);
      }
      return filePath;
    }));
    const res = {
      envIds: envsConfigFile.envIds,
      extendingConfigFile: {
        name,
        hash,
        content: configFile.content,
        extendingTarget: configFile.extendingTarget,
        filePaths: writtenPaths
      }
    };
    return res;
  }));
  return finalResult;
}

//# sourceMappingURL=extending-config-files.js.map