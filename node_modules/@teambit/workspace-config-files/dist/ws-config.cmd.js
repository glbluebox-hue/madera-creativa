"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.WsConfigWriteCmd = exports.WsConfigListCmd = exports.WsConfigCmd = exports.WsConfigCleanCmd = void 0;
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _outputs() {
  const data = require("./outputs");
  _outputs = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); } /* eslint-disable max-classes-per-file */
const COMMAND_NAME = 'ws-config';
class WsConfigCmd {
  constructor() {
    _defineProperty(this, "name", `${COMMAND_NAME} <sub-command>`);
    _defineProperty(this, "alias", 'workspace-config');
    _defineProperty(this, "description", 'generate IDE configuration files');
    _defineProperty(this, "extendedDescription", `writes configuration files (tsconfig.json, eslintrc.js, etc.) to your workspace for better IDE support.
automatically generates configs based on your components' environments and settings.
useful for enabling proper IntelliSense, linting, and type-checking in your IDE.`);
    _defineProperty(this, "options", []);
    _defineProperty(this, "group", 'workspace-tools');
    _defineProperty(this, "commands", []);
  }
  // helpUrl = '';

  async report([unrecognizedSubcommand]) {
    return _chalk().default.red(`"${unrecognizedSubcommand}" is not a subcommand of "ws-config", please run "bit ws-config --help" to list the subcommands`);
  }
}
exports.WsConfigCmd = WsConfigCmd;
class WsConfigWriteCmd {
  constructor(workspaceConfigFilesMain) {
    this.workspaceConfigFilesMain = workspaceConfigFilesMain;
    _defineProperty(this, "name", 'write');
    _defineProperty(this, "description", 'write config files in the workspace. useful for IDEs');
    _defineProperty(this, "alias", '');
    _defineProperty(this, "group", 'workspace-tools');
    _defineProperty(this, "options", [['c', 'clean', 'delete existing config files from the workspace. highly recommended to run it with "--dry-run" first'], ['w', 'writers <writers>', `only write config files for the given writers. use comma to separate multiple writers. use ${COMMAND_NAME} list to see all writers`], ['s', 'silent', 'do not prompt for confirmation'], ['', 'no-dedupe', "write configs inside each one of the component's dir, avoid deduping"], ['', 'dry-run', 'show the paths that configs will be written per env'], ['', 'dry-run-with-content', 'use with --json flag. show the config content and the paths that will be written per env'], ['v', 'verbose', 'showing verbose output for writing'], ['j', 'json', 'json format']]);
  }
  async report(_args, flags) {
    const results = await this.json(_args, flags);
    if (flags.dryRunWithContent) {
      throw new Error(`use --json flag along with --dry-run-with-content`);
    }
    const envsNotImplementing = this.workspaceConfigFilesMain.getEnvsNotImplementing();
    const warning = getWarningForNonImplementingEnvs(envsNotImplementing);
    const output = flags.verbose ? (0, _outputs().verboseFormatWriteOutput)(results, flags) : (0, _outputs().formatWriteOutput)(results, flags);
    return warning + output;
  }
  async json(_args, flags) {
    const {
      clean,
      silent,
      noDedupe,
      dryRunWithContent,
      writers
    } = flags;
    const dryRun = dryRunWithContent ? true : !!flags.dryRun;
    const {
      cleanResults,
      writeResults,
      wsDir
    } = await this.workspaceConfigFilesMain.writeConfigFiles({
      clean,
      dedupe: !noDedupe,
      dryRun,
      silent,
      writers: writers?.split(',')
    });
    if (dryRun) {
      const updatedWriteResults = writeResults;
      if (!dryRunWithContent) {
        updatedWriteResults.writersResult = updatedWriteResults.writersResult.map(oneWriterResult => {
          oneWriterResult.realConfigFiles.forEach(realConfigFile => {
            realConfigFile.writtenRealConfigFile.content = '';
          });
          oneWriterResult.extendingConfigFiles.forEach(extendingConfigFile => {
            extendingConfigFile.extendingConfigFile.content = '';
          });
          return oneWriterResult;
        });
      }
      return {
        wsDir,
        cleanResults,
        writeResults: updatedWriteResults
      };
    }
    return {
      wsDir,
      cleanResults,
      writeResults
    };
  }
}
exports.WsConfigWriteCmd = WsConfigWriteCmd;
class WsConfigCleanCmd {
  constructor(workspaceConfigFilesMain) {
    this.workspaceConfigFilesMain = workspaceConfigFilesMain;
    _defineProperty(this, "name", 'clean');
    _defineProperty(this, "description", 'clean (delete) written config files in the workspace. useful for IDEs');
    _defineProperty(this, "alias", '');
    _defineProperty(this, "group", 'workspace-tools');
    _defineProperty(this, "options", [['s', 'silent', 'do not prompt for confirmation'], ['w', 'writers <writers>', `only clean config files for the given writers. use comma to separate multiple writers. use ${COMMAND_NAME} list to see all writers`], ['', 'dry-run', 'show the paths of configs that will be cleaned'], ['j', 'json', 'json format']]);
  }
  async report(_args, flags) {
    const results = await this.json(_args, flags);
    const envsNotImplementing = this.workspaceConfigFilesMain.getEnvsNotImplementing();
    const warning = getWarningForNonImplementingEnvs(envsNotImplementing);
    const output = (0, _outputs().formatCleanOutput)(results, flags);
    return warning + output;
  }
  async json(_args, flags) {
    const {
      silent,
      dryRun
    } = flags;
    const cleanResults = await this.workspaceConfigFilesMain.cleanConfigFiles({
      dryRun,
      silent,
      writers: flags.writers?.split(',')
    });
    return cleanResults;
  }
}
exports.WsConfigCleanCmd = WsConfigCleanCmd;
class WsConfigListCmd {
  constructor(workspaceConfigFilesMain) {
    this.workspaceConfigFilesMain = workspaceConfigFilesMain;
    _defineProperty(this, "name", 'list');
    _defineProperty(this, "description", 'list config writers');
    _defineProperty(this, "alias", '');
    _defineProperty(this, "group", 'workspace-tools');
    _defineProperty(this, "options", [['j', 'json', 'json format']]);
  }
  async report() {
    const results = await this.json();
    const envsNotImplementing = this.workspaceConfigFilesMain.getEnvsNotImplementing();
    const warning = getWarningForNonImplementingEnvs(envsNotImplementing);
    const output = (0, _outputs().formatListOutput)(results);
    return warning + output;
  }
  async json() {
    const cleanResults = await this.workspaceConfigFilesMain.listConfigWriters();
    return cleanResults;
  }
}
exports.WsConfigListCmd = WsConfigListCmd;
function getWarningForNonImplementingEnvs(envsNotImplementing) {
  if (!envsNotImplementing.length) return '';
  const message = _chalk().default.yellow(`Bit cannot determine the correct contents for the config files to write. this may result in incorrect content.
The following environments need to add support for config files: ${_chalk().default.cyan(envsNotImplementing.join(', '))}.
consider updating these envs to implement config file writing to improve the dev experience.

`);
  return message;
}

//# sourceMappingURL=ws-config.cmd.js.map