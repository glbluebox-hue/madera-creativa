"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = exports.WorkspaceConfigFilesMain = void 0;
function _fsExtra() {
  const data = _interopRequireDefault(require("fs-extra"));
  _fsExtra = function () {
    return data;
  };
  return data;
}
function _path() {
  const data = require("path");
  _path = function () {
    return data;
  };
  return data;
}
function _globby() {
  const data = _interopRequireDefault(require("globby"));
  _globby = function () {
    return data;
  };
  return data;
}
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _bitError() {
  const data = require("@teambit/bit-error");
  _bitError = function () {
    return data;
  };
  return data;
}
function _pMapSeries() {
  const data = _interopRequireDefault(require("p-map-series"));
  _pMapSeries = function () {
    return data;
  };
  return data;
}
function _legacy() {
  const data = require("@teambit/legacy.consumer");
  _legacy = function () {
    return data;
  };
  return data;
}
function _yesno() {
  const data = _interopRequireDefault(require("yesno"));
  _yesno = function () {
    return data;
  };
  return data;
}
function _lodash() {
  const data = require("lodash");
  _lodash = function () {
    return data;
  };
  return data;
}
function _cli() {
  const data = require("@teambit/cli");
  _cli = function () {
    return data;
  };
  return data;
}
function _workspace() {
  const data = require("@teambit/workspace");
  _workspace = function () {
    return data;
  };
  return data;
}
function _envs() {
  const data = require("@teambit/envs");
  _envs = function () {
    return data;
  };
  return data;
}
function _logger() {
  const data = require("@teambit/logger");
  _logger = function () {
    return data;
  };
  return data;
}
function _workspaceConfigFiles() {
  const data = require("./workspace-config-files.aspect");
  _workspaceConfigFiles = function () {
    return data;
  };
  return data;
}
function _wsConfig() {
  const data = require("./ws-config.cmd");
  _wsConfig = function () {
    return data;
  };
  return data;
}
function _writeFailed() {
  const data = _interopRequireDefault(require("./exceptions/write-failed"));
  _writeFailed = function () {
    return data;
  };
  return data;
}
function _workspaceConfigFiles2() {
  const data = require("./workspace-config-files.service");
  _workspaceConfigFiles2 = function () {
    return data;
  };
  return data;
}
function _writers() {
  const data = require("./writers");
  _writers = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class PromptCanceled extends _bitError().BitError {
  constructor() {
    super(_chalk().default.yellow('operation aborted'));
  }
}
/**
 * Configs that can be configured in the workspace.jsonc file
 */

class WorkspaceConfigFilesMain {
  constructor(workspace, envs, logger, config) {
    this.workspace = workspace;
    this.envs = envs;
    this.logger = logger;
    this.config = config;
    _defineProperty(this, "envsNotImplementing", {});
  }

  /**
   * It writes the configuration files for the workspace
   * for example: tsconfig, eslint config and prettier config files.
   * @param {WriteConfigFilesOptions} options - WriteConfigFilesOptions = {}
   * @returns An object with the following properties:
   * - writeResults: An object with the following properties:
   *   - aspectsWritersResults: An array of objects with the following properties:
   *     - aspect: The aspect that was written
   *     - totalWrittenFiles: The number of files that were written
   *   - totalWrittenFiles: The total number of files that were written
   * - cleanResults: array of deleted paths
   */
  async writeConfigFiles(options = {}) {
    if (!this.workspace) {
      throw new (_legacy().ConsumerNotFound)();
    }
    const defaultOpts = {
      clean: false,
      dedupe: false,
      silent: false,
      dryRun: false,
      throw: true
    };
    const optionsWithDefaults = (0, _lodash().defaults)(options, defaultOpts);
    const execContext = await this.getExecContext();
    let pathsToClean = [];
    let writeErr;
    let writeResults;
    try {
      if (optionsWithDefaults.clean) {
        pathsToClean = await this.calcPathsToClean({
          writers: optionsWithDefaults.writers
        });
      }
      writeResults = await this.write(execContext, optionsWithDefaults);
      const allWrittenFiles = writeResults.writersResult.flatMap(writerResult => {
        return writerResult.extendingConfigFiles.flatMap(extendingConfigFile => {
          return extendingConfigFile.extendingConfigFile.filePaths;
        });
      });
      // Avoid delete and re-create files that were written by other config writers
      // instead of deleting at the beginning then write all
      // we write all and then delete the files that were not written by the config writers
      // This reduces the config files that re-created (as many times no changes needed)
      // which prevent issues with needing to restart the ts-server in the ide
      pathsToClean = pathsToClean.filter(pathToClean => !allWrittenFiles.includes((0, _path().join)(this.workspace.path, pathToClean)));
      await this.deleteFiles(pathsToClean);
    } catch (err) {
      this.logger.info('writeConfigFiles failed', err);
      if (optionsWithDefaults.throw) {
        throw new (_writeFailed().default)();
      }
      writeErr = err;
    }
    return {
      writeResults,
      cleanResults: pathsToClean,
      wsDir: this.workspace.path,
      err: writeErr
    };
  }

  /**
   * This will check the config.enableWorkspaceConfigWrite before writing the config files.
   */
  async writeConfigFilesIfEnabled(options = {}) {
    const shouldWrite = this.isWorkspaceConfigWriteEnabled();
    if (!shouldWrite) return undefined;
    return this.writeConfigFiles(options);
  }

  /**
   * It cleans (delete) the config files from the workspace.
   * This will check each file and will only delete it in case it was generated by bit
   * @param {CleanConfigFilesOptions} options - CleanConfigFilesOptions = {}
   * @returns An array of strings.
   */
  async cleanConfigFiles(options = {}) {
    // const execContext = await this.getExecContext();
    if (!this.workspace) {
      throw new (_legacy().ConsumerNotFound)();
    }
    const cleanResults = await this.clean(options);
    return cleanResults;
  }

  /**
   * The function checks if the auto writing of workspace configuration is enabled.
   * if it's enabled we will re-generate the configuration files upon bit create
   * @returns the boolean value of `!!this.config.enableWorkspaceConfigWrite`.
   */
  isWorkspaceConfigWriteEnabled() {
    return !!this.config.enableWorkspaceConfigWrite;
  }

  /**
   * It returns a list of all the config writers that have been registered with the config writer slot
   * @returns An array of objects with aspectId and configWriter properties.
   */
  async listConfigWriters() {
    if (!this.workspace) {
      throw new (_legacy().ConsumerNotFound)();
    }
    const execContexts = await this.getExecContext();
    const result = execContexts.map(executionContext => {
      const configWriters = this.getConfigWriters(executionContext);
      return {
        envId: executionContext.envRuntime.id,
        executionContext,
        configWriters
      };
    });
    return result;
  }
  groupByWriterId(writerList) {
    return writerList.reduce((acc, envConfigWriter) => {
      envConfigWriter.configWriters.forEach(configWriter => {
        acc[configWriter.id] = acc[configWriter.id] || [];
        acc[configWriter.id].push({
          configWriter,
          envId: envConfigWriter.envId
        });
      });
      return acc;
    }, {});
  }
  async write(envsExecutionContext, opts) {
    const envCompDirsMap = this.getEnvComponentsDirsMap(envsExecutionContext);
    const configsRootDir = this.getConfigsRootDir();
    const componentsRootDir = this.getComponentsRootDir();
    const configWriters = await this.listConfigWriters();
    const writerIdsToEnvEntriesMap = this.groupByWriterId(configWriters);
    const filteredWriterIdsToEnvEntriesMap = opts.writers ? (0, _lodash().pick)(writerIdsToEnvEntriesMap, opts.writers) : writerIdsToEnvEntriesMap;
    let totalRealConfigFiles = 0;
    let totalExtendingConfigFiles = 0;
    const results = await (0, _pMapSeries().default)(Object.entries(filteredWriterIdsToEnvEntriesMap), async ([writerId, envEntries]) => {
      const oneResult = await this.handleOneIdWriter(writerId, envEntries, envCompDirsMap, configsRootDir, componentsRootDir, opts);
      totalRealConfigFiles += oneResult.totalRealConfigFiles;
      totalExtendingConfigFiles += oneResult.totalExtendingConfigFiles;
      return oneResult;
    });
    const totalWrittenFiles = totalRealConfigFiles + totalExtendingConfigFiles;
    return {
      writersResult: results,
      totalWrittenFiles,
      totalRealConfigFiles,
      totalExtendingConfigFiles
    };
  }
  async handleOneIdWriter(writerId, envEntries, envCompsDirsMap, configsRootDir, componentsRootDir, opts) {
    const writtenRealConfigFilesMap = await (0, _writers().handleRealConfigFiles)(envEntries, envCompsDirsMap, configsRootDir, this.workspace.path, opts);
    const writtenExtendingConfigFiles = await (0, _writers().handleExtendingConfigFiles)(envEntries, envCompsDirsMap, writtenRealConfigFilesMap, configsRootDir, componentsRootDir, this.workspace.path, opts);
    const writtenRealConfigFiles = Object.values(writtenRealConfigFilesMap);
    const totalRealConfigFiles = writtenRealConfigFiles.length;
    const totalExtendingConfigFiles = writtenExtendingConfigFiles.reduce((acc, curr) => acc + curr.extendingConfigFile.filePaths.length, 0);
    const totalWrittenFiles = totalRealConfigFiles + totalExtendingConfigFiles;
    return {
      writerId,
      totalWrittenFiles,
      realConfigFiles: writtenRealConfigFiles,
      totalRealConfigFiles,
      extendingConfigFiles: writtenExtendingConfigFiles,
      totalExtendingConfigFiles
    };
  }
  getConfigsRootDir() {
    const userConfiguredDir = this.config.configsRootDir;
    return userConfiguredDir ? (0, _path().join)(this.workspace.path, userConfiguredDir) : this.getCacheDir(this.workspace.path);
  }
  getComponentsRootDirFromComponentsRootDir() {
    const componentsRootDir = this.config.componentsRootDir;
    if (!componentsRootDir) return undefined;
    // Remove leading './' or '/' and trailing slash
    const normalized = (0, _path().normalize)(componentsRootDir).replace(/^\.?\//, '').replace(/\/$/, '');
    return normalized;
  }
  getComponentsRootDirFromDefaultDir() {
    if (!this.config.useDefaultDirectory) return undefined;
    const defaultDir = this.workspace.defaultDirectory;
    if (!defaultDir) return undefined;
    // get path until first dynamic segment (segment with {})
    const segments = defaultDir.split('/');
    const dynamicSegmentIndex = segments.findIndex(segment => segment.startsWith('{'));
    if (dynamicSegmentIndex === -1) return defaultDir;
    return segments.slice(0, dynamicSegmentIndex).join('/');
  }
  getComponentsRootDir() {
    const fromComponentsRootDir = this.getComponentsRootDirFromComponentsRootDir();
    if (fromComponentsRootDir) return fromComponentsRootDir;
    const fromDefaultDir = this.getComponentsRootDirFromDefaultDir();
    if (fromDefaultDir) return fromDefaultDir;
  }
  getCacheDir(rootDir) {
    return (0, _path().join)(rootDir, 'node_modules', '.cache');
  }
  async getExecContext() {
    const components = await this.workspace.list();
    const runtime = await this.envs.createEnvironment(components);
    const execContext = runtime.getEnvExecutionContext();
    return execContext;
  }
  getEnvComponentsDirsMap(envsExecutionContext) {
    const envCompDirsMap = envsExecutionContext.reduce((acc, envExecution) => {
      const envRuntime = envExecution.envRuntime;
      const envId = envRuntime.id.toString();
      const value = {
        id: envRuntime.id,
        env: envRuntime.env,
        paths: envRuntime.components.map(c => this.workspace.componentDir(c.id, undefined, {
          relative: true
        }))
      };
      acc[envId] = value;
      return acc;
    }, {});
    return envCompDirsMap;
  }
  getConfigWriters(envExecutionContext) {
    if (envExecutionContext.env.workspaceConfig && (0, _lodash().isFunction)(envExecutionContext.env.workspaceConfig)) {
      return envExecutionContext.env.workspaceConfig();
    }
    this.addToEnvsNotImplementing(envExecutionContext.env.id);
    return [];
  }
  getFlatConfigWriters(envsExecutionContext) {
    return (0, _lodash().flatMap)(envsExecutionContext, envExecutionContext => {
      return this.getConfigWriters(envExecutionContext);
    });
  }

  /**
   * Clean config files written by the config-writers
   * @param envsExecutionContext
   * @param param1
   * @returns Array of paths of deleted config files
   */
  async clean({
    dryRun,
    silent,
    writers
  }) {
    const paths = await this.calcPathsToClean({
      writers
    });
    if (dryRun) return paths;
    if (!silent) await this.promptForCleaning(paths);
    await this.deleteFiles(paths);
    return paths;
  }
  async calcPathsToClean({
    writers
  }) {
    const execContext = await this.getExecContext();
    const configWriters = this.getFlatConfigWriters(execContext);
    const filteredConfigWriters = writers ? configWriters.filter(configWriter => writers.includes(configWriter.id)) : configWriters;
    const paths = (0, _lodash().uniq)(filteredConfigWriters.map(configWriter => {
      const patterns = configWriter.patterns;
      const currPaths = _globby().default.sync(patterns, {
        cwd: this.workspace.path,
        dot: true,
        onlyFiles: true,
        ignore: ['**/node_modules/**']
      });
      const filteredPaths = currPaths.filter(path => {
        const fullPath = (0, _path().join)(this.workspace.path, path);
        return configWriter.isBitGenerated ? configWriter.isBitGenerated(fullPath) : true;
      });
      return filteredPaths;
    }).flat());
    return paths;
  }
  addToEnvsNotImplementing(envId) {
    this.envsNotImplementing[envId] = true;
  }
  getEnvsNotImplementing() {
    return Object.keys(this.envsNotImplementing);
  }
  async promptForCleaning(paths) {
    this.logger.clearStatusLine();
    const ok = await (0, _yesno().default)({
      question: `${_chalk().default.underline('The following files will be deleted:')}
${paths.join('\n')}
${_chalk().default.bold('Do you want to continue? [yes(y)/no(n)]')}`
    });
    if (!ok) {
      throw new PromptCanceled();
    }
  }
  async deleteFiles(paths) {
    await Promise.all(paths.map(f => _fsExtra().default.remove((0, _path().join)(this.workspace.path, f))));
  }
  static async provider([cli, workspace, envs, loggerAspect], config) {
    const logger = loggerAspect.createLogger(_workspaceConfigFiles().WorkspaceConfigFilesAspect.id);
    envs.registerService(new (_workspaceConfigFiles2().WorkspaceConfigFilesService)(logger));
    const workspaceConfigFilesMain = new WorkspaceConfigFilesMain(workspace, envs, logger, config);
    const wsConfigCmd = new (_wsConfig().WsConfigCmd)();
    wsConfigCmd.commands = [new (_wsConfig().WsConfigWriteCmd)(workspaceConfigFilesMain), new (_wsConfig().WsConfigCleanCmd)(workspaceConfigFilesMain), new (_wsConfig().WsConfigListCmd)(workspaceConfigFilesMain)];
    cli.register(wsConfigCmd);
    return workspaceConfigFilesMain;
  }
}
exports.WorkspaceConfigFilesMain = WorkspaceConfigFilesMain;
_defineProperty(WorkspaceConfigFilesMain, "slots", []);
// define your aspect dependencies here.
// in case you need to use another aspect API.
_defineProperty(WorkspaceConfigFilesMain, "dependencies", [_cli().CLIAspect, _workspace().WorkspaceAspect, _envs().EnvsAspect, _logger().LoggerAspect]);
_defineProperty(WorkspaceConfigFilesMain, "runtime", _cli().MainRuntime);
_defineProperty(WorkspaceConfigFilesMain, "defaultConfig", {
  enableWorkspaceConfigWrite: false
});
_workspaceConfigFiles().WorkspaceConfigFilesAspect.addRuntime(WorkspaceConfigFilesMain);
var _default = exports.default = WorkspaceConfigFilesMain;

//# sourceMappingURL=workspace-config-files.main.runtime.js.map