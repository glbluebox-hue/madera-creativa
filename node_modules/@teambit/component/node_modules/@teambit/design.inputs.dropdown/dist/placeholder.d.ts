import { ReactElement } from 'react';
import { MenuItemsProps } from '@teambit/design.inputs.selectors.menu-item';
declare type PlaceholderProps = {
    /**
     * An optional icon element to be rendered at the end of the placeholder to replace the default arrow.
     */
    dropdownIcon?: ReactElement;
} & MenuItemsProps;
export declare function Placeholder({ dropdownIcon, children, className, ...rest }: PlaceholderProps): JSX.Element;
export {};
