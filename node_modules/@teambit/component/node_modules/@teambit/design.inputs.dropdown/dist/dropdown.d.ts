import React from 'react';
import { DrawerProps } from '@teambit/base-ui.surfaces.drawer';
import { ContaineeProps } from '@teambit/base-ui.surfaces.abs-container';
import type { Position } from '@teambit/base-ui.surfaces.abs-container';
import { ElevationHeight } from '@teambit/base-ui.css-components.elevation';
import { Roundness } from '@teambit/base-ui.css-components.roundness';
export declare type DropdownProps = {
    /**
     * add class to the dropdown menu.
     */
    dropClass?: string;
    /**
     * add border to the placeholder.
     */
    placeholderBorder?: boolean;
    /**
     * a plugin to display elements at the top section of the dropdown menu
     */
    topPlugin?: React.ReactNode;
    /**
     * a plugin to display elements at the bottom section of the dropdown menu
     */
    bottomPlugin?: React.ReactNode;
} & DrawerProps & DropdownMenuProps;
export declare function Dropdown({ children, position, elevation, roundness, margin, placeholderContent, dropClass, placeholderBorder, topPlugin, bottomPlugin, clickToggles, className, ...rest }: DropdownProps): JSX.Element;
declare type DropdownMenuProps = {
    position?: Position;
    elevation?: ElevationHeight;
    roundness?: Roundness;
} & Omit<ContaineeProps, 'onChange'>;
export {};
