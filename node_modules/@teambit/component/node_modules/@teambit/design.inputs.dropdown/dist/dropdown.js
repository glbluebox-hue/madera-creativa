var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React from 'react';
import classNames from 'classnames';
import { Drawer } from '@teambit/base-ui.surfaces.drawer';
import { Containee } from '@teambit/base-ui.surfaces.abs-container';
import { elevationClass } from '@teambit/base-ui.css-components.elevation';
import { roundnessClass } from '@teambit/base-ui.css-components.roundness';
import { backgrounds } from '@teambit/base-ui.surfaces.background';
import { Placeholder } from './placeholder';
import styles from './dropdown.module.scss';
export function Dropdown(_a) {
    var { children, position = 'bottom', elevation = 'low', roundness = 'small', margin = 4, placeholderContent = '', dropClass, placeholderBorder = true, topPlugin, bottomPlugin, clickToggles = false, className } = _a, rest = __rest(_a, ["children", "position", "elevation", "roundness", "margin", "placeholderContent", "dropClass", "placeholderBorder", "topPlugin", "bottomPlugin", "clickToggles", "className"]);
    return (React.createElement(Drawer, Object.assign({}, rest, { margin: margin, className: classNames(!placeholderBorder && styles.removePlaceholderBorder, className), placeholderContent: typeof placeholderContent === 'string' ? React.createElement(Placeholder, null, placeholderContent) : placeholderContent, clickToggles: clickToggles }),
        React.createElement(DropdownMenu, { position: position, elevation: elevation, roundness: roundness, className: dropClass },
            topPlugin,
            children,
            bottomPlugin)));
}
function DropdownMenu(_a) {
    var { className, elevation = 'low', roundness = 'small' } = _a, rest = __rest(_a, ["className", "elevation", "roundness"]);
    return (React.createElement(Containee, Object.assign({}, rest, { className: classNames(styles.dropdownMenu, backgrounds.layer, elevationClass[elevation], roundnessClass[roundness], className) })));
}
//# sourceMappingURL=dropdown.js.map