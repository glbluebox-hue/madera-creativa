import React from 'react';
import { ResponsiveNavbar } from './responsive-navbar';
const style = {
    margin: 16,
};
const contentTabs = [
    {
        component: function Tab() {
            return React.createElement("div", null, "Tab 1");
        },
    },
    {
        component: function Tab() {
            return React.createElement("div", null, "Tab 2");
        },
    },
    {
        component: function Tab() {
            return React.createElement("div", null, "Tab 3");
        },
        style: { marginLeft: 'auto' },
    },
];
export function LineTabs() {
    return React.createElement(ResponsiveNavbar, { tabs: contentTabs, style: style, "data-testid": "responsive-menu" });
}
export function FolderTabs() {
    return React.createElement(ResponsiveNavbar, { tabs: contentTabs, priority: "folder", style: style });
}
export function InnerTabs() {
    return React.createElement(ResponsiveNavbar, { tabs: contentTabs, priority: "inner", style: style });
}
export function TabsWithPropritySetToNone() {
    return React.createElement(ResponsiveNavbar, { tabs: contentTabs, priority: "none", style: style });
}
export function AlwaysShowActiveTab() {
    const [activeIndex, setActiveIndex] = React.useState(11);
    return (React.createElement(ResponsiveNavbar, { tabs: Array.from({ length: 12 }, (_, i) => ({
            component: function Tab() {
                return React.createElement("div", null, `Tab ${i + 1}`);
            },
        })), style: style, alwaysShowActiveTab: true, defaultActiveIndex: activeIndex, onSelect: (index) => {
            setActiveIndex(index);
        } }));
}
export function AlwaysShowActiveTab2() {
    const [activeIndex, setActiveIndex] = React.useState(11);
    return (React.createElement(ResponsiveNavbar, { tabs: Array.from({ length: 12 }, (_, i) => ({
            component: function Tab() {
                return React.createElement("div", null, `Tab ${i + 1}`);
            },
        })), style: style, defaultActiveIndex: activeIndex, onSelect: (index) => {
            setActiveIndex(index);
        } }));
}
//# sourceMappingURL=responsive-navbar.composition.js.map