var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { useState, useEffect, forwardRef } from 'react';
import classNames from 'classnames';
import styles from './tab-folder.module.scss';
export const TabFolder = forwardRef((_a, ref) => {
    var { selectedTab, enableAnimation = false, className } = _a, rest = __rest(_a, ["selectedTab", "enableAnimation", "className"]);
    const [borderStyles, setBorderStyle] = useState({
        width: undefined,
        transform: undefined,
        transition: undefined,
    });
    useEffect(() => {
        if (selectedTab) {
            const styleObj = { width: undefined, transform: undefined, transition: undefined };
            const offset = selectedTab.offsetLeft;
            styleObj.width = `${selectedTab.offsetWidth}px`;
            styleObj.transform = `translateX(${offset}px)`;
            if (enableAnimation)
                styleObj.transition = 'transform 150ms ease-in-out, width 150ms ease-in-out';
            setBorderStyle(styleObj);
        }
    }, [selectedTab, enableAnimation]);
    return (React.createElement("div", Object.assign({ className: classNames(styles.tabFolder, styles.folderStyle, className), style: borderStyles, ref: ref }, rest)));
});
//# sourceMappingURL=tab-folder.js.map