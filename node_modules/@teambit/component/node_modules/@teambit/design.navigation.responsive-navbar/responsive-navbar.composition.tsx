import React from 'react';
import { ResponsiveNavbar } from './responsive-navbar';

const style = {
  margin: 16,
};

const contentTabs = [
  {
    component: function Tab() {
      return <div>Tab 1</div>;
    },
  },
  {
    component: function Tab() {
      return <div>Tab 2</div>;
    },
  },
  {
    component: function Tab() {
      return <div>Tab 3</div>;
    },
    style: { marginLeft: 'auto' },
  },
];

export function LineTabs() {
  return <ResponsiveNavbar tabs={contentTabs} style={style} data-testid="responsive-menu" />;
}

export function FolderTabs() {
  return <ResponsiveNavbar tabs={contentTabs} priority="folder" style={style} />;
}

export function InnerTabs() {
  return <ResponsiveNavbar tabs={contentTabs} priority="inner" style={style} />;
}

export function TabsWithPropritySetToNone() {
  return <ResponsiveNavbar tabs={contentTabs} priority="none" style={style} />;
}

export function AlwaysShowActiveTab() {
  const [activeIndex, setActiveIndex] = React.useState(11);

  return (
    <ResponsiveNavbar
      tabs={Array.from({ length: 12 }, (_, i) => ({
        component: function Tab() {
          return <div>{`Tab ${i + 1}`}</div>;
        },
      }))}
      style={style}
      alwaysShowActiveTab={true}
      defaultActiveIndex={activeIndex}
      onSelect={
        (index) => {
          setActiveIndex(index);
        }
      }
    />
  );
}

export function AlwaysShowActiveTab2() {
  const [activeIndex, setActiveIndex] = React.useState(11);

  return (
    <ResponsiveNavbar
      tabs={Array.from({ length: 12 }, (_, i) => ({
        component: function Tab() {
          return <div>{`Tab ${i + 1}`}</div>;
        },
      }))}
      style={style}
      defaultActiveIndex={activeIndex}
      onSelect={
        (index) => {
          setActiveIndex(index);
        }
      }
    />
  );
}