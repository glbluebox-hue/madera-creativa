import type { Logger } from '@teambit/logger';
import type { ReactEnv } from '@teambit/react';
import type { Application, DeployFn, AppBuildContext, AppContext, ApplicationInstance } from '@teambit/application';
import type { NodeEnv } from './node.env';
import type { DeployContext } from './node-app-options';
export declare class NodeApp implements Application {
    readonly name: string;
    readonly entry: string;
    readonly portRange: number[];
    readonly nodeEnv: NodeEnv & ReactEnv;
    readonly logger: Logger;
    readonly deploy?: DeployFn | undefined;
    constructor(name: string, entry: string, portRange: number[], nodeEnv: NodeEnv & ReactEnv, logger: Logger, deploy?: DeployFn | undefined);
    applicationType: string;
    run(context: AppContext): Promise<ApplicationInstance>;
    build(context: AppBuildContext): Promise<DeployContext>;
}
