"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.NodeApp = void 0;
function _child_process() {
  const data = require("child_process");
  _child_process = function () {
    return data;
  };
  return data;
}
function _path() {
  const data = require("path");
  _path = function () {
    return data;
  };
  return data;
}
function _toolboxNetwork() {
  const data = require("@teambit/toolbox.network.get-port");
  _toolboxNetwork = function () {
    return data;
  };
  return data;
}
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class NodeApp {
  constructor(name, entry, portRange, nodeEnv, logger, deploy) {
    this.name = name;
    this.entry = entry;
    this.portRange = portRange;
    this.nodeEnv = nodeEnv;
    this.logger = logger;
    this.deploy = deploy;
    _defineProperty(this, "applicationType", 'node');
  }
  async run(context) {
    var _child$stdout;
    const logger = this.logger;
    const [from, to] = this.portRange;
    const port = context.port || (await _toolboxNetwork().Port.getPort(from, to));
    const child = (0, _child_process().execFile)('node', [this.entry, port.toString()], error => {
      if (error) {
        // @todo: this is causing uncaughtException in the main process. a better way to handle this would be to use promise.
        // however, since it expects to return a number, it would require a bigger refactor.
        throw error;
      }
    });
    (_child$stdout = child.stdout) === null || _child$stdout === void 0 || _child$stdout.on('data', function (data) {
      logger.console(data.toString());
    });
    this.logger.console(`${context.appName} is listening on http://localhost:${port}`);
    return {
      appName: context.appName,
      port
    };
  }
  async build(context) {
    const {
      base
    } = (0, _path().parse)(this.entry);
    const {
      distDir
    } = this.nodeEnv.getCompiler();
    const mainFile = (0, _path().join)(distDir, base);
    const metadata = {
      mainFile,
      artifactsDir: context.artifactsDir
    };
    const deployContext = _objectSpread(_objectSpread({}, context), {}, {
      // @todo: is this needed?
      mainFile,
      // @todo: remove this when possible. only metadata should be used.
      metadata
    });
    return deployContext;
  }
}
exports.NodeApp = NodeApp;

//# sourceMappingURL=node.application.js.map