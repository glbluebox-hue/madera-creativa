"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.WebpackBitReporterPlugin = void 0;
function _bundler() {
  const data = require("@teambit/bundler");
  _bundler = function () {
    return data;
  };
  return data;
}
function _previewCli() {
  const data = require("@teambit/preview.cli.dev-server-events-listener");
  _previewCli = function () {
    return data;
  };
  return data;
}
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
const PLUGIN_NAME = 'webpack-compiler-started-plugin';

/**
 * Monitors Webpack's compilation, and updates progress to Bit
 */
class WebpackBitReporterPlugin {
  constructor({
    options
  }) {
    // TODO: add plugin type from webpack and implement it
    _defineProperty(this, "pubsub", void 0);
    _defineProperty(this, "devServerID", void 0);
    this.pubsub = options.pubsub;
    this.devServerID = options.devServerID;
  }
  apply(compiler) {
    // "Called before a new compilation is created."
    compiler.hooks.compile.tap(PLUGIN_NAME, () => {
      const event = new (_previewCli().DevServerCompilationStartedEvent)(Date.now(), this.devServerID);
      this.pubsub.pub(_bundler().BundlerAspect.id, event);
    });

    // "Executed when the compilation has completed."
    compiler.hooks.done.tap(PLUGIN_NAME, stats => {
      const results = {
        errors: stats.compilation.errors,
        warnings: stats.compilation.warnings,
        compiling: false
      };
      const event = new (_previewCli().DevServerCompilationDoneEvent)(Date.now(), this.devServerID, results);
      this.pubsub.pub(_bundler().BundlerAspect.id, event);
    });
  }

  // flag to indicate whether the plugin uses the dev server events or the legacy webpack events
}
exports.WebpackBitReporterPlugin = WebpackBitReporterPlugin;
_defineProperty(WebpackBitReporterPlugin, "USE_DEV_SERVER_EVENTS", true);

//# sourceMappingURL=webpack-bit-reporter-plugin.js.map