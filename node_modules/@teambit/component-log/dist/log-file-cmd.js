"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.LogFileCmd = void 0;
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _logCmd() {
  const data = require("./log-cmd");
  _logCmd = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class LogFileCmd {
  constructor(componentLog) {
    this.componentLog = componentLog;
    _defineProperty(this, "name", 'log-file <filepath>');
    _defineProperty(this, "description", 'EXPERIMENTAL. display history of changes to a specific file');
    _defineProperty(this, "extendedDescription", `shows version history for a specific file within component versions.
tracks file-level changes across component snaps and tags.
displays file modifications, hashes, and associated commit information.`);
    _defineProperty(this, "group", 'version-control');
    _defineProperty(this, "alias", '');
    _defineProperty(this, "options", [['o', 'one-line', 'show each log entry in one line']]);
    _defineProperty(this, "arguments", [{
      name: 'filepath',
      description: 'file path relative to the workspace'
    }]);
  }
  async report([filePath], {
    oneLine = false
  }) {
    const results = await this.componentLog.getFileLog(filePath);
    if (oneLine) {
      const table = (0, _logCmd().getEmptyTableWithoutStyle)();
      results.forEach(({
        hash,
        tag,
        username,
        date,
        message,
        fileHash
      }) => {
        table.push([hash, tag || '', username || '', date, message, fileHash]);
      });
      return table.toString();
    }
    const output = results.map(({
      hash,
      tag,
      username,
      email,
      date,
      message,
      fileDiff
    }) => {
      const title = tag ? `tag ${tag} (${hash})\n` : `snap ${hash}\n`;
      const author = (0, _logCmd().paintAuthor)(email, username);
      const body = (fileDiff || _chalk().default.green('<FILE-ADDED>')).split('\n').map(line => `      ${line}`).join('\n');
      return _chalk().default.yellow(title) + author + (date ? _chalk().default.white(`date: ${date}\n`) : '') + (message ? _chalk().default.white(`message: ${message}\n\n`) : '\n') + body;
    });
    return output.join('\n');
  }
}
exports.LogFileCmd = LogFileCmd;

//# sourceMappingURL=log-file-cmd.js.map