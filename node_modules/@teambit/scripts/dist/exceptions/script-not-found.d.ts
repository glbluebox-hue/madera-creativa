import { BitError } from '@teambit/bit-error';
export declare class ScriptNotFound extends BitError {
    readonly scriptName: string;
    readonly envId: string;
    constructor(scriptName: string, envId: string);
}
