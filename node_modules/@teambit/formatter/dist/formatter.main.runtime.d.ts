import type { CLIMain } from '@teambit/cli';
import type { Component, ComponentMain } from '@teambit/component';
import type { EnvsMain, ExecutionContext } from '@teambit/envs';
import type { Logger, LoggerMain } from '@teambit/logger';
import type { Workspace } from '@teambit/workspace';
import { FormatterService } from './formatter.service';
import { FormatTask } from './format.task';
import type { FormatterOptions } from './formatter-context';
import type { Formatter } from './formatter';
import type { SnappingMain } from '@teambit/snapping';
export type FormatterConfig = {
    formatOnPreSnap?: boolean;
};
export declare class FormatterMain {
    private envs;
    private formatterService;
    private logger;
    static runtime: import("@teambit/harmony").RuntimeDefinition;
    constructor(envs: EnvsMain, formatterService: FormatterService, logger: Logger);
    /**
     * format an array of components.
     */
    format(components: Component[], opts: FormatterOptions): Promise<import("@teambit/envs").EnvsExecutionResult<import("./formatter").FormatResults>>;
    /**
     * check format an array of components.
     */
    check(components: Component[], opts: FormatterOptions): Promise<import("@teambit/envs").EnvsExecutionResult<import("./formatter").FormatResults>>;
    getFormatter(context: ExecutionContext, options: FormatterOptions): Formatter | undefined;
    private toFormatServiceOptions;
    /**
     * create a format task for build pipelines.
     * @param name name of the task.
     */
    createTask(name?: string): FormatTask;
    static dependencies: import("@teambit/harmony").Aspect[];
    static defaultConfig: FormatterConfig;
    static provider([envs, cli, component, loggerAspect, workspace, snapping]: [
        EnvsMain,
        CLIMain,
        ComponentMain,
        LoggerMain,
        Workspace,
        SnappingMain
    ], config: FormatterConfig): Promise<FormatterMain>;
}
