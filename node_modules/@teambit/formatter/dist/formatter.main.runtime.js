"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.FormatterMain = void 0;
function _cli() {
  const data = require("@teambit/cli");
  _cli = function () {
    return data;
  };
  return data;
}
function _component() {
  const data = require("@teambit/component");
  _component = function () {
    return data;
  };
  return data;
}
function _envs() {
  const data = require("@teambit/envs");
  _envs = function () {
    return data;
  };
  return data;
}
function _logger() {
  const data = require("@teambit/logger");
  _logger = function () {
    return data;
  };
  return data;
}
function _workspace() {
  const data = require("@teambit/workspace");
  _workspace = function () {
    return data;
  };
  return data;
}
function _formatter() {
  const data = require("./formatter.aspect");
  _formatter = function () {
    return data;
  };
  return data;
}
function _formatter2() {
  const data = require("./formatter.service");
  _formatter2 = function () {
    return data;
  };
  return data;
}
function _format() {
  const data = require("./format.task");
  _format = function () {
    return data;
  };
  return data;
}
function _format2() {
  const data = require("./format.cmd");
  _format2 = function () {
    return data;
  };
  return data;
}
function _snapping() {
  const data = require("@teambit/snapping");
  _snapping = function () {
    return data;
  };
  return data;
}
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class FormatterMain {
  constructor(envs, formatterService, logger) {
    this.envs = envs;
    this.formatterService = formatterService;
    this.logger = logger;
  }

  /**
   * format an array of components.
   */
  async format(components, opts) {
    const envsRuntime = await this.envs.createEnvironment(components);
    const formatResults = envsRuntime.run(this.formatterService, this.toFormatServiceOptions(opts, false));
    return formatResults;
  }

  /**
   * check format an array of components.
   */
  async check(components, opts) {
    const envsRuntime = await this.envs.createEnvironment(components);
    const formatResults = envsRuntime.run(this.formatterService, this.toFormatServiceOptions(opts, true));
    return formatResults;
  }
  getFormatter(context, options) {
    return this.formatterService.getFormatter(context, options);
  }
  toFormatServiceOptions(opts, check = false) {
    return _objectSpread(_objectSpread({}, opts), {}, {
      check
    });
  }

  /**
   * create a format task for build pipelines.
   * @param name name of the task.
   */
  createTask(name) {
    return new (_format().FormatTask)(_formatter().FormatterAspect.id, name);
  }
  static async provider([envs, cli, component, loggerAspect, workspace, snapping], config) {
    const logger = loggerAspect.createLogger(_formatter().FormatterAspect.id);
    const formatterService = new (_formatter2().FormatterService)(config);
    const formatterMain = new FormatterMain(envs, formatterService, logger);
    envs.registerService(formatterService);
    cli.register(new (_format2().FormatCmd)(formatterMain, component.getHost(), workspace));
    snapping.registerOnPreSnap(async components => {
      if (!config.formatOnPreSnap) {
        return;
      }
      await formatterMain.format(components, {
        check: false
      });
    });
    return formatterMain;
  }
}
exports.FormatterMain = FormatterMain;
_defineProperty(FormatterMain, "runtime", _cli().MainRuntime);
_defineProperty(FormatterMain, "dependencies", [_envs().EnvsAspect, _cli().CLIAspect, _component().ComponentAspect, _logger().LoggerAspect, _workspace().WorkspaceAspect, _snapping().SnappingAspect]);
_defineProperty(FormatterMain, "defaultConfig", {});
_formatter().FormatterAspect.addRuntime(FormatterMain);

//# sourceMappingURL=formatter.main.runtime.js.map