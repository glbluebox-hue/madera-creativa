import type { Command, CommandOptions } from '@teambit/cli';
import type { Logger } from '@teambit/logger';
import type { Workspace } from '@teambit/workspace';
import type { BuilderMain } from './builder.main.runtime';
type BuildOpts = {
    unmodified?: boolean;
    dev: boolean;
    rebuild: boolean;
    install: boolean;
    cachePackagesOnCapsulesRoot: boolean;
    reuseCapsules: boolean;
    rewrite?: boolean;
    reinstall?: boolean;
    tasks: string;
    listTasks?: string;
    skipTests?: boolean;
    skipTasks?: string;
    failFast?: boolean;
    includeSnap?: boolean;
    includeTag?: boolean;
    ignoreIssues?: string;
    loose?: boolean;
};
export declare class BuilderCmd implements Command {
    private builder;
    private workspace;
    private logger;
    name: string;
    description: string;
    extendedDescription: string;
    arguments: {
        name: string;
        description: string;
    }[];
    examples: {
        cmd: string;
        description: string;
    }[];
    helpUrl: string;
    alias: string;
    group: string;
    options: CommandOptions;
    constructor(builder: BuilderMain, workspace: Workspace, logger: Logger);
    report([pattern]: [string], { unmodified, dev, install, cachePackagesOnCapsulesRoot, reuseCapsules, rewrite, reinstall, tasks, listTasks, skipTests, skipTasks, failFast, includeSnap, includeTag, ignoreIssues, loose, }: BuildOpts): Promise<string>;
    private getListTasks;
}
export {};
