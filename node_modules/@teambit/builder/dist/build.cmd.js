"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.BuilderCmd = void 0;
function _cli() {
  const data = require("@teambit/cli");
  _cli = function () {
    return data;
  };
  return data;
}
function _prettyTime() {
  const data = _interopRequireDefault(require("pretty-time"));
  _prettyTime = function () {
    return data;
  };
  return data;
}
function _workspace() {
  const data = require("@teambit/workspace");
  _workspace = function () {
    return data;
  };
  return data;
}
function _legacy() {
  const data = require("@teambit/legacy.constants");
  _legacy = function () {
    return data;
  };
  return data;
}
function _componentIssues() {
  const data = require("@teambit/component-issues");
  _componentIssues = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class BuilderCmd {
  constructor(builder, workspace, logger) {
    this.builder = builder;
    this.workspace = workspace;
    this.logger = logger;
    _defineProperty(this, "name", 'build [component-pattern]');
    _defineProperty(this, "description", 'run build pipeline tasks in isolated environments');
    _defineProperty(this, "extendedDescription", `executes the complete build pipeline including compilation, testing, linting, and other tasks defined by component environments.
the build takes place in isolated directories called "capsules" where component files are copied and dependencies are installed via the package manager.
by default processes only new and modified components - use --unmodified to build all components.
because this process can take a while on large workspaces, various flags are available to optimize the process - see examples for debugging workflows.`);
    _defineProperty(this, "arguments", [{
      name: 'component-pattern',
      description: _legacy().COMPONENT_PATTERN_HELP
    }]);
    _defineProperty(this, "examples", [{
      cmd: 'build --reuse-capsules --tasks "custom-task"',
      description: 'helps to debug this "custom-task" without recreating the capsules from scratch'
    }, {
      cmd: 'build --reuse-capsules --rewrite --tasks "BabelCompile,MochaTest"',
      description: `helpful when for example the tests are failing and code changes are needed to debug it.
the "--rewrite" flag ensures the component files are fresh, and the "--tasks" ensures to re-compile them and then run the tests`
    }]);
    _defineProperty(this, "helpUrl", 'reference/build-pipeline/builder-overview');
    _defineProperty(this, "alias", '');
    _defineProperty(this, "group", 'component-development');
    _defineProperty(this, "options", [['u', 'unmodified', 'include unmodified components (by default, only new and modified components are built)'], ['d', 'dev', 'run the pipeline in dev mode'], ['', 'install', 'install core aspects in capsules'], ['', 'reuse-capsules', 'avoid deleting the capsules root-dir before starting the build'], ['', 'rewrite', 'use only with --reuse-capsules. rewrite the component files'], ['', 'reinstall', 'use only with --reuse-capsules. rerun the installation'], ['', 'tasks <string>', `build the specified task(s) only. for multiple tasks, separate by a comma and wrap with quotes.
specify the task-name (e.g. "TypescriptCompiler") or the task-aspect-id (e.g. teambit.compilation/compiler)`], ['', 'cache-packages-on-capsule-root', 'set the package-manager cache on the capsule root'], ['', 'list-tasks <string>', 'list tasks of an env or a component-id for each one of the pipelines: build, tag and snap'], ['', 'skip-tests', 'skip running component tests during build process'], ['', 'loose', 'allow build to succeed even if tasks like tests or lint fail'], ['', 'skip-tasks <string>', `skip the given tasks. for multiple tasks, separate by a comma and wrap with quotes.
  specify the task-name (e.g. "TypescriptCompiler") or the task-aspect-id (e.g. teambit.compilation/compiler)`], ['', 'fail-fast', 'stop pipeline execution on the first failed task (by default a task is skipped only when its dependency failed)'], ['', 'include-snap', 'include snap pipeline tasks. Warning: this may deploy/publish if you have such tasks'], ['', 'include-tag', 'include tag pipeline tasks. Warning: this may deploy/publish if you have such tasks'], ['i', 'ignore-issues <issues>', `ignore component issues (shown in "bit status" as "issues found"), issues to ignore:
[${Object.keys(_componentIssues().IssuesClasses).join(', ')}]
to ignore multiple issues, separate them by a comma and wrap with quotes. to ignore all issues, specify "*".`]]);
  }
  async report([pattern], {
    unmodified = false,
    dev = false,
    install = false,
    cachePackagesOnCapsulesRoot = false,
    reuseCapsules = false,
    rewrite = false,
    reinstall = false,
    tasks,
    listTasks,
    skipTests,
    skipTasks,
    failFast,
    includeSnap,
    includeTag,
    ignoreIssues,
    loose = false
  }) {
    if (rewrite && !reuseCapsules) throw new Error('cannot use --rewrite without --reuse-capsules');
    if (reinstall && !reuseCapsules) throw new Error('cannot use --reinstall without --reuse-capsules');
    if (!this.workspace) throw new (_workspace().OutsideWorkspaceError)();
    if (listTasks) {
      return this.getListTasks(listTasks);
    }
    this.logger.setStatusLine('build');
    const start = process.hrtime();
    // If pattern is provided, don't pass the unmodified flag as "all" - the pattern should take precedence
    const components = await this.workspace.getComponentsByUserInput(pattern ? false : unmodified, pattern, true);
    if (!components.length) {
      return (0, _cli().formatHint)(`no components found to build. use "--unmodified" flag to build all components or specify the ids to build, otherwise, only new and modified components will be built`);
    }
    const skipTasksParsed = skipTasks ? skipTasks.split(',').map(t => t.trim()) : undefined;
    const envsExecutionResults = await this.builder.build(components, {
      installOptions: {
        installTeambitBit: install,
        packageManagerConfigRootDir: this.workspace.path,
        installPackages: rewrite && !reinstall ? false : true
      },
      linkingOptions: {
        linkTeambitBit: !install
      },
      emptyRootDir: !reuseCapsules,
      getExistingAsIs: reuseCapsules && !rewrite && !reinstall,
      cachePackagesOnCapsulesRoot
    }, {
      dev,
      tasks: tasks ? tasks.split(',').map(task => task.trim()) : [],
      skipTests,
      skipTasks: skipTasksParsed,
      exitOnFirstFailedTask: failFast
    }, {
      includeSnap,
      includeTag,
      ignoreIssues
    });
    this.logger.console(`build output can be found in path: ${envsExecutionResults.capsuleRootDir}`);
    const duration = (0, _prettyTime().default)(process.hrtime(start));
    const succeedOrFailed = envsExecutionResults.hasErrors(loose) ? 'failed' : 'succeeded';
    const msg = `build ${succeedOrFailed}. completed in ${duration}.`;
    if (envsExecutionResults.hasErrors(loose)) {
      this.logger.consoleFailure(msg);
    }
    envsExecutionResults.throwErrorsIfExist(loose);
    return (0, _cli().formatSuccessSummary)(msg);
  }
  async getListTasks(componentIdStr) {
    const compId = await this.workspace.resolveComponentId(componentIdStr);
    const component = await this.workspace.get(compId);
    const results = this.builder.listTasks(component);
    const buildItems = results.buildTasks.map(t => (0, _cli().formatItem)(t));
    const tagItems = results.tagTasks.map(t => (0, _cli().formatItem)(t));
    const snapItems = results.snapTasks.length ? results.snapTasks.map(t => (0, _cli().formatItem)(t)) : [(0, _cli().formatItem)('<N/A>')];
    return (0, _cli().joinSections)([`${(0, _cli().formatTitle)('Task List')}\nid:    ${results.id.toString()}\nenvId: ${results.envId}`, `${(0, _cli().formatTitle)('Build Pipeline Tasks')}\n${buildItems.join('\n')}`, `${(0, _cli().formatTitle)('Tag Pipeline Tasks')}\n${tagItems.join('\n')}`, `${(0, _cli().formatTitle)('Snap Pipeline Tasks')}\n${snapItems.join('\n')}`]);
  }
}
exports.BuilderCmd = BuilderCmd;

//# sourceMappingURL=build.cmd.js.map