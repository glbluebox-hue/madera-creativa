"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ListCmd = void 0;
function _lodash() {
  const data = require("lodash");
  _lodash = function () {
    return data;
  };
  return data;
}
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _legacy() {
  const data = require("@teambit/legacy.utils");
  _legacy = function () {
    return data;
  };
  return data;
}
function _listTemplate() {
  const data = require("./list-template");
  _listTemplate = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class ListCmd {
  constructor(lister) {
    this.lister = lister;
    _defineProperty(this, "name", 'list [remote-scope]');
    _defineProperty(this, "description", 'display components in workspace or remote scope');
    _defineProperty(this, "extendedDescription", `shows components in the current workspace by default, or from a specified remote scope.
supports filtering by scope, namespace, and various display options.
use --outdated to highlight components that have newer versions available.`);
    _defineProperty(this, "group", 'info-analysis');
    _defineProperty(this, "helpUrl", 'reference/reference/cli-reference#list');
    _defineProperty(this, "alias", 'ls');
    _defineProperty(this, "options", [['i', 'ids', 'show only component ids, unformatted'], ['l', 'local-scope', 'show only components stored in the local scope, including indirect dependencies'], ['s', 'scope <string>', 'filter components by their scope name (e.g., teambit.workspace)'], ['o', 'outdated', 'highlight outdated components, in comparison with their latest remote version (if one exists)'], ['d', 'include-deleted', 'EXPERIMENTAL. show also deleted components'], ['j', 'json', 'show the output in JSON format'], ['n', 'namespace <string>', "filter components by their namespace (a logical grouping within a scope, e.g., 'ui', '*/ui')"]]);
    _defineProperty(this, "loader", true);
    _defineProperty(this, "skipWorkspace", true);
    _defineProperty(this, "remoteOp", true);
  }
  async report([scopeName], listFlags) {
    if (scopeName && (listFlags.localScope || listFlags.scope)) {
      throw new Error('The --local-scope and --scope flags cannot be used when listing a remote scope.');
    }
    const listScopeResults = await this.getListResults(scopeName, listFlags);
    const {
      ids,
      outdated = false
    } = listFlags;
    function decideHeaderSentence() {
      if (!scopeName) return `found ${listScopeResults.length} components\n`;
      return _chalk().default.white(`found ${listScopeResults.length} components in ${_chalk().default.bold(scopeName)}\n`);
    }
    if ((0, _lodash().isEmpty)(listScopeResults)) {
      return _chalk().default.white(decideHeaderSentence());
    }
    if (ids) return JSON.stringify(listScopeResults.map(result => result.id.toString()));
    // TODO - use a cheaper list for ids flag (do not fetch versions at all) @!IMPORTANT
    return decideHeaderSentence() + (0, _listTemplate().listTemplate)(listScopeResults, false, outdated);
  }
  async json([scopeName], listFlags) {
    if (scopeName && (listFlags.localScope || listFlags.scope)) {
      throw new Error('The --local-scope and --scope flags cannot be used when listing a remote scope.');
    }
    const listScopeResults = await this.getListResults(scopeName, listFlags);
    if ((0, _lodash().isEmpty)(listScopeResults)) {
      return [];
    }
    const {
      ids,
      outdated = false
    } = listFlags;
    if (ids) return listScopeResults.map(result => result.id.toString());
    return (0, _listTemplate().listTemplate)(listScopeResults, true, outdated);
  }
  async getListResults(scopeName, {
    namespace,
    localScope,
    scope,
    outdated,
    includeDeleted
  } = {}) {
    const getNamespaceWithWildcard = () => {
      if (!namespace) return undefined;
      if ((0, _legacy().hasWildcard)(namespace)) return namespace;
      return `${namespace}/**`;
    };
    const namespacesUsingWildcards = getNamespaceWithWildcard();
    return scopeName ? this.lister.remoteList(scopeName, {
      namespacesUsingWildcards,
      includeDeleted
    }) : this.lister.localList(localScope, outdated, namespacesUsingWildcards, scope);
  }
}
exports.ListCmd = ListCmd;

//# sourceMappingURL=list.cmd.js.map