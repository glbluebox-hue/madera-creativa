"use strict";

function _chai() {
  const data = require("chai");
  _chai = function () {
    return data;
  };
  return data;
}
function _validateEnvPolicy() {
  const data = require("./validate-env-policy");
  _validateEnvPolicy = function () {
    return data;
  };
  return data;
}
describe('validateEnvPolicyConfigObject', () => {
  it('should throw an exception if peer supportedRange is empty', () => {
    (0, _chai().expect)(() => (0, _validateEnvPolicy().validateEnvPolicyConfigObject)({
      peers: [{
        name: 'peer',
        supportedRange: ''
      }]
    })).to.throw('Peer "peer" has an empty supportedRange');
  });
  it('should throw an exception if peer supportedRange is null', () => {
    (0, _chai().expect)(() => (0, _validateEnvPolicy().validateEnvPolicyConfigObject)({
      peers: [{
        name: 'peer',
        supportedRange: null
      }]
    })).to.throw('Peer "peer" has no supportedRange set');
  });
  it('should throw an exception if peer version is empty', () => {
    (0, _chai().expect)(() => (0, _validateEnvPolicy().validateEnvPolicyConfigObject)({
      peers: [{
        name: 'peer',
        supportedRange: '1',
        version: ''
      }]
    })).to.throw('Peer "peer" has an empty version');
  });
  it('should throw an exception if peer version is null', () => {
    (0, _chai().expect)(() => (0, _validateEnvPolicy().validateEnvPolicyConfigObject)({
      peers: [{
        name: 'peer',
        supportedRange: '1',
        version: null
      }]
    })).to.throw('Peer "peer" has no version set');
  });
});

//# sourceMappingURL=validate-env-policy.spec.js.map