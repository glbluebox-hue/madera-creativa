"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.EnvPolicy = void 0;
function _validateEnvPolicy() {
  const data = require("./validate-env-policy");
  _validateEnvPolicy = function () {
    return data;
  };
  return data;
}
function _variantPolicy() {
  const data = require("../variant-policy");
  _variantPolicy = function () {
    return data;
  };
  return data;
}
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * Config that is used before the new env.jsonc format was introduced.
 */

class EnvPolicy extends _variantPolicy().VariantPolicy {
  constructor(_policiesEntries, selfPolicy, envId) {
    super(_policiesEntries);
    this.selfPolicy = selfPolicy;
    this.envId = envId;
  }
  static fromConfigObject(configObject, {
    includeLegacyPeersInSelfPolicy
  } = {}, envId) {
    (0, _validateEnvPolicy().validateEnvPolicyConfigObject)(configObject);

    /**
     * Calculate the policy for the env itself.
     * Always force it for the env itself
     */
    let selfPeersEntries;
    // @ts-ignore TODO: need to fix this, the | confusing the compiler
    if (includeLegacyPeersInSelfPolicy && !configObject.peers && configObject.peerDependencies) {
      // @ts-ignore TODO: need to fix this, the | confusing the compiler
      selfPeersEntries = handleLegacyPeers(configObject);
    } else {
      // @ts-ignore TODO: need to fix this, the | confusing the compiler
      selfPeersEntries = entriesFromKey(configObject, 'peers', 'version', 'runtime', {
        source: 'env-own',
        force: true
      });
    }
    const selfPolicy = _variantPolicy().VariantPolicy.fromArray(selfPeersEntries);

    /**
     * Legacy policy used by the old getDependencies function on the env aspect.
     * when we used to configure dependencies, devDependencies, peerDependencies as objects of dependencyId: version
     * Those were always forced on the components as visible dependencies.
     */
    const legacyPolicy = _variantPolicy().VariantPolicy.fromConfigObject(configObject, {
      source: 'env',
      force: true,
      hidden: false
    });
    // @ts-ignore TODO: need to fix this, the | confusing the compiler
    const componentPeersEntries = entriesFromKey(configObject, 'peers', 'supportedRange', 'peer', {
      source: 'env'
    });
    const otherKeyNames = ['dev', 'runtime'];
    const otherEntries = otherKeyNames.reduce((acc, keyName) => {
      // @ts-ignore TODO: need to fix this, the | confusing the compiler
      const currEntries = entriesFromKey(configObject, keyName, 'version', keyName, {
        source: 'env'
      });
      return acc.concat(currEntries);
    }, []);
    const newPolicy = _variantPolicy().VariantPolicy.fromArray(componentPeersEntries.concat(otherEntries));
    const finalComponentPolicy = _variantPolicy().VariantPolicy.mergePolices([legacyPolicy, newPolicy]);
    return new EnvPolicy(finalComponentPolicy.entries, selfPolicy, envId);
  }
  static getEmpty() {
    return new EnvPolicy([], _variantPolicy().VariantPolicy.getEmpty());
  }
}
exports.EnvPolicy = EnvPolicy;
function handleLegacyPeers(configObject) {
  if (!configObject.peerDependencies) {
    return [];
  }
  const entries = Object.entries(configObject.peerDependencies).map(([packageName, version]) => {
    return (0, _variantPolicy().createVariantPolicyEntry)(packageName, version, 'runtime', {
      source: 'env-own',
      hidden: false,
      force: true
    });
  });
  return entries;
}
function entriesFromKey(configObject, keyName, versionKey = 'version', lifecycleType, options) {
  const configEntries = configObject[keyName];
  if (!configEntries) {
    return [];
  }
  const entries = configEntries.map(entry => {
    if (!entry[versionKey]) {
      throw new Error(`env.jsonc: "policy.${keyName}" entry must be a property with a "${versionKey}" field. got "${entry}"`);
    }
    const peerEntry = entry;
    const hasPeerProps = 'workspaceSingleton' in entry || 'override' in entry;
    const value = hasPeerProps ? {
      version: entry[versionKey],
      workspaceSingleton: peerEntry.workspaceSingleton,
      override: peerEntry.override
    } : entry[versionKey];
    return (0, _variantPolicy().createVariantPolicyEntry)(entry.name, value, lifecycleType, _objectSpread(_objectSpread({}, options), {}, {
      source: options.source ?? 'env',
      hidden: entry.hidden,
      // allow override the entry's force value (used for the env itself)
      force: options.force ?? !!entry.force,
      optional: options.optional ?? !!entry.optional
    }));
  });
  return entries;
}

//# sourceMappingURL=env-policy.js.map