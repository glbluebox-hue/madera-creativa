import type { MergeStrategy } from '@teambit/component.modules.merge-helper';
import type { Command, CommandOptions } from '@teambit/cli';
import type { LanesMain } from './lanes.main.runtime';
export declare class SwitchCmd implements Command {
    private lanes;
    name: string;
    description: string;
    extendedDescription: string;
    private: boolean;
    group: string;
    alias: string;
    arguments: {
        name: string;
        description: string;
    }[];
    options: CommandOptions;
    loader: boolean;
    constructor(lanes: LanesMain);
    report([lane]: [string], { head, skipFetch, alias, autoMergeResolve, forceOurs, forceTheirs, getAll, workspaceOnly, skipDependencyInstallation, pattern, verbose, json, branch, }: {
        head?: boolean;
        skipFetch?: boolean;
        alias?: string;
        autoMergeResolve?: MergeStrategy;
        forceOurs?: boolean;
        forceTheirs?: boolean;
        getAll?: boolean;
        workspaceOnly?: boolean;
        skipDependencyInstallation?: boolean;
        override?: boolean;
        pattern?: string;
        verbose?: boolean;
        json?: boolean;
        branch?: boolean;
    }): Promise<string | {
        data: string;
        code: number;
        details: string;
    }>;
}
