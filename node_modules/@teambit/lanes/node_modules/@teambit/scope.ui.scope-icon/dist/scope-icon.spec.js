import { jsx as _jsx } from "react/jsx-runtime";
import { render } from '@testing-library/react';
import { DefaultTextExample, TextWithBackgroundColorExample, } from './scope-icon.composition';
it('should render correctly', () => {
    const { getByTestId } = render(_jsx(DefaultTextExample, { "data-testid": "test-icon" }));
    const rendered = getByTestId('test-icon');
    expect(rendered).toBeTruthy();
});
it('should render with inline back-ground style in main div element', () => {
    const { container } = render(_jsx(TextWithBackgroundColorExample, {}));
    const div = container.querySelector('div');
    expect(div).toHaveStyle('background-color: #8f24a9');
});
//# sourceMappingURL=scope-icon.spec.js.map