"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ESLintMain = void 0;
exports.runTransformersWithContext = runTransformersWithContext;
function _cli() {
  const data = require("@teambit/cli");
  _cli = function () {
    return data;
  };
  return data;
}
function _logger() {
  const data = require("@teambit/logger");
  _logger = function () {
    return data;
  };
  return data;
}
function _defenderEslint() {
  const data = require("@teambit/defender.eslint.config-mutator");
  _defenderEslint = function () {
    return data;
  };
  return data;
}
function _legacy() {
  const data = require("@teambit/legacy.constants");
  _legacy = function () {
    return data;
  };
  return data;
}
function _eslint() {
  const data = require("./eslint.aspect");
  _eslint = function () {
    return data;
  };
  return data;
}
function _eslint2() {
  const data = require("./eslint.linter");
  _eslint2 = function () {
    return data;
  };
  return data;
}
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); } // import { Linter as ESLinter, ESLint as ESLintLib } from 'eslint';
class ESLintMain {
  constructor(logger) {
    this.logger = logger;
  }

  /**
   * @deprecated use eslint linter from https://bit.cloud/teambit/defender/eslint-linter
   * create a eslint linter instance.
   * @param options eslint options.
   * @param ESLintModule reference to an `eslint` module.
   */
  createLinter(context, options, transformers = [], ESLintModule) {
    this.logger.consoleWarning(`The 'Eslint' aspect is deprecated. Please use the 'Eslint linter' component instead. For more details, visit: https://${(0, _legacy().getCloudDomain)()}/teambit/defender/eslint-linter`);
    const mergedOptions = getOptions(options, context);
    const configMutator = new (_defenderEslint().EslintConfigMutator)(mergedOptions);
    const transformerContext = {
      fix: !!context.fix
    };
    const afterMutation = runTransformersWithContext(configMutator.clone(), transformers, transformerContext);

    // @ts-ignore
    return new (_eslint2().ESLintLinter)(this.logger, afterMutation.raw, ESLintModule);
  }
  static async provider([loggerExt]) {
    const logger = loggerExt.createLogger(_eslint().ESLintAspect.id);
    return new ESLintMain(logger);
  }
}
exports.ESLintMain = ESLintMain;
_defineProperty(ESLintMain, "runtime", _cli().MainRuntime);
_defineProperty(ESLintMain, "dependencies", [_logger().LoggerAspect]);
_eslint().ESLintAspect.addRuntime(ESLintMain);

/**
 * get options for eslint.
 */
function getOptions(options, context) {
  const mergedConfig = {
    // @ts-ignore - this is a bug in the @types/eslint types
    overrideConfig: options.config,
    extensions: context.extensionFormats,
    useEslintrc: false,
    // TODO: this should be probably be replaced with resolve-plugins-relative-to
    // https://eslint.org/docs/latest/use/command-line-interface#--resolve-plugins-relative-to
    cwd: options.pluginPath,
    fix: !!context.fix,
    fixTypes: context.fixTypes
  };
  return Object.assign({}, options, {
    config: mergedConfig,
    extensions: context.extensionFormats
  });
}
function runTransformersWithContext(config, transformers = [], context) {
  if (!Array.isArray(transformers)) return config;
  const newConfig = transformers.reduce((acc, transformer) => {
    return transformer(acc, context);
  }, config);
  return newConfig;
}

//# sourceMappingURL=eslint.main.runtime.js.map