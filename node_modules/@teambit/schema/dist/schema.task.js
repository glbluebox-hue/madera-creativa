"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SchemaTask = exports.SCHEMA_TASK_NAME = exports.SCHEMA_ARTIFACT_NAME = void 0;
exports.getSchemaArtifactDef = getSchemaArtifactDef;
exports.getSchemaArtifactPath = getSchemaArtifactPath;
function _builder() {
  const data = require("@teambit/builder");
  _builder = function () {
    return data;
  };
  return data;
}
function _fsExtra() {
  const data = _interopRequireDefault(require("fs-extra"));
  _fsExtra = function () {
    return data;
  };
  return data;
}
function _pMapSeries() {
  const data = _interopRequireDefault(require("p-map-series"));
  _pMapSeries = function () {
    return data;
  };
  return data;
}
function _path() {
  const data = require("path");
  _path = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
const SCHEMA_TASK_NAME = exports.SCHEMA_TASK_NAME = 'ExtractSchema';
const SCHEMA_ARTIFACT_NAME = exports.SCHEMA_ARTIFACT_NAME = 'schema';

/**
 * extract and persist the component schema as a json file
 */
class SchemaTask {
  constructor(aspectId, schema, logger) {
    this.aspectId = aspectId;
    this.schema = schema;
    this.logger = logger;
    _defineProperty(this, "name", SCHEMA_TASK_NAME);
    _defineProperty(this, "location", 'end');
    _defineProperty(this, "description", 'extract api schema for a set of components');
  }
  async execute(context) {
    const startTime = Date.now();
    const capsules = context.capsuleNetwork.seedersCapsules;
    const schemaResult = [];
    const rootDir = context.capsuleNetwork.capsulesRootDir;
    await (0, _pMapSeries().default)(capsules, async capsule => {
      const component = capsule.component;
      const isTaskDisabled = this.schema.isSchemaTaskDisabled(component);
      if (isTaskDisabled) return;
      try {
        const schema = await this.schema.getSchema(component, false, true, rootDir, capsule.path);
        const schemaObj = schema.toObject();
        await _fsExtra().default.outputFile((0, _path().join)(capsule.path, getSchemaArtifactPath()), JSON.stringify(schemaObj, null, 2));
        schemaResult.push({
          component,
          startTime,
          endTime: Date.now()
        });
      } catch (e) {
        this.logger.warn(`failed extracting schema for ${component.id.toString()}`);
        /**
         * @todo once schema extractor is more stable change this to an error
         */
        if (e instanceof Error) {
          schemaResult.push({
            component,
            startTime,
            endTime: Date.now(),
            warnings: [e.message]
          });
        }
      }
    });
    return {
      artifacts: [getSchemaArtifactDef()],
      componentsResults: schemaResult
    };
  }
}
exports.SchemaTask = SchemaTask;
function getSchemaArtifactPath() {
  return (0, _path().join)(_builder().CAPSULE_ARTIFACTS_DIR, 'schema.json');
}
function getSchemaArtifactDef() {
  const def = {
    name: SCHEMA_ARTIFACT_NAME,
    globPatterns: [getSchemaArtifactPath()]
  };
  return def;
}

//# sourceMappingURL=schema.task.js.map