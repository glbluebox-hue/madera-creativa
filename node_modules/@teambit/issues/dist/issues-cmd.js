"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ComponentIssuesCmd = void 0;
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _issues() {
  const data = require("./issues.aspect");
  _issues = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class ComponentIssuesCmd {
  constructor(issues) {
    this.issues = issues;
    _defineProperty(this, "name", 'component-issues');
    _defineProperty(this, "description", 'list available component-issues');
    _defineProperty(this, "alias", '');
    _defineProperty(this, "group", 'info-analysis');
    _defineProperty(this, "options", [['j', 'json', 'output issues in json format']]);
    _defineProperty(this, "loader", true);
    _defineProperty(this, "private", true);
  }
  async report() {
    const results = await this.json();
    const issuesList = results.map(result => `${_chalk().default.bold(result.name)}
${result.description}
Possible solution: ${result.solution}
Is Tag/Snap blocker: ${result.isTagBlocker ? 'yes' : 'no'}
`).join('\n');
    const bypassInstructions = `
${_chalk().default.yellow('━'.repeat(80))}
${_chalk().default.yellow.bold('IGNORING ISSUES (Emergency Use Only)')}
${_chalk().default.yellow('━'.repeat(80))}

${_chalk().default.bold('While strongly discouraged')}, it is possible to ignore issues in two ways:

${_chalk().default.bold('1. Temporarily ignore for a single command:')}
   Use the --ignore-issues flag with tag/snap/build commands:

   ${_chalk().default.cyan('bit tag --ignore-issues "IssueName1, IssueName2"')}
   ${_chalk().default.cyan('bit snap --ignore-issues "IssueName1"')}
   ${_chalk().default.cyan('bit build --ignore-issues "*"')}  ${_chalk().default.dim('(ignores all issues)')}

${_chalk().default.bold('2. Permanently ignore in workspace configuration:')}
   Add to your workspace.jsonc:

   ${_chalk().default.cyan(`"${_issues().IssuesAspect.id}": {
     "ignoreIssues": ["IssueName1", "IssueName2"]
   }`)}

${_chalk().default.yellow('⚠')}  ${_chalk().default.bold('Warning:')} Ignoring issues may lead to broken components, failed builds,
   or other problems. Only use this in emergency situations where you understand
   the consequences and have a plan to fix the underlying issues.
`;
    return `${issuesList}\n${bypassInstructions}`;
  }
  async json() {
    return this.issues.listIssues();
  }
}
exports.ComponentIssuesCmd = ComponentIssuesCmd;

//# sourceMappingURL=issues-cmd.js.map