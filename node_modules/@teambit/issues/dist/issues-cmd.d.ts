import type { Command, CommandOptions } from '@teambit/cli';
import type { IssuesMain } from './issues.main.runtime';
export declare class ComponentIssuesCmd implements Command {
    private issues;
    name: string;
    description: string;
    alias: string;
    group: string;
    options: CommandOptions;
    loader: boolean;
    private: boolean;
    constructor(issues: IssuesMain);
    report(): Promise<string>;
    json(): Promise<{
        name: string;
        description: string;
        solution: string;
        isTagBlocker: boolean;
    }[]>;
}
