"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.MergeMoveLaneCmd = void 0;
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _cli() {
  const data = require("@teambit/cli");
  _cli = function () {
    return data;
  };
  return data;
}
function _bitError() {
  const data = require("@teambit/bit-error");
  _bitError = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class MergeMoveLaneCmd {
  constructor(mergeLanes) {
    this.mergeLanes = mergeLanes;
    _defineProperty(this, "name", 'merge-move <new-lane-name>');
    _defineProperty(this, "description", `EXPERIMENT. move the current merge state into a new lane. the current lane will be reset`);
    _defineProperty(this, "extendedDescription", `this command is useful when you got a messy merge state that on one hand you don't want
to lose the changes, but on the other hand, you want to keep your lane without those changes.
this command does the following:
1. create a new lane with the current merge state. including all the filesystem changes. (in practice, it leaves the fs intact)
2. reset the current lane to the state before the merge. so then once done with the new lane, you can switch to the current lane and it'll be clean.`);
    _defineProperty(this, "alias", '');
    _defineProperty(this, "options", [['s', 'scope <scope-name>', 'remote scope to which this lane will be exported, default to the workspace.json\'s defaultScope (can be changed up to first export of the lane with "bit lane change-scope")']]);
    _defineProperty(this, "loader", true);
    _defineProperty(this, "remoteOp", true);
  }
  async report([newLaneName], {
    scope
  }) {
    const currentLane = await this.mergeLanes.lanes.getCurrentLane();
    if (!currentLane) {
      throw new (_bitError().BitError)(`this command makes sense only when checked out to a lane. otherwise, there is no lane to revert to.
in order to move all local merge changes to a new lane, you can simply create a new lane (bit lane create)`);
    }
    const result = await this.mergeLanes.mergeMove(newLaneName, {
      scope
    });
    const remoteScopeOrDefaultScope = scope ? `the remote scope ${_chalk().default.bold(scope)}` : `the default-scope ${_chalk().default.bold(result.laneId.scope)}. you can change the lane's scope, before it is exported, with the "bit lane change-scope" command`;
    const title = (0, _cli().formatSuccessSummary)(`added and checked out to the new lane ${_chalk().default.bold(result.alias || result.laneId.name)} based on lane ${_chalk().default.bold(currentLane.name)}`);
    const remoteScopeOutput = (0, _cli().formatHint)(`this lane will be exported to ${remoteScopeOrDefaultScope}`);
    return (0, _cli().joinSections)([title, remoteScopeOutput]);
  }
}
exports.MergeMoveLaneCmd = MergeMoveLaneCmd;

//# sourceMappingURL=merge-move.cmd.js.map