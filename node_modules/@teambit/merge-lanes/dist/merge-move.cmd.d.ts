import type { Command, CommandOptions } from '@teambit/cli';
import type { MergeLanesMain } from './merge-lanes.main.runtime';
export type MergeAbortOpts = {
    silent?: boolean;
};
export declare class MergeMoveLaneCmd implements Command {
    private mergeLanes;
    name: string;
    description: string;
    extendedDescription: string;
    alias: string;
    options: CommandOptions;
    loader: boolean;
    remoteOp: boolean;
    constructor(mergeLanes: MergeLanesMain);
    report([newLaneName]: [string], { scope, }: {
        scope?: string;
    }): Promise<string>;
}
