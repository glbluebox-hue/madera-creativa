"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.DependentsCmd = void 0;
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _dependents() {
  const data = require("./dependents");
  _dependents = function () {
    return data;
  };
  return data;
}
function _template() {
  const data = require("./template");
  _template = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class DependentsCmd {
  constructor(workspace) {
    this.workspace = workspace;
    _defineProperty(this, "name", 'dependents <component-name>');
    _defineProperty(this, "helpUrl", 'reference/dependencies/inspecting-dependencies#review-dependents');
    _defineProperty(this, "arguments", [{
      name: 'component-name',
      description: 'component name or component id'
    }]);
    _defineProperty(this, "description", 'show components that depend on the specified component');
    _defineProperty(this, "extendedDescription", `displays components from both workspace and scope that depend on the specified component.
useful for understanding impact before making changes to a component or when planning refactoring.
shows both direct and transitive dependents organized by their origin (workspace vs scope).`);
    _defineProperty(this, "group", 'dependencies');
    _defineProperty(this, "alias", '');
    _defineProperty(this, "options", [['j', 'json', 'return the dependents in JSON format']]);
  }
  async report([id]) {
    const results = await (0, _dependents().dependents)(id, this.workspace);
    if (!results.scopeDependents.length && !results.workspaceDependents.length) {
      return `no dependents found for ${results.id.toString()}.
try running "bit cat-component ${results.id.toStringWithoutVersion()}" to see whether the component/version exists locally`;
    }
    const scopeTable = (0, _template().generateDependentsInfoTable)(results.scopeDependents, results.id);
    const workspaceTable = (0, _template().generateDependentsInfoTable)(results.workspaceDependents, results.id);
    return `${_chalk().default.bold('Dependents originated from workspace')}
${workspaceTable || '<none>'}

${_chalk().default.bold('Dependents originated from scope')}
${scopeTable || '<none>'}`;
  }
  async json([id]) {
    const results = await (0, _dependents().dependents)(id, this.workspace);
    const depInfoToString = depInfo => {
      return _objectSpread(_objectSpread({}, depInfo), {}, {
        id: depInfo.id.toString()
      });
    };
    return {
      scopeDependents: results.scopeDependents.map(depInfoToString),
      workspaceDependents: results.workspaceDependents.map(depInfoToString),
      id: results.id.toString()
    };
  }
}
exports.DependentsCmd = DependentsCmd;

//# sourceMappingURL=dependents-cmd.js.map