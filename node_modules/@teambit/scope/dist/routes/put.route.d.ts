import type { Route, Request, Response } from '@teambit/express';
import { Verb } from '@teambit/express';
import type { OnPostPutSlot, ScopeMain } from '../scope.main.runtime';
export declare class PutRoute implements Route {
    private scope;
    private postPutSlot;
    constructor(scope: ScopeMain, postPutSlot: OnPostPutSlot);
    method: string;
    route: string;
    verb: Verb;
    middlewares: ((req: Request, res: Response) => Promise<import("express").Response<any, Record<string, any>> | undefined>)[];
}
