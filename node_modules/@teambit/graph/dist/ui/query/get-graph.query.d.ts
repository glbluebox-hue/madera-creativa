import type { EdgeType } from '../../edge-type';
export declare const GET_GRAPH_IDS: import("graphql").DocumentNode;
export declare const GET_GRAPH: import("graphql").DocumentNode;
export type RawGraphQuery = {
    graph: RawGraph;
};
export type RawGraph = {
    nodes: RawNode[];
    edges: [];
};
export type RawNode = {
    id: string;
    component?: {
        id: {
            name: string;
            scope: string;
            version: string;
        };
        displayName: string;
        deprecation: {
            isDeprecate: boolean;
        };
        env: {
            id: string;
            icon: string;
        };
    };
};
export type RawEdge = {
    sourceId: string;
    targetId: string;
    dependencyLifecycleType: EdgeType;
};
