"use strict";

function _chai() {
  const data = require("chai");
  _chai = function () {
    return data;
  };
  return data;
}
function _coreAspects() {
  const data = require("./core-aspects");
  _coreAspects = function () {
    return data;
  };
  return data;
}
describe('core-aspects', () => {
  describe('getCoreAspectName', () => {
    it('should include the namespace in the aspect name', () => {
      const aspectName = (0, _coreAspects().getCoreAspectName)('teambit.workspace/e2e/workspace');
      (0, _chai().expect)(aspectName).to.equal('e2e.workspace');
    });
  });
});

//# sourceMappingURL=core-aspects.spec.js.map