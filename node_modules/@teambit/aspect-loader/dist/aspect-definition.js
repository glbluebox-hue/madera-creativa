"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.AspectDefinition = void 0;
class AspectDefinition {
  constructor(
  /**
   * path to the root directory of the aspect module.
   */
  aspectPath,
  /**
   * path to the aspect file (.aspect).
   */
  aspectFilePath,
  /**
   * path to the runtime entry
   */
  runtimePath,
  /**
   * aspect component
   */
  component,
  /**
   * id of the component (used instead of component in the case of core aspect)
   */
  id,
  /**
   * aspect defined using 'file://' protocol
   */
  local) {
    this.aspectPath = aspectPath;
    this.aspectFilePath = aspectFilePath;
    this.runtimePath = runtimePath;
    this.component = component;
    this.id = id;
    this.local = local;
  }
  get getId() {
    if (this.component) return this.component.id.toString();
    if (this.id) return this.id;
    return null;
  }
  static from({
    component,
    aspectPath,
    aspectFilePath,
    runtimePath,
    id,
    local
  }) {
    return new AspectDefinition(aspectPath, aspectFilePath, runtimePath, component, id, local);
  }
}
exports.AspectDefinition = AspectDefinition;

//# sourceMappingURL=aspect-definition.js.map