"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.CloudMain = void 0;
function _harmony() {
  const data = require("@teambit/harmony");
  _harmony = function () {
    return data;
  };
  return data;
}
function _cli() {
  const data = require("@teambit/cli");
  _cli = function () {
    return data;
  };
  return data;
}
function _uuid() {
  const data = require("uuid");
  _uuid = function () {
    return data;
  };
  return data;
}
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _os() {
  const data = _interopRequireDefault(require("os"));
  _os = function () {
    return data;
  };
  return data;
}
function _open() {
  const data = _interopRequireDefault(require("open"));
  _open = function () {
    return data;
  };
  return data;
}
function _scopes() {
  const data = require("@teambit/scopes.scope-descriptor");
  _scopes = function () {
    return data;
  };
  return data;
}
function _scopes2() {
  const data = require("@teambit/scopes.scope-id");
  _scopes2 = function () {
    return data;
  };
  return data;
}
function _logger() {
  const data = require("@teambit/logger");
  _logger = function () {
    return data;
  };
  return data;
}
function _legacy() {
  const data = require("@teambit/legacy.constants");
  _legacy = function () {
    return data;
  };
  return data;
}
function _scope() {
  const data = require("@teambit/scope");
  _scope = function () {
    return data;
  };
  return data;
}
function _scope2() {
  const data = require("@teambit/scope.network");
  _scope2 = function () {
    return data;
  };
  return data;
}
function _graphql() {
  const data = require("@teambit/graphql");
  _graphql = function () {
    return data;
  };
  return data;
}
function _workspace() {
  const data = require("@teambit/workspace");
  _workspace = function () {
    return data;
  };
  return data;
}
function _express() {
  const data = require("@teambit/express");
  _express = function () {
    return data;
  };
  return data;
}
function _child_process() {
  const data = require("child_process");
  _child_process = function () {
    return data;
  };
  return data;
}
function _ui() {
  const data = require("@teambit/ui");
  _ui = function () {
    return data;
  };
  return data;
}
function _cloud() {
  const data = require("./cloud.graphql");
  _cloud = function () {
    return data;
  };
  return data;
}
function _cloud2() {
  const data = require("./cloud.aspect");
  _cloud2 = function () {
    return data;
  };
  return data;
}
function _login() {
  const data = require("./login.cmd");
  _login = function () {
    return data;
  };
  return data;
}
function _logout() {
  const data = require("./logout.cmd");
  _logout = function () {
    return data;
  };
  return data;
}
function _whoami() {
  const data = require("./whoami.cmd");
  _whoami = function () {
    return data;
  };
  return data;
}
function _npmrc() {
  const data = require("./npmrc.cmd");
  _npmrc = function () {
    return data;
  };
  return data;
}
function _configStore() {
  const data = require("@teambit/config-store");
  _configStore = function () {
    return data;
  };
  return data;
}
var _CloudMain;
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
const HTTPS = 'https://';
class CloudMain {
  constructor(config, logger, express, workspace, scope, configStore, onSuccessLoginSlot) {
    this.config = config;
    this.logger = logger;
    this.express = express;
    this.workspace = workspace;
    this.scope = scope;
    this.configStore = configStore;
    this.onSuccessLoginSlot = onSuccessLoginSlot;
    _defineProperty(this, "authListenerByPort", new Map());
    _defineProperty(this, "REDIRECT_URL", void 0);
  }
  getNpmConfig() {
    try {
      const output = (0, _child_process().execFileSync)('npm', ['config', 'list', '--json'], {
        encoding: 'utf8'
      });
      return JSON.parse(output);
    } catch (error) {
      throw new Error(`failed to get npm config. error: ${error}`);
    }
  }
  async generateNpmrc({
    dryRun,
    force
  } = {}) {
    const authToken = this.getAuthToken();
    if (!authToken) {
      throw new Error('user is not logged in');
    }
    const currentUser = await this.getCurrentUser();
    let username = currentUser?.username;
    if (!username) {
      this.logger.warn('failed to fetch username from cloud for the current user. falling back to the global config.');
      username = this.getUsername();
    }
    if (!username) {
      throw new Error('username is not found in the global config or the cloud. please login first.');
    }
    return this.updateNpmConfig({
      authToken,
      username,
      dryRun,
      force
    });
  }
  detectConfigConflicts({
    newConfigs,
    registryDomain
  }) {
    const existingConfigs = this.getNpmConfig();
    const conflicts = [];
    newConfigs.split('\n').forEach(line => {
      const [key, newValue] = line.split('=');
      if (key && newValue) {
        const trimmedKey = key.trim();
        const existingValue = existingConfigs[trimmedKey];
        const isRegistryConflict = existingValue && !existingValue.includes(registryDomain);
        if (existingValue && isRegistryConflict) {
          conflicts.push({
            original: `${trimmedKey}=${existingValue}`,
            modifications: `${trimmedKey}=${newValue.trim()}`
          });
        }
      }
    });
    return conflicts;
  }
  async updateNpmConfig({
    authToken,
    username,
    dryRun,
    force
  }) {
    const orgs = (await this.getUserOrganizations()) ?? [];
    const orgNames = orgs.map(org => org.name);
    const allOrgs = Array.from(new Set([...CloudMain.PRESET_ORGS, ...orgNames, username])).sort();
    const registryUrlStr = this.getRegistryUrl();
    const registryUrl = new URL(registryUrlStr);
    // Single source of truth for the npm config entries, so conflict detection, the returned
    // `configUpdates` string, and the actual `npm config set` argv can never drift apart.
    const configEntries = [...allOrgs.map(org => [`@${org}:registry`, registryUrlStr]), [`//${registryUrl.host}/:_authToken`, authToken]];
    const configUpdates = configEntries.map(([key, value]) => `${key}="${value}"`).join('\n');
    if (!force) {
      const conflicts = this.detectConfigConflicts({
        newConfigs: configUpdates,
        registryDomain: this.getCloudDomain()
      });
      if (conflicts.length > 0) {
        return {
          conflicts,
          configUpdates
        };
      }
    }
    if (dryRun) {
      return {
        configUpdates
      };
    }

    // Pass each key=value as a separate argv element via execFileSync (no shell), so an
    // attacker-controlled org/username (e.g. from the login callback's query string) can never
    // be interpreted as shell syntax. The quotes in `configUpdates` are only needed for shell
    // parsing, so the argv values are unquoted — npm receives the literal value.
    const npmConfigArgs = ['config', 'set', ...configEntries.map(([key, value]) => `${key}=${value}`)];
    (0, _child_process().execFileSync)('npm', npmConfigArgs, {
      stdio: 'ignore'
    });
    return {
      configUpdates
    };
  }
  setupAuthListener({
    port: portFromParams,
    clientId = (0, _uuid().v4)(),
    skipConfigUpdate,
    cloudDomain
  } = {}) {
    return new Promise((resolve, reject) => {
      const port = portFromParams || this.getLoginPort();
      const existingAuthListener = this.authListenerByPort.get(port);
      if (existingAuthListener) {
        this.logger.debug(`auth server is already running on port ${port}`);
        resolve(existingAuthListener);
        return;
      }
      const expressApp = this.express.createApp();
      this.authListenerByPort.set(port, {
        port,
        clientId
      });
      const authServer = expressApp.listen(port, () => {
        this.logger.debug(`cloud express server started on port ${port}`);
        const existing = this.authListenerByPort.get(port) ?? {};
        this.authListenerByPort.set(port, _objectSpread(_objectSpread({
          port,
          clientId
        }, existing), {}, {
          server: authServer
        }));
        resolve({
          port,
          server: authServer,
          clientId
        });
      }).on('error', err => {
        // @ts-ignore
        const {
          code
        } = err;
        if (code === 'EADDRINUSE') {
          // set up a new auth listener with new port
          this.logger.warn(`port: ${port} already in use for cloud auth listener, trying port ${port + 1}`);
          // eslint-disable-next-line promise/no-promise-in-callback
          this.setupAuthListener({
            port: port + 1
          }).then(resolve).catch(reject);
          return;
        }
        this.logger.error(`cloud express server failed to start on port ${port}`, err);
        reject(err);
      });

      // eslint-disable-next-line @typescript-eslint/no-misused-promises
      expressApp.get('/', async (req, res) => {
        this.logger.debug('cloud.authListener', 'received request', req.query);
        try {
          const {
            clientId: clientIdFromReq,
            redirectUri,
            username: usernameFromReq
          } = req.query;
          const username = typeof usernameFromReq === 'string' ? usernameFromReq : undefined;
          let {
            token
          } = req.query;
          if (Array.isArray(token)) {
            token = token[0];
          }
          if (typeof token !== 'string') {
            res.status(400).send('Invalid token format');
            return res;
          }
          await this.configStore.setConfig(_legacy().CFG_USER_TOKEN_KEY, token);
          if (username) await this.configStore.setConfig(_legacy().CFG_USER_NAME_KEY, username);
          const existing = this.authListenerByPort.get(port) ?? {};
          this.authListenerByPort.set(port, _objectSpread(_objectSpread({
            port,
            server: authServer,
            clientId: clientIdFromReq
          }, existing), {}, {
            username
          }));
          const onLoggedInFns = this.onSuccessLoginSlot.values();
          const updatedConfigs = await this.updateGlobalConfigOnLogin(cloudDomain);
          if (!skipConfigUpdate) {
            this.updateNpmConfig({
              authToken: token,
              username: username
            }).then(configUpdates => {
              onLoggedInFns.forEach(fn => fn({
                username,
                token: token,
                npmrcUpdateResult: configUpdates,
                updatedConfigs
              }));
            }).catch(error => {
              onLoggedInFns.forEach(fn => fn({
                username,
                token: token,
                updatedConfigs,
                npmrcUpdateResult: {
                  error: new Error(`failed to update npmrc. error ${error?.toString}`)
                }
              }));
            });
          } else {
            onLoggedInFns.forEach(fn => fn({
              username,
              token: token
            }));
          }
          if (this.REDIRECT_URL) return res.redirect(this.REDIRECT_URL);
          if (typeof redirectUri === 'string' && redirectUri) return res.redirect(redirectUri);
          return res.status(200).send('Login successful');
        } catch (err) {
          this.logger.error(`Error on login: ${err}`);
          res.status(CloudMain.ERROR_RESPONSE).send('Login failed');
          reject(err);
          return res;
        }
      });
    });
  }
  registerOnSuccessLogin(onSuccessLoginFn) {
    this.onSuccessLoginSlot.register(onSuccessLoginFn);
    return this;
  }
  getCloudDomain() {
    return this.config.cloudDomain;
  }
  getCloudHubDomain() {
    return this.config.cloudHubDomain;
  }
  getCloudApi() {
    return this.config.cloudApi;
  }
  getCloudGraphQL() {
    return this.config.cloudGraphQL;
  }
  getLoginDomain() {
    return this.config.loginDomain;
  }
  getAnalyticsDomain() {
    return this.config.analyticsDomain;
  }
  getRegistryUrl() {
    return this.config.registryUrl;
  }
  getCloudExporterUrl() {
    return this.config.cloudExporterUrl;
  }
  getHubName() {
    return this.config.cloudHubName;
  }
  getLoginPort() {
    return this.config.loginPort || CloudMain.DEFAULT_PORT;
  }
  async isLoggedIn() {
    const currentUser = await this.getCurrentUser();
    return Boolean(currentUser);
  }
  getAuthToken() {
    const processToken = _cli().globalFlags.token;
    const token = processToken || this.configStore.getConfig(_legacy().CFG_USER_TOKEN_KEY);
    if (!token) return null;
    return token;
  }
  getAuthHeader() {
    return {
      Authorization: `Bearer ${this.getAuthToken()}`
    };
  }
  getUsername() {
    return this.configStore.getConfig(_legacy().CFG_USER_NAME_KEY);
  }
  ensureHttps(url) {
    if (!url.startsWith('http')) {
      return `${HTTPS}${url}`;
    }
    return url;
  }
  calculateLoginDomain(cloudDomain) {
    let finalCloudDomain = this.ensureHttps(cloudDomain);
    if (!finalCloudDomain.endsWith('/bit-login')) {
      finalCloudDomain = `${finalCloudDomain}/bit-login`;
    }
    return finalCloudDomain;
  }
  async getLoginUrl({
    redirectUrl,
    machineName,
    cloudDomain,
    port: portFromParams
  } = {}) {
    const loginUrl = cloudDomain ? this.calculateLoginDomain(cloudDomain) : this.getLoginDomain();
    const port = Number(portFromParams) || this.getLoginPort();
    if (redirectUrl) {
      this.REDIRECT_URL = redirectUrl;
    }
    const authListenerForPort = this.authListenerByPort.get(port);
    if (authListenerForPort) {
      return `${loginUrl}?port=${port}&clientId=${authListenerForPort.clientId}&responseType=token&deviceName=${machineName || _os().default.hostname()}&os=${process.platform}`;
    }
    const authListener = await this.setupAuthListener({
      port
    });
    if (!authListener) return null;
    return encodeURI(`${loginUrl}?port=${port}&clientId=${authListener?.clientId}&responseType=token&deviceName=${machineName || _os().default.hostname()}&os=${process.platform}`);
  }
  async logout() {
    await this.configStore.delConfig(_legacy().CFG_USER_TOKEN_KEY);
    await this.configStore.delConfig(_legacy().CFG_USER_NAME_KEY);
  }
  setRedirectUrl(redirectUrl) {
    this.REDIRECT_URL = redirectUrl;
  }
  async whoami() {
    const currentUser = await this.getCurrentUser();
    if (!currentUser?.username) {
      return undefined;
    }
    return currentUser.username;
  }
  globalConfigsToUpdateOnLogin(domain) {
    const currentCloudDomain = this.configStore.getConfig(_legacy().CFG_CLOUD_DOMAIN_KEY);
    const res = {};
    if (!domain || domain === _legacy().DEFAULT_CLOUD_DOMAIN) {
      if (currentCloudDomain && currentCloudDomain !== _legacy().DEFAULT_CLOUD_DOMAIN) {
        res[_legacy().CFG_CLOUD_DOMAIN_KEY] = '';
      }
      return res;
    }
    if (currentCloudDomain !== domain) {
      res[_legacy().CFG_CLOUD_DOMAIN_KEY] = domain;
    }
    return res;
  }
  async updateGlobalConfigOnLogin(domain) {
    const configsToUpdate = this.globalConfigsToUpdateOnLogin(domain);
    for await (const [key, value] of Object.entries(configsToUpdate)) {
      if (value) {
        await this.configStore.setConfig(key, value);
      } else {
        await this.configStore.delConfig(key);
      }
    }
    // Refresh the config after updating
    (0, _legacy().clearCachedUrls)();
    this.config = CloudMain.calculateConfig();
    return configsToUpdate;
  }
  async login(port, suppressBrowserLaunch, machineName, cloudDomain, redirectUrl, skipConfigUpdate, defaultCloudDomain) {
    if (defaultCloudDomain) {
      cloudDomain = _legacy().DEFAULT_CLOUD_DOMAIN;
    }
    const isLoggedIn = await this.isLoggedIn();
    return new Promise((resolve, reject) => {
      if (isLoggedIn) {
        resolve({
          isAlreadyLoggedIn: true,
          username: this.configStore.getConfig(_legacy().CFG_USER_NAME_KEY),
          token: this.configStore.getConfig(_legacy().CFG_USER_TOKEN_KEY)
        });
        return;
      }
      const promptLogin = async () => {
        this.REDIRECT_URL = redirectUrl;
        this.registerOnSuccessLogin(loggedInParams => {
          resolve({
            username: loggedInParams.username,
            token: loggedInParams.token,
            npmrcUpdateResult: loggedInParams.npmrcUpdateResult,
            globalConfigUpdates: loggedInParams.updatedConfigs
          });
        });
        const loginUrl = await this.getLoginUrl({
          machineName,
          cloudDomain,
          port
        });
        if (!loginUrl) {
          reject(new Error('Failed to get login url'));
          return;
        }
        if (!suppressBrowserLaunch) {
          console.log(_chalk().default.yellow(`Your browser has been opened to visit:\n${loginUrl}`)); // eslint-disable-line no-console
          (0, _open().default)(loginUrl).catch(err => {
            this.logger.error(`failed to open browser on login, err: ${err}`);
            reject(err);
          });
        } else {
          console.log(_chalk().default.yellow(`Go to the following link in your browser::\n${loginUrl}`)); // eslint-disable-line no-console
        }
      };
      try {
        this.setupAuthListener({
          port: Number(port),
          skipConfigUpdate,
          cloudDomain
        }).then(promptLogin).catch(e => reject(e));
      } catch (err) {
        reject(err);
      }
    });
  }
  async getCloudScopes(scopes) {
    const remotes = await this.scope.getRemoteScopes();
    const filteredScopesToFetch = scopes.filter(scope => {
      return _scopes2().ScopeID.isValid(scope) && remotes.isHub(scope);
    });
    const queryResponse = await this.fetchFromSymphonyViaGQL(CloudMain.GET_SCOPES, {
      ids: filteredScopesToFetch
    });
    const scopesFromQuery = queryResponse?.data?.getScopes;
    if (!scopesFromQuery) return [];
    return scopesFromQuery.map(scope => {
      const scopeDescriptorObj = _objectSpread(_objectSpread({}, scope), {}, {
        id: _scopes2().ScopeID.fromString(scope.id)
      });
      return _scopes().ScopeDescriptor.fromObject(scopeDescriptorObj);
    });
  }
  async getCurrentUser() {
    return this.fetchFromSymphonyViaGQL(CloudMain.GET_CURRENT_USER).then(response => {
      if (!response) return null;
      return {
        isLoggedIn: true,
        displayName: response.data.me.displayName,
        username: response.data.me.username,
        profileImage: response.data.me.image
      };
    });
  }
  async getUserOrganizations() {
    return this.fetchFromSymphonyViaGQL(CloudMain.GET_USER_ORGANIZATIONS).then(response => {
      if (!response) return null;
      return response.data.getUserOrganizations;
    });
  }
  async fetchFromSymphonyViaGQL(query, variables) {
    const graphqlUrl = `${this.getCloudApi()}${CloudMain.GRAPHQL_ENDPOINT}`;
    const body = JSON.stringify({
      query,
      variables
    });
    const headers = _objectSpread({
      'Content-Type': 'application/json'
    }, this.getAuthHeader());
    try {
      const response = await (0, _scope2().fetchWithAgent)(graphqlUrl, {
        method: 'POST',
        headers,
        body
      });
      if (!response.ok) {
        throw new Error(`HTTP error! status: ${response.status}`);
      }
      return await response.json();
    } catch (error) {
      this.logger.debug('fetchFromSymphonyViaGQL. ', 'Error fetching data: ', error);
      return null;
    }
  }
  static calculateConfig() {
    return {
      cloudDomain: (0, _legacy().getCloudDomain)(),
      cloudHubDomain: _legacy().DEFAULT_HUB_DOMAIN,
      cloudApi: (0, _legacy().getSymphonyUrl)(),
      cloudGraphQL: _legacy().SYMPHONY_GRAPHQL,
      loginDomain: (0, _legacy().getLoginUrl)(),
      analyticsDomain: _legacy().DEFAULT_ANALYTICS_DOMAIN,
      registryUrl: (0, _legacy().getRegistryUrl)(),
      cloudExporterUrl: _legacy().CENTRAL_BIT_HUB_URL,
      cloudHubName: _legacy().CENTRAL_BIT_HUB_NAME,
      loginPort: CloudMain.DEFAULT_PORT
    };
  }
  static async provider([loggerMain, graphql, express, workspace, scope, configStore, cli, ui], config, [onSuccessLoginSlot]) {
    const logger = loggerMain.createLogger(_cloud2().CloudAspect.id);
    const cloudMain = new CloudMain(config, logger, express, workspace, scope, configStore, onSuccessLoginSlot);
    const loginCmd = new (_login().LoginCmd)(cloudMain, 8889);
    const logoutCmd = new (_logout().LogoutCmd)(cloudMain);
    const whoamiCmd = new (_whoami().WhoamiCmd)(cloudMain);
    const npmrcGenerateCmd = new (_npmrc().NpmrcGenerateCmd)(cloudMain, 8889);
    const npmrc = new (_npmrc().NpmrcCmd)();
    npmrc.commands = [npmrcGenerateCmd];
    cli.register(loginCmd, logoutCmd, whoamiCmd, npmrc);
    graphql.register(() => (0, _cloud().cloudSchema)(cloudMain));
    if (workspace) {
      ui.registerOnStart(async () => {
        await cloudMain.setupAuthListener();
        return undefined;
      });
    }
    return cloudMain;
  }
}
exports.CloudMain = CloudMain;
_CloudMain = CloudMain;
_defineProperty(CloudMain, "ERROR_RESPONSE", 500);
_defineProperty(CloudMain, "DEFAULT_PORT", 8888);
_defineProperty(CloudMain, "REDIRECT", 302);
_defineProperty(CloudMain, "GRAPHQL_ENDPOINT", '/graphql');
_defineProperty(CloudMain, "GET_SCOPES", `
      query GET_SCOPES($ids: [String]!) {
        getScopes(ids: $ids) {
          id
          scopeStyle {
            icon
            backgroundIconColor
            stripColor
          }
        }
      }
    `);
_defineProperty(CloudMain, "GET_CURRENT_USER", `
    query GET_ME {
      me {
        id
        username
        image
        displayName
      }
    }
  `);
_defineProperty(CloudMain, "GET_USER_ORGANIZATIONS", `
    query GET_USER_ORGANIZATIONS {
      getUserOrganizations {
        id
        name
      }
    }
  `);
_defineProperty(CloudMain, "PRESET_ORGS", ['bitdev', 'teambit', 'bitdesign', 'frontend', 'backend']);
_defineProperty(CloudMain, "slots", [_harmony().Slot.withType()]);
_defineProperty(CloudMain, "dependencies", [_logger().LoggerAspect, _graphql().GraphqlAspect, _express().ExpressAspect, _workspace().WorkspaceAspect, _scope().ScopeAspect, _configStore().ConfigStoreAspect, _cli().CLIAspect, _ui().UIAspect]);
_defineProperty(CloudMain, "runtime", _cli().MainRuntime);
_defineProperty(CloudMain, "defaultConfig", _CloudMain.calculateConfig());
_cloud2().CloudAspect.addRuntime(CloudMain);

//# sourceMappingURL=cloud.main.runtime.js.map