"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getLastExportPath = getLastExportPath;
exports.readLastExport = readLastExport;
exports.writeLastExport = writeLastExport;
function _path() {
  const data = _interopRequireDefault(require("path"));
  _path = function () {
    return data;
  };
  return data;
}
function _fsExtra() {
  const data = _interopRequireDefault(require("fs-extra"));
  _fsExtra = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
const LAST_EXPORT_FILE = 'last-export.json';
function getLastExportPath(scopePath) {
  return _path().default.join(scopePath, LAST_EXPORT_FILE);
}
async function writeLastExport(scopePath, data) {
  await _fsExtra().default.writeFile(getLastExportPath(scopePath), JSON.stringify(data, null, 2));
}
async function readLastExport(scopePath) {
  const filePath = getLastExportPath(scopePath);
  if (!(await _fsExtra().default.pathExists(filePath))) return null;
  try {
    const raw = await _fsExtra().default.readFile(filePath, 'utf-8');
    const parsed = JSON.parse(raw);
    return isValidLastExport(parsed) ? parsed : null;
  } catch {
    return null;
  }
}
function isValidLastExport(value) {
  if (!value || typeof value !== 'object') return false;
  const v = value;
  if (typeof v.timestamp !== 'string') return false;
  if (!Array.isArray(v.rippleJobs) || !v.rippleJobs.every(j => typeof j === 'string' && j.length > 0)) return false;
  if (!Array.isArray(v.exportedComponents) || !v.exportedComponents.every(c => typeof c === 'string' && c.length > 0)) return false;
  if (v.lane !== undefined) {
    if (!v.lane || typeof v.lane !== 'object') return false;
    const lane = v.lane;
    if (typeof lane.scope !== 'string' || lane.scope.length === 0) return false;
    if (typeof lane.name !== 'string' || lane.name.length === 0) return false;
  }
  return true;
}

//# sourceMappingURL=last-export.js.map