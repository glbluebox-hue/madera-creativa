export interface LastExportData {
    timestamp: string;
    /** ripple job slugs returned by the central-hub — used to look up jobs via getJob(slug) */
    rippleJobs: string[];
    lane?: {
        scope: string;
        name: string;
    };
    exportedComponents: string[];
}
export declare function getLastExportPath(scopePath: string): string;
export declare function writeLastExport(scopePath: string, data: LastExportData): Promise<void>;
export declare function readLastExport(scopePath: string): Promise<LastExportData | null>;
