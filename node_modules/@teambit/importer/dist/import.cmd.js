"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ImportCmd = void 0;
function _cli() {
  const data = require("@teambit/cli");
  _cli = function () {
    return data;
  };
  return data;
}
function _chalk() {
  const data = _interopRequireDefault(require("chalk"));
  _chalk = function () {
    return data;
  };
  return data;
}
function _lodash() {
  const data = require("lodash");
  _lodash = function () {
    return data;
  };
  return data;
}
function _componentModules() {
  const data = require("@teambit/component.modules.merge-helper");
  _componentModules = function () {
    return data;
  };
  return data;
}
function _componentId() {
  const data = require("@teambit/component-id");
  _componentId = function () {
    return data;
  };
  return data;
}
function _bitError() {
  const data = require("@teambit/bit-error");
  _bitError = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
class ImportCmd {
  // gets populated by yargs-adapter.handler().

  constructor(importer) {
    this.importer = importer;
    _defineProperty(this, "name", 'import [component-patterns...]');
    _defineProperty(this, "description", 'bring components from remote scopes into your workspace');
    _defineProperty(this, "helpUrl", 'reference/components/importing-components');
    _defineProperty(this, "arguments", [{
      name: 'component-patterns...',
      description: 'component IDs or component patterns (separated by space). Use patterns to import groups of components using a common scope or namespace. E.g., "utils/*" (wrap with double quotes)'
    }]);
    _defineProperty(this, "extendedDescription", `brings component source files from remote scopes into your workspace and installs their dependencies as packages.
supports pattern matching for bulk imports, merge strategies for handling conflicts, and various optimization options.
without arguments, fetches all workspace components' latest versions from their remote scopes.`);
    _defineProperty(this, "group", 'collaborate');
    _defineProperty(this, "alias", '');
    _defineProperty(this, "options", [['p', 'path <path>', 'import components into a specific directory (a relative path in the workspace)'], ['o', 'objects', 'import components objects to the local scope without checkout (without writing them to the file system). This is the default behavior for import with no id argument'], ['O', 'override', 'override local changes'], ['v', 'verbose', 'show verbose output for inspection'], ['j', 'json', 'return the output as JSON'],
    // ['', 'conf', 'write the configuration file (component.json) of the component'], // not working. need to fix once ComponentWriter is moved to Harmony
    ['x', 'skip-dependency-installation', 'do not auto-install dependencies of the imported components'], ['', 'skip-write-config-files', 'do not write config files (such as eslint, tsconfig, prettier, etc...)'], ['m', 'merge [strategy]', 'merge local changes with the imported version. strategy should be "theirs", "ours" or "manual"'], ['', 'dependencies', 'import all dependencies (bit components only) of imported components and write them to the workspace'], ['', 'dependencies-head', 'same as --dependencies, except it imports the dependencies with their head version'], ['', 'dependencies-depth <n>', 'limit how many levels deep to traverse with --dependencies/--dependencies-head (1=direct deps only). default: traverse all levels'], ['', 'dependents', 'import components found while traversing from the imported components upwards to the workspace components'], ['', 'dependents-via <string>', 'same as --dependents except the traversal must go through the specified component. to specify multiple components, wrap with quotes and separate by a comma'], ['', 'dependents-all', 'same as --dependents except not prompting for selecting paths but rather selecting all paths and showing final confirmation before importing'], ['', 'dependents-dry-run', 'DEPRECATED. (this is the default now). same as --dependents, except it prints the found dependents and waits for confirmation before importing them'], ['', 'silent', 'no prompt for --dependents/--dependents-via flags'], ['', 'filter-envs <envs>', 'only import components that have the specified environment (e.g., "teambit.react/react-env")'], ['', 'save-in-lane', 'when checked out to a lane and the component is not on the remote-lane, save it in the lane (defaults to save on main)'], ['', 'all-history', 'relevant for fetching all components objects. avoid optimizations, fetch all history versions, always'], ['', 'fetch-deps', "fetch dependencies (bit components) objects to the local scope, but don't add to the workspace. Useful to resolve errors about missing dependency data"], ['', 'write-deps <target>', 'write all workspace component dependencies to the specified target ("package.json" or "workspace.jsonc"), resolving conflicts by picking the ranges that match the highest versions'], ['', 'track-only', 'do not write any component files, just create .bitmap entries of the imported components. Useful when the files already exist and just want to re-add the component to the bitmap'], ['', 'include-deprecated', 'when importing with patterns, include deprecated components (default to exclude them)'], ['', 'lane-only', 'when using wildcards on a lane, only import components that exist on the lane (never from main)'], ['', 'owner', 'treat the argument as an owner name and import all components from all scopes of that owner'], ['', 'write-to-empty-dir', 'when the target directory is not empty, import into an available empty directory (e.g. "foo" => "foo_1") instead of failing']]);
    _defineProperty(this, "loader", true);
    _defineProperty(this, "remoteOp", true);
    _defineProperty(this, "_packageManagerArgs", void 0);
  }
  async report([ids = []], importFlags) {
    const {
      importDetails,
      importedIds,
      importedDeps,
      installationError,
      compilationError,
      workspaceConfigUpdateResult,
      missingIds,
      cancellationMessage,
      lane
    } = await this.getImportResults(ids, importFlags);
    if (!importedIds.length && !missingIds?.length) {
      return _chalk().default.yellow(cancellationMessage || 'nothing to import');
    }
    const importedIdsUniqNoVersion = _componentId().ComponentIdList.fromArray(importedIds).toVersionLatest();
    const summaryPrefix = importedIdsUniqNoVersion.length === 1 ? 'successfully imported one component' : `successfully imported ${importedIdsUniqNoVersion.length} components`;
    let upToDateCount = 0;
    const importedComponents = importedIds.map(bitId => {
      const details = importDetails.find(c => c.id === bitId.toStringWithoutVersion());
      if (!details) throw new Error(`missing details for component ${bitId.toString()}`);
      if (details.status === 'up to date') {
        upToDateCount += 1;
      }
      return formatPlainComponentItemWithVersions(bitId, details);
    }).filter(Boolean);
    const getWsConfigUpdateLogs = () => {
      const logs = workspaceConfigUpdateResult?.logs;
      if (!logs || !logs.length) return '';
      const logsStr = logs.join('\n');
      return `${(0, _cli().formatTitle)('verbose logs of workspace config update')}\n${logsStr}`;
    };
    const upToDateSuffix = lane ? ' on the lane' : '';
    const upToDateStr = upToDateCount === 0 ? '' : `, ${upToDateCount} components are up to date${upToDateSuffix}`;
    const summary = `${summaryPrefix}${upToDateStr}`;
    const importOutput = importedComponents.length ? (0, _cli().formatSection)('imported components', '', importedComponents) : '';
    const importedDepsOutput = importFlags.displayDependencies && importedDeps.length ? (0, _cli().formatSection)('imported component dependencies', '', (0, _lodash().uniq)(importedDeps.map(formatPlainComponentItem))) : '';
    const getRemovedWarning = () => {
      const removedDetails = importDetails.filter(d => d.removed);
      if (!removedDetails.length) return '';
      const removedItems = removedDetails.map(d => (0, _cli().formatItem)(_chalk().default.bold(d.id), _cli().errorSymbol));
      const hintBase = `run "bit recover <component-id>" to restore`;
      const hintSuffix = lane ? `, then "bit lane merge main" to get latest updates` : '';
      return (0, _cli().joinSections)([(0, _cli().formatSection)('deleted components', 'imported component(s) are marked as deleted and may not be up to date', removedItems), (0, _cli().formatHint)(`${hintBase}${hintSuffix}`)]);
    };
    return (0, _cli().joinSections)([getWsConfigUpdateLogs(), importOutput, importedDepsOutput, formatMissingComponents(missingIds), getRemovedWarning(), (0, _componentModules().getWorkspaceConfigUpdateOutput)(workspaceConfigUpdateResult), (0, _componentModules().installationErrorOutput)(installationError), (0, _componentModules().compilationErrorOutput)(compilationError), (0, _cli().formatSuccessSummary)(summary)]);
  }
  async json([ids], importFlags) {
    const {
      importDetails,
      installationError,
      missingIds
    } = await this.getImportResults(ids, importFlags);
    return {
      importDetails,
      installationError,
      missingIds
    };
  }
  async getImportResults(ids, {
    path,
    objects = false,
    override = false,
    verbose = false,
    conf,
    skipDependencyInstallation = false,
    skipWriteConfigFiles = false,
    merge,
    filterEnvs,
    saveInLane = false,
    dependencies = false,
    dependenciesHead = false,
    dependenciesDepth,
    dependents = false,
    dependentsDryRun = false,
    silent,
    dependentsVia,
    dependentsAll,
    allHistory = false,
    fetchDeps = false,
    trackOnly = false,
    includeDeprecated = false,
    writeDeps,
    laneOnly = false,
    owner = false,
    writeToEmptyDir = false
  }) {
    if (dependentsDryRun) {
      this.importer.logger.warn(`the "--dependents-dry-run" flag is deprecated and is now the default behavior`);
    }
    if (objects && merge) {
      throw new (_bitError().BitError)(' --objects and --merge flags cannot be used together');
    }
    if (override && merge) {
      throw new (_bitError().BitError)('--override and --merge cannot be used together');
    }
    if (writeToEmptyDir) {
      // --override deletes the occupied dir to write in place, and --path targets one specific directory; both
      // contradict --write-to-empty-dir, which relocates elsewhere when the target dir is occupied.
      if (override) throw new (_bitError().BitError)('--override and --write-to-empty-dir cannot be used together');
      if (path) throw new (_bitError().BitError)('--path and --write-to-empty-dir cannot be used together');
    }
    if (!ids.length && dependencies) {
      throw new (_bitError().BitError)('you have to specify ids to use "--dependencies" flag');
    }
    if (!ids.length && dependenciesHead) {
      throw new (_bitError().BitError)('you have to specify ids to use "--dependencies-head" flag');
    }
    if (!ids.length && dependents) {
      throw new (_bitError().BitError)('you have to specify ids to use "--dependents" flag');
    }
    if (!ids.length && dependentsVia) {
      throw new (_bitError().BitError)('you have to specify ids to use "--dependents-via" flag');
    }
    if (!ids.length && trackOnly) {
      throw new (_bitError().BitError)('you have to specify ids to use "--track-only" flag');
    }
    if (owner && ids.length !== 1) {
      throw new (_bitError().BitError)('--owner flag requires exactly one argument (the owner name)');
    }
    let dependenciesDepthNum;
    if (dependenciesDepth !== undefined) {
      if (!dependencies && !dependenciesHead) {
        throw new (_bitError().BitError)('"--dependencies-depth" flag requires "--dependencies" or "--dependencies-head"');
      }
      dependenciesDepthNum = Number(dependenciesDepth);
      if (!Number.isInteger(dependenciesDepthNum) || dependenciesDepthNum < 1) {
        throw new (_bitError().BitError)(`"--dependencies-depth" must be a positive integer, got "${dependenciesDepth}"`);
      }
    }
    let mergeStrategy;
    if (merge && typeof merge === 'string') {
      const options = Object.keys(_componentModules().MergeOptions);
      if (!options.includes(merge)) {
        throw new (_bitError().BitError)(`merge must be one of the following: ${options.join(', ')}`);
      }
      mergeStrategy = merge;
    }
    const envsToFilter = filterEnvs ? filterEnvs.split(',').map(p => p.trim()) : undefined;
    const importOptions = {
      ids,
      verbose,
      merge: Boolean(merge),
      filterEnvs: envsToFilter,
      mergeStrategy,
      writeToPath: path,
      objectsOnly: objects,
      override,
      writeConfig: Boolean(conf),
      installNpmPackages: !skipDependencyInstallation,
      writeConfigFiles: !skipWriteConfigFiles,
      saveInLane,
      importDependenciesDirectly: dependencies,
      importHeadDependenciesDirectly: dependenciesHead,
      dependenciesDepth: dependenciesDepthNum,
      importDependents: dependents,
      dependentsVia,
      dependentsAll,
      silent,
      allHistory,
      fetchDeps,
      trackOnly,
      includeDeprecated,
      writeDeps,
      laneOnly,
      owner,
      writeToEmptyDir
    };
    return this.importer.import(importOptions, this._packageManagerArgs);
  }
}
exports.ImportCmd = ImportCmd;
function formatMissingComponents(missing) {
  if (!missing?.length) return '';
  const desc = `the following components are missing from the remote in the requested version, try running "bit status" to re-sync your .bitmap file\nalso, check that the requested version exists on main or the checked out lane`;
  const items = missing.map(id => (0, _cli().formatItem)(_chalk().default.red(id), _cli().errorSymbol));
  return (0, _cli().formatSection)('missing components', desc, items);
}
function formatPlainComponentItem({
  scope,
  name,
  version,
  deprecated
}) {
  const id = `${scope ? `${scope}/` : ''}${name}@${version ? version.toString() : 'latest'}`;
  const suffix = deprecated ? ` ${_chalk().default.yellow('[deprecated]')}` : '';
  return (0, _cli().formatItem)(_chalk().default.cyan(id) + suffix);
}
function formatPlainComponentItemWithVersions(bitId, importDetails) {
  const status = importDetails.status;
  const id = bitId.toStringWithoutVersion();
  let usingLatest = '';
  const getVersionsOutput = () => {
    if (!importDetails.versions.length) return '';
    if (importDetails.latestVersion) {
      if (importDetails.latestVersion === bitId.version && status === 'added') {
        usingLatest = ' (latest)';
        return '';
      }
      return `${importDetails.versions.length} new version(s) available, latest ${importDetails.latestVersion}`;
    }
    return importDetails.versions.length > 5 ? `${importDetails.versions.length} new versions` : `new versions: ${importDetails.versions.join(', ')}`;
  };
  const versions = getVersionsOutput();
  const usedVersion = status === 'added' ? `currently used version ${bitId.version}${usingLatest}` : '';
  const getConflictMessage = () => {
    if (!importDetails.filesStatus) return '';
    const conflictedFiles = Object.keys(importDetails.filesStatus)
    // @ts-ignore file is set
    .filter(file => importDetails.filesStatus[file] === _componentModules().FileStatus.manual);
    if (!conflictedFiles.length) return '';
    return `(the following files were saved with conflicts ${conflictedFiles.map(file => _chalk().default.bold(file)).join(', ')}) `;
  };
  const conflictMessage = getConflictMessage();
  const deprecated = importDetails.deprecated && !importDetails.removed ? _chalk().default.yellow('deprecated') : '';
  const removed = importDetails.removed ? _chalk().default.red('deleted') : '';
  const missingDeps = importDetails.missingDeps.length ? _chalk().default.red(`missing dependencies: ${importDetails.missingDeps.map(d => d.toString()).join(', ')}`) : '';
  if (status === 'up to date' && !missingDeps && !deprecated && !conflictMessage && !removed) {
    return undefined;
  }
  const symbol = removed ? _cli().errorSymbol : deprecated ? _cli().warnSymbol : undefined;
  const versionOutput = [versions, usedVersion].filter(Boolean).join(', ');
  const stateOutput = `${conflictMessage}${deprecated}${removed}`;
  const details = [_chalk().default.green(status), _chalk().default.cyan(id), versionOutput, stateOutput, missingDeps].filter(Boolean).join(' ');
  return (0, _cli().formatItem)(_chalk().default.dim(details), symbol);
}

//# sourceMappingURL=import.cmd.js.map