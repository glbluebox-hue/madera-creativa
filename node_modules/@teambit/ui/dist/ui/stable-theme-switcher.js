"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.StableThemeSwitcher = StableThemeSwitcher;
function _react() {
  const data = _interopRequireWildcard(require("react"));
  _react = function () {
    return data;
  };
  return data;
}
function _designThemes() {
  const data = require("@teambit/design.themes.base-theme");
  _designThemes = function () {
    return data;
  };
  return data;
}
function _designThemes2() {
  const data = require("@teambit/design.themes.dark-theme");
  _designThemes2 = function () {
    return data;
  };
  return data;
}
function _baseReactThemes() {
  const data = require("@teambit/base-react.themes.theme-switcher");
  _baseReactThemes = function () {
    return data;
  };
  return data;
}
function _designThemes3() {
  const data = require("@teambit/design.themes.light-theme");
  _designThemes3 = function () {
    return data;
  };
  return data;
}
function _interopRequireWildcard(e, t) { if ("function" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function (e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, default: e }; if (null === e || "object" != typeof e && "function" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]); return f; })(e, t); }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
// These must match the overrides each theme component passes to BaseTheme.
// LightTheme passes these 3 overrides; DarkTheme passes darkThemeValues.
const lightOverrides = {
  backgroundColor: '#fdfdff',
  surface01Color: '#f7f7f7',
  warningColor: '#BB8C25'
};
const themeOverridesMap = {
  light: lightOverrides,
  dark: _designThemes2().darkThemeValues
};
const themes = [_designThemes3().LightTheme, _designThemes2().DarkTheme];
/**
 * A stable theme switcher that does NOT swap React component types on theme change.
 * Instead, it renders a single stable <BaseTheme> and only changes the CSS variable
 * overrides, preventing the entire React tree from unmounting and remounting.
 */
function readPreflashTheme() {
  if (typeof document === 'undefined') return undefined;
  const fromAttr = document.documentElement.dataset.theme;
  if (fromAttr) return fromAttr;
  try {
    const fromSession = sessionStorage.getItem('workspace-theme');
    if (fromSession) return fromSession;
  } catch {
    /* ignore */
  }
  return undefined;
}
function StableThemeSwitcher({
  children,
  defaultTheme
}) {
  const getInitialTheme = (0, _react().useCallback)(() => {
    const requested = defaultTheme ?? readPreflashTheme();
    return requested && themes.find(t => t.themeName === requested) || themes[0];
  }, [defaultTheme]);
  const [theme, setThemeState] = (0, _react().useState)(getInitialTheme);
  (0, _react().useEffect)(() => {
    setThemeState(getInitialTheme);
  }, [getInitialTheme]);
  const setTheme = (0, _react().useCallback)(option => {
    setThemeState(() => option);
  }, []);
  const picker = (0, _react().useMemo)(() => ({
    current: theme,
    get currentIdx() {
      return themes.indexOf(theme);
    },
    options: themes,
    setTheme
  }), [theme, setTheme]);
  const overrides = themeOverridesMap[theme.themeName || 'light'];

  // BaseTheme sets CSS variables on its wrapping <div>, so portaled DOM
  // (menus, dropdowns rendered into document.body) can't resolve them and
  // falls back to the hardcoded light defaults baked into each component.
  // Mirror the merged tokens onto :root via a <style> tag so portaled
  // content tracks the active theme.
  const rootThemeCss = (0, _react().useMemo)(() => {
    const merged = _objectSpread(_objectSpread({}, _designThemes().baseThemeDefaults), overrides);
    const decls = Object.entries(merged).map(([key, value]) => `--${key.replace(/[A-Z]/g, '-$&').toLowerCase()}: ${String(value)};`).join('');
    return `:root{${decls}}`;
  }, [overrides]);
  return /*#__PURE__*/_react().default.createElement(_baseReactThemes().ThemePickerContext.Provider, {
    value: picker
  }, /*#__PURE__*/_react().default.createElement("style", null, rootThemeCss), /*#__PURE__*/_react().default.createElement(_designThemes().BaseTheme, {
    overrides: overrides
  }, children));
}

//# sourceMappingURL=stable-theme-switcher.js.map