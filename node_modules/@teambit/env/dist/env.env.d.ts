import type { AspectEnv } from '@teambit/aspect';
import type { PackageJsonProps } from '@teambit/pkg';
import type { PreviewStrategyName } from '@teambit/preview';
import type { AspectLoaderMain } from '@teambit/aspect-loader';
import type { GetNpmIgnoreContext } from '@teambit/envs';
export declare const EnvEnvType = "env";
/**
 * a component environment built for Envs.
 */
export declare class EnvEnv {
    private aspectEnv;
    private aspectLoader;
    constructor(aspectEnv: AspectEnv, aspectLoader: AspectLoaderMain);
    icon: string;
    __getDescriptor(): Promise<{
        type: string;
    }>;
    getNpmIgnore(context?: GetNpmIgnoreContext): string[];
    getPackageJsonProps(): PackageJsonProps;
    getPreviewConfig(): {
        strategyName: PreviewStrategyName;
        splitComponentBundle: boolean;
    };
}
