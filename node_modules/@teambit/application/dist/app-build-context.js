"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.AppBuildContext = void 0;
function _appContext() {
  const data = require("./app-context");
  _appContext = function () {
    return data;
  };
  return data;
}
class AppBuildContext extends _appContext().AppContext {
  constructor(appContext, capsuleNetwork, previousTasksResults, pipeName, capsule, name, appComponent, artifactsDir, laneId) {
    super(appContext.appName, appContext.harmony, appContext.dev, appContext.appComponent, appContext.workdir, appContext.execContext, appContext.hostRootDir, appContext.port, appContext.args, appContext.workspaceComponentPath, appContext.envVariables);
    this.appContext = appContext;
    this.capsuleNetwork = capsuleNetwork;
    this.previousTasksResults = previousTasksResults;
    this.pipeName = pipeName;
    this.capsule = capsule;
    this.name = name;
    this.appComponent = appComponent;
    this.artifactsDir = artifactsDir;
    this.laneId = laneId;
  }
  static create({
    name,
    capsule,
    appComponent,
    artifactsDir,
    appContext,
    buildContext
  }) {
    return new AppBuildContext(appContext, buildContext.capsuleNetwork, buildContext.previousTasksResults, buildContext.pipeName, capsule, name, appComponent, artifactsDir, buildContext.laneId);
  }
}
exports.AppBuildContext = AppBuildContext;

//# sourceMappingURL=app-build-context.js.map