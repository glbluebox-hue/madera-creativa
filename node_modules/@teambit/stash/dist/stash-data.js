"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.StashData = void 0;
function _componentId() {
  const data = require("@teambit/component-id");
  _componentId = function () {
    return data;
  };
  return data;
}
class StashData {
  constructor(metadata, stashCompsData) {
    this.metadata = metadata;
    this.stashCompsData = stashCompsData;
  }
  toObject() {
    return {
      metadata: this.metadata,
      stashCompsData: this.stashCompsData.map(({
        id,
        hash,
        bitmapEntry,
        isNew
      }) => ({
        id: id.changeVersion(undefined).toObject(),
        hash,
        bitmapEntry,
        isNew
      }))
    };
  }
  static async fromObject(obj) {
    const stashCompsData = await Promise.all(obj.stashCompsData.map(async compData => {
      const id = _componentId().ComponentID.fromObject(compData.id);
      return {
        id,
        hash: compData.hash,
        bitmapEntry: compData.bitmapEntry,
        isNew: compData.isNew
      };
    }));
    return new StashData(obj.metadata, stashCompsData);
  }
}
exports.StashData = StashData;

//# sourceMappingURL=stash-data.js.map