"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.StashSaveCmd = exports.StashLoadCmd = exports.StashListCmd = exports.StashCmd = void 0;
function _cli() {
  const data = require("@teambit/cli");
  _cli = function () {
    return data;
  };
  return data;
}
function _legacy() {
  const data = require("@teambit/legacy.constants");
  _legacy = function () {
    return data;
  };
  return data;
}
function _bitError() {
  const data = require("@teambit/bit-error");
  _bitError = function () {
    return data;
  };
  return data;
}
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); } // eslint-disable-next-line max-classes-per-file
class StashSaveCmd {
  constructor(stash) {
    this.stash = stash;
    _defineProperty(this, "name", 'save');
    _defineProperty(this, "alias", 's');
    _defineProperty(this, "description", 'stash modified components');
    _defineProperty(this, "group", 'version-control');
    _defineProperty(this, "options", [['p', 'pattern', _legacy().COMPONENT_PATTERN_HELP], ['', 'include-new', 'EXPERIMENTAL. by default, only modified components are stashed. use this flag to include new components'], ['m', 'message <string>', 'message to be attached to the stashed components']]);
    _defineProperty(this, "loader", true);
  }
  async report(_arg, {
    pattern,
    message,
    includeNew
  }) {
    const compIds = await this.stash.save({
      pattern,
      message,
      includeNew
    });
    return (0, _cli().formatSuccessSummary)(`stashed ${compIds.length} components`);
  }
}
exports.StashSaveCmd = StashSaveCmd;
class StashListCmd {
  constructor(stash) {
    this.stash = stash;
    _defineProperty(this, "name", 'list');
    _defineProperty(this, "description", 'list stash');
    _defineProperty(this, "group", 'version-control');
    _defineProperty(this, "options", []);
    _defineProperty(this, "loader", true);
  }
  async report() {
    const list = await this.stash.list();
    return list.map(listItem => `${listItem.id} (${listItem.components.length} components) ${listItem.message || ''}`).join('\n');
  }
}
exports.StashListCmd = StashListCmd;
class StashLoadCmd {
  constructor(stash) {
    this.stash = stash;
    _defineProperty(this, "name", 'load [stash-id]');
    _defineProperty(this, "alias", 'pop');
    _defineProperty(this, "description", 'apply the changes according to the stash. if no stash-id provided, it loads the latest stash');
    _defineProperty(this, "group", 'version-control');
    _defineProperty(this, "options", [['r', 'auto-merge-resolve <merge-strategy>', 'in case of merge conflict, resolve according to the provided strategy: [ours, theirs, manual]'], ['', 'manual', 'same as "--auto-merge-resolve manual". in case of merge conflict, write the files with the conflict markers'], ['', 'force-ours', 'do not merge, preserve local files as is'], ['', 'force-theirs', 'do not merge, just overwrite with incoming files']]);
    _defineProperty(this, "loader", true);
  }
  async report([stashId], {
    autoMergeResolve,
    forceOurs,
    forceTheirs,
    manual
  }) {
    if (forceOurs && forceTheirs) {
      throw new (_bitError().BitError)('please use either --force-ours or --force-theirs, not both');
    }
    if (autoMergeResolve && autoMergeResolve !== 'ours' && autoMergeResolve !== 'theirs' && autoMergeResolve !== 'manual') {
      throw new (_bitError().BitError)('--auto-merge-resolve must be one of the following: [ours, theirs, manual]');
    }
    if (manual) autoMergeResolve = 'manual';
    const checkoutProps = {
      mergeStrategy: autoMergeResolve,
      forceOurs,
      forceTheirs
    };
    const compIds = await this.stash.loadLatest(checkoutProps, stashId);
    return (0, _cli().formatSuccessSummary)(`checked out ${compIds.length} components according to the latest stash`);
  }
}
exports.StashLoadCmd = StashLoadCmd;
class StashCmd {
  constructor(stash) {
    this.stash = stash;
    _defineProperty(this, "name", 'stash <sub-command>');
    _defineProperty(this, "description", 'temporarily save and restore component changes');
    _defineProperty(this, "extendedDescription", `temporarily stores modified component files without creating versions.
allows saving work-in-progress changes and switching context, then restoring changes later.`);
    _defineProperty(this, "group", 'version-control');
    _defineProperty(this, "options", [['p', 'pattern', _legacy().COMPONENT_PATTERN_HELP], ['m', 'message <string>', 'message to be attached to the stashed components']]);
    _defineProperty(this, "loader", true);
    _defineProperty(this, "commands", []);
  }
  async report([unrecognizedSubcommand]) {
    return `${_cli().errorSymbol} "${unrecognizedSubcommand}" is not a subcommand of "stash", please run "bit stash --help" to list the subcommands`;
  }
}
exports.StashCmd = StashCmd;

//# sourceMappingURL=stash.cmd.js.map