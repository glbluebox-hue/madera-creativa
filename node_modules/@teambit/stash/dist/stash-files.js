"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.StashFiles = void 0;
function _globby() {
  const data = _interopRequireDefault(require("globby"));
  _globby = function () {
    return data;
  };
  return data;
}
function _path() {
  const data = _interopRequireDefault(require("path"));
  _path = function () {
    return data;
  };
  return data;
}
function _fsExtra() {
  const data = _interopRequireDefault(require("fs-extra"));
  _fsExtra = function () {
    return data;
  };
  return data;
}
function _stashData() {
  const data = require("./stash-data");
  _stashData = function () {
    return data;
  };
  return data;
}
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
const STASH_DIR = 'stash';
const STASH_FILE_PREFIX = 'stash';
class StashFiles {
  constructor(workspace) {
    this.workspace = workspace;
  }
  getPath() {
    return _path().default.join(this.workspace.scope.path, STASH_DIR);
  }
  async getStashFiles() {
    const stashPath = this.getPath();
    const files = await (0, _globby().default)(`${STASH_FILE_PREFIX}-*`, {
      cwd: stashPath
    });
    return files;
  }
  async getLatestStashFile() {
    const files = await this.getStashFiles();
    const latest = files.sort().pop();
    return latest;
  }
  async getNextStashFileName() {
    const latest = await this.getLatestStashFile();
    const latestIndex = latest ? parseInt(latest.split('-')[1]) : 0;
    return `${STASH_FILE_PREFIX}-${latestIndex + 1}.json`;
  }
  async deleteStashFile(filename) {
    await _fsExtra().default.remove(_path().default.join(this.getPath(), filename));
  }
  async saveStashData(stashData) {
    const nextStashFile = await this.getNextStashFileName();
    const stashPath = this.getPath();
    const filePath = _path().default.join(stashPath, nextStashFile);
    await _fsExtra().default.outputFile(filePath, JSON.stringify(stashData.toObject(), undefined, 4));
  }
  async getStashData(filename) {
    const stashPath = this.getPath();
    const filePath = _path().default.join(stashPath, filename);
    const content = await _fsExtra().default.readFile(filePath, 'utf8');
    const data = JSON.parse(content);
    return _stashData().StashData.fromObject(data);
  }
}
exports.StashFiles = StashFiles;

//# sourceMappingURL=stash-files.js.map