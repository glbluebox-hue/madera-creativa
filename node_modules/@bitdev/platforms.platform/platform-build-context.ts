import { ComponentID } from "@teambit/component-id";
import { AppBuildContext } from "@teambit/application";
import { ArtifactDefinition } from "@teambit/builder";
import { Component } from "@teambit/component";

export type PlatformServiceBuild = {
	/**
	 * frontend, backend or another layer.
	 */
	type?: string;

	/**
	 * ID of the platform component.
	 */
	componentId?: ComponentID;

	/**
	 * list of artifacts.
	 */
	artifacts?: ArtifactDefinition[];

	/**
	 * platform component object.
	 */
	component?: Component;

	/**
	 * name of the application.
	 */
	name: string;

	/**
	 * remote URL of the application.
	 */
	url?: string;
};

export interface PlatformBuildContext extends AppBuildContext {
	/**
	 * platform service components build information.
	 */
	platformServices: PlatformServiceBuild[];

	mainFrontend: PlatformServiceBuild;

	mainBackend: PlatformServiceBuild;

	/**
	 * absolute path to the assembled platform bundle directory inside the
	 * capsule. populated by the default Platform.build() before userBuild /
	 * userDeploy run, so user hooks can read manifest.json or package the
	 * bundle further.
	 */
	bundlePath?: string;
}
